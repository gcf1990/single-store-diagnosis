# 单店销售诊断工作台 前端表单数据映射

## 1. 文档目的

本文档用于说明 Super APP 前端在读取销售、话务、试驾、打标明细和后续 Codex 输出表时，哪些页面内容来自表单数据集，哪些内容继续来自真实指标源。

本文档不新增指标口径，只做前端读取映射。指标分子分母见 `单店销售诊断工作台_数据口径文档.md`，Codex 输出字段见 `单店销售诊断工作台_Codex沉淀清单与字段口径.md`。

## 2. 读取原则

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

首期 `index-no-ai.html` 读取顺序：

1. 先按当前大区、小区、经销商、日期区间读取销售漏斗指标源。
2. 再读取邀约、试驾指标源和日趋势数据；日趋势只放已确认真实源的过程指标。
3. 读取 IP 打标明细、试驾打标明细，用于深度分析的标签分布、顾问分布、客户明细和证据抽屉。
4. 用经销商、日期区间和指标编码读取 `官方排名分位结果表`，仅给绝对值指标展示小区排名。

后续 AI 诊断阶段才读取 `Codex批次状态表` 和 `门店诊断结果表`。

当前表单采用“经销商当前态 upsert”写入策略：Codex 先完成新批次取数和本地输出生成，再按不含批次的当前态主键更新表单现有记录；写入前不删除旧数据。因此前端不需要筛选 `诊断批次ID`，批次 ID 只用于追溯和排障。

## 3. Codex 输出表单数据集

| 业务表 | 表单数据集 dsId | form 写入 fmId | 前端用途 |
|---|---|---|---|
| 门店诊断结果表 | `baf4b86a458ca4b2a8ce874d` | `a_11770b-f0d7-4349-a33d-00d76e485ef2` | 后续 AI 诊断阶段使用；首期页面不展示 AI 诊断总览 |
| 官方排名分位结果表 | `xa257b3a018be4418b6100bc` | `a_50f89c-da7c-46cc-aaa1-b09a8de9b5b5` | 销售漏斗绝对值指标的小区排名、分位、对标口径 |
| Codex 批次状态表 | `vef05c5e538074b2885f3445` | `a_1c81a3-b2b6-469d-adae-a4950184fa2f` | 后续 AI 诊断阶段使用；首期页面不展示批次状态 |

说明：

- 前端正式读取使用 `dsId`。
- Codex 写入和联调脚本使用 `fmId`。
- 表单写入后必须等表单数据集刷新，前端才能通过 `dsId` 读到最新批次。

## 4. 页面模块映射

### 4.1 顶部对象区

| 页面内容 | 数据来源 | 字段 |
|---|---|---|
| 大区 | 销售漏斗指标源 | `大区简称` |
| 小区 | 销售漏斗指标源 | `小区简称` |
| 经销商 | 销售漏斗指标源 | `经销商代码`、`经销商简称` |
| 日期 | 页面日期区间 | `开始日期`、`结束日期`，默认本月 1 日至昨天 |
| 批次状态 | 后续 AI 诊断阶段读取 Codex批次状态表 | `诊断批次ID`、`环节状态`、`源数据刷新状态`、`表单数据集刷新状态` |

说明：

- 顶部标题中的门店名称使用销售漏斗指标源的 `经销商简称`。
- 大区、小区、经销商筛选候选值首版从销售漏斗指标源中按权限范围去重生成，不单独依赖组织数据集。

### 4.2 AI 诊断总览

首期 `index-no-ai.html` 不展示门店 AI 诊断总览。本节作为后续 AI 诊断阶段预留。

| 页面内容 | 表单 | 字段 |
|---|---|---|
| 总体诊断标题 | 门店诊断结果表 | `总体诊断结论` |
| 主问题 | 门店诊断结果表 | `主问题编码`、`主问题名称` |
| 结果断点 | 门店诊断结果表 | `结果断点` |
| 过程原因 | 门店诊断结果表 | `过程原因` |
| 优先动作 | 门店诊断结果表 | `优先动作` |
| 默认聚焦模块 | 门店诊断结果表 | `聚焦数据域` |
| 默认聚焦指标 | 门店诊断结果表 | `聚焦指标编码`、`聚焦指标名称` |
| 高亮指标 | 门店诊断结果表 + 真实指标数据集 | `高亮指标编码` 标记字段；指标数值由前端从真实指标源计算 |
| 证据摘要 | 门店诊断结果表 | `证据摘要` |
| AI 解释 | 门店诊断结果表 | `AI 解释文案` |

边界：

- 前端不生成或改写 `总体诊断结论`、`主问题名称`、`结果断点`、`过程原因`、`优先动作`。
- `高亮指标编码` 只告诉前端哪些指标需要突出显示，不在诊断结果表中存基础指标值。

### 4.3 销售漏斗

| 页面内容 | 数据来源 | 字段或口径 |
|---|---|---|
| 下发线索、到店、试驾、订单 | 销售漏斗指标源 | 前端按当前门店、页面日期区间读取真实基础数值 |
| 转化率 | 前端基于销售漏斗基础数值计算 | 线索到店率、到店试驾率、试驾订单率、线索订单率 |
| 月环比 | 前端读取当前日期区间和上月同期同口径基础数值后计算 | 见数据口径文档 |
| 小区排名/分位 | Codex 每日定时任务写入的官方排名分位结果表 | `统计开始日期`、`统计结束日期`、`指标编码`、`官方排名`、`排名总数`、`官方分位`；必须匹配当前页面日期区间，当前只在绝对值指标展示 `小区排名：X/Y` |

说明：

- 销售漏斗卡片里的 `小区排名：X/Y` 不从销售漏斗指标源读取，也不由前端计算。
- 门店人员受行权限限制，前端可见门店范围不等于小区全量门店范围，因此不能用前端当前可见数据计算小区排名。
- 前端只负责按 `经销商代码 + 统计开始日期 + 统计结束日期 + 指标编码 + rank_scope_type = 小区` 读取 Codex 排名表结果并格式化展示。

销售漏斗小区排名建议读取指标编码：

| 页面指标 | 排名表指标编码 |
|---|---|
| 下发线索 | `assigned_leads` |
| 到店 | `arrivals` |
| 试驾 | `test_drives` |
| 订单 | `orders` |
| 线索到店率 | `lead_to_arrival_rate`，当前页面不展示小区排名 |
| 到店试驾率 | `arrival_to_drive_rate`，当前页面不展示小区排名 |
| 试驾订单率 | `drive_to_order_rate`，当前页面不展示小区排名 |
| 线索订单率 | `lead_to_order_rate`，当前页面不展示小区排名 |

### 4.4 邀约

| 页面内容 | 数据来源 | 字段或口径 |
|---|---|---|
| 线索接通率 | DCC 话务指标源 | 前端按 182 看板 `72小时线索外呼接通率` 口径计算 |
| 30s以下线索占比 | DCC 话务指标源 | 前端按数据口径计算 |
| 30分钟外呼率 | DCC 话务指标源 | 前端按 182 看板 `(10-18)30分钟外呼率` 口径计算 |
| 2天3呼率 | DCC 话务指标源 | 前端按 182 看板 `2天3呼达标率` 口径计算 |
| 负向邀约占比 | `IP电话邀约问题诊断明细表-202606后`（dsId: `n418e47dacdb94291993d3d9`） | 前端按负向电话数 / 总通话数计算；分子、分母均按 `呼叫编码` 去重 |
| 邀约负向明细数 | 门店诊断结果表 | `邀约负向明细数` |
| 邀约样本量 | 门店诊断结果表 | `邀约样本量` |
| 邀约相关官方排名/分位 | 官方排名分位结果表 | 按 `指标编码` 匹配 |

说明：

- IP 打标明细真实数据集已确认，电话邀约深度分析和负向邀约占比读取 `n418e47dacdb94291993d3d9`。
- 正式前端的标签分布、顾问分布、客户明细不从 Codex 结果表读取，应从 IP 打标明细数据集读取。
- DCC 话务指标源使用 `【双品牌】DCC话务指标_182`，日期字段为 `下发CRM时间`；当前过滤条件为 `品牌名称 = MG`、`开业状态 = 1`、`data_type_ch != 来电咨询`、`线索免考核 = 待考核`、`需跟进 = 需跟进`，渠道大类限定为厂方新媒体、媒介投放、官网及电商、经销商新媒体、网销平台、基地、官方新媒体、MCN。
- 邀约趋势中 DCC 四个过程指标使用 182 数据集按 `下发CRM时间` 日期聚合；`负向邀约占比` 使用 IP 打标明细按 `呼叫开始时间` 日期聚合。

### 4.5 试驾

| 页面内容 | 数据来源 | 字段或口径 |
|---|---|---|
| 试驾平均里程 | 试驾明细宽表 | 前端按数据口径计算 |
| 平均时长 | 试驾明细宽表 | 前端按数据口径计算 |
| 负向试驾接待占比 | 试驾打标明细数据集 | 前端按负向明细数 / 有试驾原文样本数计算 |
| 试驾接待负向明细数 | 门店诊断结果表 | `试驾接待负向明细数` |
| 试驾接待样本量 | 门店诊断结果表 | `试驾接待样本量` |
| 试驾相关官方排名/分位 | 官方排名分位结果表 | 按 `指标编码` 匹配 |

说明：

- 试驾打标明细真实数据集上线前，联调用 mock 明细。
- 正式前端的标签分布、顾问分布、客户明细不从 Codex 结果表读取，应从试驾打标明细数据集读取。
- 试驾趋势使用试驾明细宽表按 `试驾接待日期` 日期聚合；`负向试驾接待占比` 在试驾打标明细真实数据集未确认前继续标记 mock，不进入真实趋势图。

### 4.6 深度分析和证据抽屉

| 页面内容 | 数据来源 | 说明 |
|---|---|---|
| 标签分布 | IP/试驾打标明细数据集 | 电话邀约按 `一级标签`、`二级标签` 聚合负向标签行；一通电话多标签可贡献多条标签命中 |
| 顾问分布 | IP/试驾打标明细数据集 | 电话邀约按 `顾问编码 + 呼叫编码` 去重统计负向电话数 |
| 客户明细 | IP/试驾打标明细数据集 | 电话邀约展示客户、顾问、`呼叫开始时间`、`一级标签`、`二级标签`、命中原因；明细表和详情抽屉均需同时展示一级、二级问题 |
| 证据摘录 | IP/试驾打标明细数据集 | 电话邀约使用 `证据原文-客户`、`证据原文-顾问` |
| 标签命中原因 | IP/试驾打标明细数据集 | 使用数仓加工后的 `命中原因` |
| 建议话术 | IP/试驾打标明细数据集 | 如数仓提供则展示；否则本期不由前端临时生成 |

## 5. 前端默认筛选条件

| 场景 | 筛选条件 |
|---|---|
| 读取当前指标 | `经销商代码 = 当前门店`、`日期 >= 页面开始日期`、`日期 <= 页面结束日期` |
| 读取排名 | `经销商代码 = 当前门店`、`统计开始日期 = 页面开始日期`、`统计结束日期 = 页面结束日期`、`指标编码 = 页面指标编码` |
| 读取批次状态 | 后续 AI 诊断阶段使用；`经销商代码 = 当前门店`，按 `运行日期` 或 `生成时间` 取最新 |
| 读取指定联调批次 | 仅排障时使用 `诊断批次ID = 指定批次ID` |

异常展示：

- 表单层已写入但数据集层未刷新：这是观远每日定时刷新边界，不属于 Codex 写表任务失败；前端在数据集刷新前仍会读取到上一轮数据。
- 无当前指标：展示指标空态。
- 后续 AI 诊断阶段无最新成功批次：展示无诊断结果。
- `诊断状态 = 样本不足`：后续 AI 诊断阶段保留基础指标展示，诊断总览提示样本不足。
- `数据状态 = 明细为空`：保留销售漏斗和过程指标，隐藏或置空深度分析证据。

## 6. 联调验收口径

指定一个测试批次后，前端表单数据准备完成需同时满足：

| 验收项 | 通过标准 |
|---|---|
| 门店诊断结果表 | 后续 AI 诊断阶段：当前日期区间、当前门店能读取到 1 条诊断结果 |
| 官方排名分位结果表 | 当前日期区间、当前门店、页面所需绝对值指标能读取到小区排名或分位 |
| Codex批次状态表 | 后续 AI 诊断阶段：当前门店能读取到拉取数据、生成诊断、写入表单等环节状态 |
| 表单数据集刷新 | 由观远每日定时刷新验证，不作为 Codex 写表即时验收项 |
| 前端字段覆盖 | 销售漏斗、过程指标、深度分析和小区排名所需字段均非空或有明确异常状态；后续 AI 诊断字段单独验收 |
| 趋势图数据 | 邀约趋势中 182 指标按 `下发CRM时间` 日聚合，`负向邀约占比` 按 IP 打标明细 `呼叫开始时间` 日聚合；试驾趋势来自试驾明细宽表按 `试驾接待日期` 日聚合，`负向试驾接待占比` 在试驾打标明细未确认前不进入真实趋势 |

可用脚本：

```bash
python3 scripts/validate_codex_batch_visibility.py --batch-id <诊断批次ID>
```

该脚本用于排障时验证指定批次是否已写入表单，不作为前端日常读取条件。
