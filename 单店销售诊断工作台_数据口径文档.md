# 单店销售诊断工作台 数据口径文档

## 1. 文档目的

本文档定义单店销售诊断工作台当前页面已展示指标的分子、分母、过滤条件、去重规则、异常处理和前端计算边界。

本文档只覆盖 `index.html` 当前页面范围：

- 销售漏斗指标。
- 邀约过程指标。
- 试驾过程指标。
- 月环比与趋势方向。
- 标签分布、顾问分布和明细计数的聚合口径。

不在本文档展开：

- Codex 官方诊断结论、主问题、结果断点、优先动作字段。见 `单店销售诊断工作台_Codex沉淀清单与字段口径.md`。
- 官方排名、官方分位、跨组织对标计算逻辑。排名/分位必须由数据层或 Codex 提前预计算并落表，前端只读。
- 数仓 AI 标签、证据摘录、标签命中原因的模型规则和 Prompt 细节。

## 2. 核心原则

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

| 类型 | 处理方式 |
|---|---|
| 基础销售指标 | 前端从观远销售指标数据集读取后，在当前门店和当前月份范围内计算 |
| 过程指标 | 前端从观远过程指标或数仓明细读取后，在当前门店和当前月份范围内计算 |
| AI 负向标签 | 数仓加工后提供，前端只做汇总，不重新判定 |
| 官方诊断 | Codex 每日任务沉淀，前端只读 |
| 官方排名/分位 | 数据层或 Codex 预计算落表，前端只读 |

## 3. 通用过滤与周期口径

| 项目 | 默认口径 |
|---|---|
| 统计对象 | 当前经销商代码对应门店 |
| 统计周期 | 当前页面选择月份，按自然月统计 |
| 月份格式 | `YYYY-MM` |
| 日期边界 | 包含统计月份首日 00:00:00 至月末 23:59:59 |
| 组织过滤 | 大区、小区、经销商来自观远权限范围和页面筛选 |
| 权限边界 | 前端只能读取当前用户有权限的数据 |
| 删除/作废记录 | 默认剔除已删除、已作废、测试、重复导入记录 |
| 空值处理 | 分子或分母缺失时展示为空或 `--`，不展示 0% 误导用户 |
| 除零处理 | 分母为 0 时指标值为空，趋势方向为空 |

## 4. 销售漏斗指标

### 4.1 指标清单

| 指标编码 | 展示名称 | 单位 | 类型 | 是否前端计算 | 数据来源 |
|---|---|---|---|---:|---|
| `assigned_leads` | 下发线索 | 条 | 绝对量 | 是 | 门店销售指标数据集 |
| `arrivals` | 到店 | 人 | 绝对量 | 是 | 门店销售指标数据集 |
| `test_drives` | 试驾 | 人 | 绝对量 | 是 | 门店销售指标数据集 |
| `orders` | 订单 | 单 | 绝对量 | 是 | 门店销售指标数据集 |
| `lead_to_arrival_rate` | 线索到店率 | % | 转化率 | 是 | 门店销售指标数据集 |
| `arrival_to_drive_rate` | 到店试驾率 | % | 转化率 | 是 | 门店销售指标数据集 |
| `drive_to_order_rate` | 试驾订单率 | % | 转化率 | 是 | 门店销售指标数据集 |
| `lead_to_order_rate` | 线索订单率 | % | 转化率 | 是 | 门店销售指标数据集 |

### 4.2 绝对量指标口径

| 指标 | 分子/取值 | 去重规则 | 过滤条件 | 异常处理 |
|---|---|---|---|---|
| 下发线索 | 当前门店当前月份下发线索数 | 按线索ID去重 | 剔除测试线索、作废线索、重复下发记录 | 无线索时为 0 |
| 到店 | 当前门店当前月份有效到店客户数 | 优先按客户ID去重；无客户ID按线索ID去重 | 剔除无效到店、重复到店、非本店接待记录 | 无到店时为 0 |
| 试驾 | 当前门店当前月份有效试驾客户数 | 优先按客户ID去重；同一客户多次试驾计 1 人 | 剔除取消试驾、无效试驾、测试试驾 | 无试驾时为 0 |
| 订单 | 当前门店当前月份有效订单数 | 按订单ID去重 | 剔除取消订单、作废订单、测试订单 | 无订单时为 0 |

说明：

- 漏斗绝对量用于判断结果断点，但官方诊断结论不由前端生成。
- 当前 demo 仅展示绝对量卡片的小区排名；排名值来自 `官方排名/分位结果表`。

### 4.3 转化率指标口径

| 指标 | 分子 | 分母 | 公式 | 异常处理 |
|---|---|---|---|---|
| 线索到店率 | 到店 | 下发线索 | 到店 / 下发线索 * 100% | 下发线索为 0 时为空 |
| 到店试驾率 | 试驾 | 到店 | 试驾 / 到店 * 100% | 到店为 0 时为空 |
| 试驾订单率 | 订单 | 试驾 | 订单 / 试驾 * 100% | 试驾为 0 时为空 |
| 线索订单率 | 订单 | 下发线索 | 订单 / 下发线索 * 100% | 下发线索为 0 时为空 |

展示规则：

- 百分比默认保留 1 位小数。
- 分母为 0 时展示 `--`，不展示 0.0%。
- 转化率卡片不由前端生成官方排名；如需展示分位，读取官方排名/分位结果表。

## 5. 邀约过程指标

### 5.1 指标清单

| 指标编码 | 展示名称 | 单位 | 类型 | 是否前端计算 | 数据来源 |
|---|---|---|---|---:|---|
| `ip_connect_rate` | IP 电话接通率 | % | 占比 | 是 | 门店过程指标或 IP 打标明细 |
| `short_call_under_30s_rate` | 30s 以下线索占比 | % | 占比 | 是 | 门店过程指标或 IP 打标明细 |
| `outbound_within_30min_rate` | 30 分钟外呼率 | % | 占比 | 是 | 门店过程指标或 IP 打标明细 |
| `two_day_three_call_rate` | 2 天 3 呼率 | % | 占比 | 是 | 门店过程指标或 IP 打标明细 |
| `negative_invitation_rate` | 负向邀约占比 | % | 占比 | 是 | 数仓 IP 打标明细 |

### 5.2 过程指标口径

| 指标 | 分子 | 分母 | 公式 | 关键过滤 |
|---|---|---|---|---|
| IP 电话接通率 | 有至少 1 通有效接通的线索数 | 有 IP 电话触达要求的线索数 | 分子 / 分母 * 100% | 只统计当前门店、当前月份有效线索 |
| 30s 以下线索占比 | 首次或关键通话接通时长小于 30 秒的线索数 | 有有效通话记录的线索数 | 分子 / 分母 * 100% | 未接通线索不进入分母 |
| 30 分钟外呼率 | 线索下发后 30 分钟内发生首次有效外呼的线索数 | 当前月份下发且需要外呼的线索数 | 分子 / 分母 * 100% | 外呼起点为线索下发时间 |
| 2 天 3 呼率 | 线索下发后 2 天内累计外呼次数不少于 3 次的线索数 | 当前月份下发且需要外呼的线索数 | 分子 / 分母 * 100% | 默认统计外呼次数，不要求全部接通 |
| 负向邀约占比 | 被数仓标记为负向邀约的 IP 事件数或线索数 | 已完成 AI 打标的 IP 事件数或线索数 | 分子 / 分母 * 100% | 负向判定来自数仓，不由前端判定 |

### 5.3 邀约过程去重规则

| 场景 | 默认规则 |
|---|---|
| 同一线索多通电话 | 接通率按线索去重，只要有 1 通接通即计入接通 |
| 30s 以下线索 | 按线索去重，默认取首次有效接通通话时长判断 |
| 30 分钟外呼 | 按线索去重，取线索下发后的首次外呼时间判断 |
| 2 天 3 呼 | 按线索去重，统计下发后 48 小时内外呼次数 |
| 负向邀约 | 首版按事件级汇总；如同一事件多标签，只计 1 次负向事件 |

### 5.4 邀约过程异常处理

| 异常 | 处理方式 |
|---|---|
| 缺少线索下发时间 | 不纳入 30 分钟外呼率、2 天 3 呼率分母 |
| 缺少通话时长 | 不纳入 30s 以下线索占比分母 |
| IP 明细未完成 AI 打标 | 不纳入负向邀约占比分母 |
| 分母为 0 | 指标展示为空，趋势方向为空 |
| 负向标签多标签命中 | 同一事件只计 1 次负向，标签分布可按标签长表统计 |

## 6. 试驾过程指标

### 6.1 指标清单

| 指标编码 | 展示名称 | 单位 | 类型 | 是否前端计算 | 数据来源 |
|---|---|---|---|---:|---|
| `avg_test_drive_mileage_km` | 试驾平均里程 | km | 均值 | 是 | 门店过程指标或试驾打标明细 |
| `avg_test_drive_duration_min` | 平均时长 | min | 均值 | 是 | 门店过程指标或试驾打标明细 |
| `negative_reception_rate` | 负向试驾接待占比 | % | 占比 | 是 | 数仓试驾打标明细 |

### 6.2 过程指标口径

| 指标 | 分子 | 分母 | 公式 | 关键过滤 |
|---|---|---|---|---|
| 试驾平均里程 | 有效试驾里程求和 | 有有效里程的试驾事件数 | 分子 / 分母 | 剔除取消、测试、里程缺失或异常试驾 |
| 平均时长 | 有效试驾时长求和 | 有有效时长的试驾事件数 | 分子 / 分母 | 剔除取消、测试、时长缺失或异常试驾 |
| 负向试驾接待占比 | 被数仓标记为负向试驾接待的试驾事件数 | 已完成 AI 打标的试驾事件数 | 分子 / 分母 * 100% | 负向判定来自数仓，不由前端判定 |

### 6.3 试驾过程去重规则

| 场景 | 默认规则 |
|---|---|
| 同一客户多次试驾 | 均值指标按试驾事件统计，不按客户去重 |
| 同一试驾事件多标签 | 负向接待占比同一事件只计 1 次负向 |
| 试驾里程异常 | 首版剔除小于等于 0 或明显异常高值的试驾事件，阈值由数仓或数据契约提供 |
| 试驾时长异常 | 首版剔除小于等于 0 或明显异常高值的试驾事件，阈值由数仓或数据契约提供 |

### 6.4 试驾过程异常处理

| 异常 | 处理方式 |
|---|---|
| 里程缺失 | 不纳入试驾平均里程分子和分母 |
| 时长缺失 | 不纳入平均时长分子和分母 |
| 试驾明细未完成 AI 打标 | 不纳入负向试驾接待占比分母 |
| 分母为 0 | 指标展示为空，趋势方向为空 |
| 负向标签多标签命中 | 同一试驾事件只计 1 次负向，标签分布可按标签长表统计 |

## 7. 月环比与趋势方向

| 项目 | 口径 |
|---|---|
| 本期 | 当前选择月份的同口径指标值 |
| 上期 | 上一自然月同口径指标值 |
| 绝对量环比 | (本期值 - 上期值) / 上期值 * 100% |
| 百分比指标环比 | 本期百分比 - 上期百分比，单位为 pct |
| 均值指标环比 | 本期均值 - 上期均值，保留原单位 |
| 趋势方向 | 环比大于 0 为上升，小于 0 为下降，等于 0 为持平 |

异常处理：

- 上期分母为 0 或上期值为空时，不展示环比。
- 本期值为空时，不展示环比和趋势方向。
- 负向指标上升表示风险加重，前端样式需要和业务方向区分，不仅按数值上升判断好坏。

## 8. 标签分布与顾问分布

### 8.1 标签分布

| 项目 | 口径 |
|---|---|
| 数据来源 | 数仓 IP 打标明细、数仓试驾打标明细 |
| 一级标签数量 | 当前门店、当前月份、当前 Tab 下对应一级标签命中的记录数 |
| 一级标签占比 | 一级标签数量 / 当前 Tab 下全部标签命中记录数 |
| 二级标签数量 | 当前一级标签下各二级标签命中的记录数 |
| 多标签处理 | 标签分布按标签长表统计，同一事件多标签可贡献多条标签命中 |

### 8.2 顾问分布

| 项目 | 口径 |
|---|---|
| 数据来源 | 数仓 IP 打标明细、数仓试驾打标明细 |
| 顾问问题数 | 当前门店、当前月份、当前 Tab 下顾问关联的负向或问题标签记录数 |
| 顾问风险等级 | 优先读取数仓已加工风险等级；无风险等级时不由前端临时判定 |
| 排序 | 前端可按当前权限范围内顾问问题数降序展示 |
| 筛选影响 | 顾问筛选只影响第三层明细，不反算上方漏斗、过程指标和官方诊断 |

## 9. 明细清单与证据抽屉

| 页面字段 | 数据来源 | 口径 |
|---|---|---|
| 客户 | 数仓打标明细 | 使用脱敏展示名 |
| 顾问 | 数仓打标明细 | 使用顾问展示名 |
| 事件时间 | 数仓打标明细 | IP 使用通话时间；试驾使用试驾时间 |
| AI 标签 | 数仓打标明细 | 一级标签展示，二级标签可在后续扩展 |
| 问题摘要 | 数仓打标明细 | 数仓加工结果，前端只读 |
| 风险等级 | 数仓打标明细 | 中文枚举值，前端直接展示 |
| 证据摘录 | 数仓打标明细 | 数仓加工并脱敏后的证据 |
| 标签命中原因 | 数仓打标明细 | 用于解释标签为何命中 |
| 建议话术 | 数仓打标明细 | 数仓加工结果，前端只读 |

## 10. 前端展示格式

| 类型 | 展示格式 |
|---|---|
| 绝对量 | 整数，带单位，例如 `168 条` |
| 百分比 | 保留 1 位小数，例如 `28.0%` |
| pct 环比 | 保留 1 位小数，例如 `-1.6pct` |
| km | 保留 1 位小数，例如 `4.8km` |
| min | 默认整数，例如 `18min` |
| 空值 | 展示 `--` |
| 排名 | 来自官方排名/分位结果表，可展示为 `排名：X/Y` |

## 11. 后续待固化事项

| 事项 | 说明 |
|---|---|
| 源数据集字段名 | 需在数据契约中明确每个指标读取的观远数据集字段 |
| 组织权限字段 | 需在数据契约中明确大区、小区、经销商权限字段 |
| 异常高值阈值 | 试驾里程、试驾时长异常阈值需由数仓或业务确认 |
| 负向标签字典 | IP 与试驾负向标签字典由数仓打标体系维护 |
| 样本不足阈值 | Codex 诊断样本不足阈值需和数据口径保持一致 |

