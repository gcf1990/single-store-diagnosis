# 单店销售诊断工作台 数据口径文档

## 1. 文档目的

本文档定义单店销售诊断工作台当前页面已展示指标的分子、分母、过滤条件、去重规则、异常处理和前端计算边界。

本文档只覆盖 `index-no-ai.html` 首期当前页面范围：

- 销售漏斗指标。
- 邀约指标。
- 试驾指标。
- 月环比与趋势方向。
- 标签分布、顾问分布和明细计数的聚合口径。

不在本文档展开：

- Codex 官方诊断结论、主问题、结果断点、优先动作字段。见 `单店销售诊断工作台_Codex沉淀清单与字段口径.md`。
- 官方排名、官方分位、跨组织对标计算逻辑。排名/分位必须由数据层或 Codex 提前预计算并落表，前端只读。
- 数仓 AI 标签、证据摘录、标签命中原因的模型规则和 Prompt 细节。

## 2. 核心原则

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

| 类型 | 处理方式 |
|---|---|
| 基础销售指标 | 前端从观远销售指标数据集读取后，在当前门店和页面日期区间内计算 |
| 过程指标 | 前端从观远过程指标或数仓明细读取后，在当前门店和页面日期区间内计算 |
| AI 负向标签 | 数仓加工后提供，前端只做汇总，不重新判定 |
| 官方诊断 | 后续 AI 诊断阶段由 Codex 每日任务沉淀，首期页面不展示 |
| 官方排名/分位 | 数据层或 Codex 预计算落表，前端只读 |

## 3. 通用过滤与周期口径

| 项目 | 默认口径 |
|---|---|
| 统计对象 | 当前经销商代码对应门店 |
| 统计周期 | 当前页面日期筛选器选择的开始日期至结束日期 |
| Codex 默认处理范围 | `2026-03-01` 至任务执行日前一天；包含昨天，不包含今天 |
| 日期格式 | `YYYY-MM-DD` |
| 日期边界 | 包含开始日期 00:00:00 至结束日期 23:59:59 |
| 上月同期 | 将当前日期区间整体平移到上一个自然月；例如 `2026-06-01` 至 `2026-06-24` 对比 `2026-05-01` 至 `2026-05-24` |
| 组织过滤 | 大区、小区、经销商来自观远权限范围和页面筛选 |
| 顶部对象区字段来源 | 大区展示取销售漏斗指标源 `大区简称`，小区展示取 `小区简称`，经销商展示取 `经销商简称`，经销商主键取 `经销商代码` |
| 权限边界 | 前端只能读取当前用户有权限的数据 |
| 删除/作废记录 | 默认剔除已删除、已作废、测试、重复导入记录 |
| 空值处理 | 分子或分母缺失时展示为空或 `--`，不展示 0% 误导用户 |
| 除零处理 | 分母为 0 时指标值为空，趋势方向为空 |

## 4. 销售漏斗指标

### 4.1 指标清单

| 指标编码 | 展示名称 | 单位 | 类型 | 是否前端计算 | 数据来源 |
|---|---|---|---|---:|---|
| `assigned_leads` | 下发线索 | 条 | 绝对量 | 是 | 门店销售指标数据集 |
| `arrivals` | 到店 | 人 | 绝对量 | 是 | 门店销售指标数据集 |
| `test_drives` | 试驾 | 人 | 绝对量 | 是 | 门店销售指标数据集 |
| `orders` | 订单 | 单 | 绝对量 | 是 | 门店销售指标数据集 |
| `lead_to_arrival_rate` | 线索到店率 | % | 转化率 | 是 | 门店销售指标数据集 |
| `arrival_to_drive_rate` | 到店试驾率 | % | 转化率 | 是 | 门店销售指标数据集 |
| `drive_to_order_rate` | 试驾订单率 | % | 转化率 | 是 | 门店销售指标数据集 |
| `lead_to_order_rate` | 线索订单率 | % | 转化率 | 是 | 门店销售指标数据集 |

### 4.2 绝对量指标口径

| 指标 | 分子/取值 | 去重规则 | 过滤条件 | 异常处理 |
|---|---|---|---|---|
| 下发线索 | 页面日期区间内 `当日下发线索数` 求和 | 数据集已为门店日粒度聚合，前端不再做明细去重 | 当前门店、当前日期区间 | 无线索时为 0 |
| 到店 | 页面日期区间内 `当日首触客流数` 求和 | 数据集已为门店日粒度聚合，前端不再做明细去重 | 当前门店、当前日期区间 | 无到店时为 0 |
| 试驾 | 页面日期区间内 `当日首触试驾数` 求和 | 数据集已为门店日粒度聚合，前端不再做明细去重 | 当前门店、当前日期区间 | 无试驾时为 0 |
| 订单 | 页面日期区间内 `当日订单数（首触）` 求和 | 数据集已为门店日粒度聚合，前端不再做明细去重 | 当前门店、当前日期区间 | 无订单时为 0 |

说明：

- 漏斗绝对量用于判断结果断点，但官方诊断结论不由前端生成。
- 当前 demo 仅展示绝对量卡片的小区排名；排名值固定来自 Codex 每日定时任务写入的 `官方排名/分位结果表`。
- 由于门店人员存在行权限限制，前端不能基于当前可见数据计算本店在小区内的排名，也不能用销售漏斗指标源临时推算排名。
- 页面展示名“到店、试驾、订单”为简化名称，业务口径分别为首触到店、首触试驾、首触订单。

### 4.3 转化率指标口径

| 指标 | 分子 | 分母 | 公式 | 异常处理 |
|---|---|---|---|---|
| 线索到店率 | 到店 | 下发线索 | 到店 / 下发线索 * 100% | 下发线索为 0 时为空 |
| 到店试驾率 | 试驾 | 到店 | 试驾 / 到店 * 100% | 到店为 0 时为空 |
| 试驾订单率 | 订单 | 试驾 | 订单 / 试驾 * 100% | 试驾为 0 时为空 |
| 线索订单率 | 订单 | 下发线索 | 订单 / 下发线索 * 100% | 下发线索为 0 时为空 |

展示规则：

- 百分比默认保留 1 位小数。
- 分母为 0 时展示 `--`，不展示 0.0%。
- 转化率卡片不由前端生成官方排名；如需展示分位，读取官方排名/分位结果表。

## 5. 邀约指标

### 5.1 指标清单

| 指标编码 | 展示名称 | 单位 | 类型 | 是否前端计算 | 数据来源 |
|---|---|---|---|---:|---|
| `ip_connect_rate` | 线索接通率 | % | 占比 | 是 | `【双品牌】DCC话务指标_182` |
| `short_call_under_30s_rate` | 30s 以下线索占比 | % | 占比 | 是 | `【双品牌】DCC话务指标_182` |
| `outbound_within_30min_rate` | 30 分钟外呼率 | % | 占比 | 是 | `【双品牌】DCC话务指标_182` |
| `two_day_three_call_rate` | 2 天 3 呼率 | % | 占比 | 是 | `【双品牌】DCC话务指标_182` |
| `negative_invitation_rate` | 负向邀约占比 | % | 占比 | 是 | `IP电话邀约问题诊断明细表-202606后` |

### 5.2 过程指标口径

| 指标 | 分子 | 分母 | 公式 | 关键过滤 |
|---|---|---|---|---|
| 线索接通率 | 按 `线索编码` 去重统计 `线索下发72小时外呼接通次数 > 0` 的线索数 | 按 `线索编码` 去重统计线索数 | 分子 / 分母 * 100% | 对齐 182 看板字段 `72小时线索外呼接通率` |
| 30s 以下线索占比 | 去重统计 `72小时总通话时长 < 30` 或 `72小时总通话时长` 为空、null 的 `线索编码` 数 | `count(distinct 线索编码)` | 分子 / 分母 * 100% | 当前门店、页面日期区间，`线索编码` 为空不参与统计 |
| 30 分钟外呼率 | `工作时段30分钟跟进（10-18） = 是` 的线索行数 | `是否工作时段线索（10-18） = 工作时段` 的线索行数 | 分子 / 分母 * 100% | 对齐 182 看板字段 `(10-18) 30分钟外呼率` |
| 2 天 3 呼率 | `是否完成72小时三呼 = 是` 的线索行数 | 当前口径过滤后的 `线索编码` 非空行数 | 分子 / 分母 * 100% | 对齐 182 看板字段 `2天3呼达标率` |
| 负向邀约占比 | 当前门店页面日期区间内，按 `呼叫编码` 去重；同一通电话任一行 `标签正负向 = 负向` 即计入分子 | 当前门店页面日期区间内，`IP电话邀约问题诊断明细表-202606后` 按 `呼叫编码` 去重的全部通话数 | 分子 / 分母 * 100% | 负向判定来自数仓，不由前端判定；未命中标签的电话进入分母，不进分子 |

182 话务指标通用过滤条件：

| 过滤项 | 字段 | 条件 |
|---|---|---|
| 门店 | `经销商代码` / `经销商简称` | 当前页面所选门店；前端优先用销售漏斗源返回的 `经销商代码` 过滤 |
| 日期 | `下发CRM时间` | 页面开始日期 `00:00:00` 至结束日期 `23:59:59` |
| 品牌 | `品牌名称` | `MG` |
| 渠道大类 | `线索渠道大类名称` | `厂方新媒体`、`媒介投放`、`官网及电商`、`经销商新媒体`、`网销平台`、`基地`、`官方新媒体`、`MCN` |
| 开业状态 | `开业状态` | `1` |
| 数据类型 | `data_type_ch` | 排除 `来电咨询` |
| 考核范围 | `线索免考核` | `待考核` |
| 跟进范围 | `需跟进` | `需跟进` |

校验样例：宁波宁爵，`2026-06-01` 至 `2026-06-24`，上述过滤后 `线索接通率 = 57.8%`、`30分钟外呼率 = 85.7%`、`2天3呼率 = 87.2%`，与 182 看板红框字段一致。

### 5.3 邀约去重规则

| 场景 | 默认规则 |
|---|---|
| DCC 线索数统计 | `线索接通率` 按 `线索编码` 去重；`30分钟外呼率`、`2天3呼率` 按 182 看板对应字段的行数口径计算；`线索编码` 为空不参与统计 |
| 同一线索多通电话 | 接通率按线索去重，只要有 1 通接通即计入接通 |
| 30s 以下线索 | 按 `线索编码` 去重，基于 `72小时总通话时长` 判断；小于 30 或为空、null 均计入分子 |
| 30 分钟外呼 | 对齐 182 看板 `(10-18)` 字段：`工作时段30分钟跟进（10-18）` / `是否工作时段线索（10-18）` |
| 2 天 3 呼 | 对齐 182 看板 `2天3呼达标率` 字段：`是否完成72小时三呼 = 是` / 口径过滤后线索行数 |
| 负向邀约 | 按 `呼叫编码` 去重汇总；如同一通电话多标签或正负共存，只要任一标签为负向，该通电话计 1 次负向 |

### 5.4 邀约异常处理

| 异常 | 处理方式 |
|---|---|
| 缺少线索下发时间 | 不纳入 30 分钟外呼率、2 天 3 呼率分母 |
| 缺少 `72小时总通话时长` | 计入 30s 以下线索占比分子，仍纳入分母 |
| 标签正负向为空 | 说明该通电话未命中正向或负向标签；纳入负向邀约占比分母，不纳入正/负向分子 |
| 免考核/无需跟进/未开业 | 182 话务指标已明确过滤：`线索免考核 = 待考核`、`需跟进 = 需跟进`、`开业状态 = 1` |
| 分母为 0 | 指标展示为空，趋势方向为空 |
| 负向标签多标签命中 | 同一事件只计 1 次负向，标签分布可按标签长表统计 |

## 6. 试驾指标

### 6.1 指标清单

| 指标编码 | 展示名称 | 单位 | 类型 | 是否前端计算 | 数据来源 |
|---|---|---|---|---:|---|
| `avg_test_drive_mileage_km` | 试驾平均里程 | km | 均值 | 是 | `[准实时]试驾明细宽表&ads_sale_mart_trial_dtl_wide_comb_wms` |
| `avg_test_drive_duration_min` | 平均时长 | min | 均值 | 是 | `[准实时]试驾明细宽表&ads_sale_mart_trial_dtl_wide_comb_wms` |
| `negative_reception_rate` | 负向试驾接待占比 | % | 占比 | 是 | `试驾顾问接待问题诊断表` |

### 6.2 过程指标口径

| 指标 | 分子 | 分母 | 公式 | 关键过滤 |
|---|---|---|---|---|
| 试驾平均里程 | 当前门店页面日期区间内，筛选 `是否成功试驾 = 是` 后的 `试驾里程` 求和 | 筛选 `是否成功试驾 = 是` 后，按 `试驾接待编码` 去重的有效试驾事件数 | 分子 / 分母 | 来源数据集 `c6428f1c9ca204859b553421` |
| 平均时长 | 当前门店页面日期区间内，筛选 `是否成功试驾 = 是` 后的 `试驾时长(分钟)` 求和 | 筛选 `是否成功试驾 = 是` 后，按 `试驾接待编码` 去重的有效试驾事件数 | 分子 / 分母 | 来源数据集 `c6428f1c9ca204859b553421` |
| 负向试驾接待占比 | 按 `试驾清单ID` 去重且 `标签正负向 = 负向` 的数量 | 当前门店页面日期区间内有试驾原文且按 `试驾清单ID` 去重的试驾事件数 | 分子 / 分母 * 100% | 负向判定来自数仓，不由前端判定 |

### 6.3 试驾去重规则

| 场景 | 默认规则 |
|---|---|
| 同一客户多次试驾 | 均值指标按试驾事件统计，不按客户去重 |
| 同一试驾事件多标签 | 负向接待占比同一事件只计 1 次负向 |
| 试驾里程异常 | 首版剔除小于等于 0 或明显异常高值的试驾事件，阈值由数仓或数据契约提供 |
| 试驾时长异常 | 首版剔除小于等于 0 或明显异常高值的试驾事件，阈值由数仓或数据契约提供 |

### 6.4 试驾异常处理

| 异常 | 处理方式 |
|---|---|
| 里程缺失 | 不纳入试驾平均里程分子和分母 |
| 时长缺失 | 不纳入平均时长分子和分母 |
| 缺少试驾原文 | 不纳入负向试驾接待占比分母 |
| 分母为 0 | 指标展示为空，趋势方向为空 |
| 负向标签多标签命中 | 同一试驾事件只计 1 次负向，标签分布可按标签长表统计 |

## 7. 月环比与趋势方向

| 项目 | 口径 |
|---|---|
| 本期 | 当前页面日期区间的同口径指标值 |
| 上期 | 当前页面日期区间对应的上月同期指标值 |
| 绝对量环比 | (本期值 - 上期值) / 上期值 * 100% |
| 百分比指标环比 | 本期百分比 - 上期百分比，单位为 pct |
| 均值指标环比 | 本期均值 - 上期均值，保留原单位 |
| 趋势方向 | 环比大于 0 为上升，小于 0 为下降，等于 0 为持平 |

上月同期规则：

- 当前日期区间为 `开始日期 S` 至 `结束日期 E`。
- 上月同期为 `S` 和 `E` 分别向前平移一个自然月后的日期区间。
- 如果上月不存在对应日期，取上月最后一天。例如 `2026-03-31` 上月同期结束日为 `2026-02-28`。
- 环比计算使用上月同期的完整可用数据；如果上月同期任一关键日期无数据，按空值处理，不用 0 补齐。

异常处理：

- 上期分母为 0 或上期值为空时，不展示环比。
- 本期值为空时，不展示环比和趋势方向。
- 负向指标上升表示问题加重，前端样式需要和业务方向区分，不仅按数值上升判断好坏。

## 8. 标签分布与顾问分布

### 8.1 标签分布

| 项目 | 口径 |
|---|---|
| 数据来源 | 电话邀约 Tab 读取 `IP电话邀约问题诊断明细表-202606后`；试驾接待 Tab 读取 `试驾顾问接待问题诊断表` |
| 一级标签数量 | 当前门店、页面日期区间、当前 Tab 下对应一级标签命中的记录数 |
| 一级标签占比 | 一级标签数量 / 当前 Tab 下全部标签命中记录数 |
| 二级标签数量 | 当前一级标签下各二级标签命中的记录数 |
| 多标签处理 | 标签分布按标签长表统计，同一事件多标签可贡献多条标签命中 |

### 8.2 顾问分布

| 项目 | 口径 |
|---|---|
| 数据来源 | 电话邀约 Tab 读取 `IP电话邀约问题诊断明细表-202606后`；试驾接待 Tab 读取 `试驾顾问接待问题诊断表` |
| 顾问问题数 | 电话邀约 Tab 默认按 `顾问编码 + 呼叫编码` 去重统计负向电话数；试驾接待 Tab 按后续试驾表口径确认 |
| 顾问正负向 | 使用明细表字段 `标签正负向`；当前顾问分布仅聚焦负向问题数量，不定义高风险等级 |
| 排序 | 前端可按当前权限范围内顾问问题数降序展示 |
| 筛选影响 | 顾问筛选只影响深度分析明细，不反算上方漏斗和过程指标 |

## 9. 明细清单与证据抽屉

| 页面字段 | 数据来源 | 口径 |
|---|---|---|
| 客户 | 数仓打标明细 | 使用脱敏展示名 |
| 顾问 | 数仓打标明细 | 使用顾问展示名 |
| 事件时间 | 数仓打标明细 | IP 使用通话时间；试驾使用试驾时间 |
| AI 标签 | 数仓打标明细 | 一级标签展示，二级标签可在后续扩展 |
| 问题摘要 | 数仓打标明细 | 数仓加工结果，前端只读 |
| 标签正负向 | 数仓打标明细 | 中文枚举值，前端直接展示；当前负向明细列表展示 `负向` |
| 证据摘录 | 数仓打标明细 | 数仓加工并脱敏后的证据 |
| 标签命中原因 | 数仓打标明细 | 用于解释标签为何命中 |
| 建议话术 | 数仓打标明细 | 数仓加工结果，前端只读 |

## 10. 前端展示格式

| 类型 | 展示格式 |
|---|---|
| 绝对量 | 整数，带单位，例如 `168 条` |
| 百分比 | 保留 1 位小数，例如 `28.0%` |
| pct 环比 | 保留 1 位小数，例如 `-1.6pct` |
| km | 保留 1 位小数，例如 `4.8km` |
| min | 默认整数，例如 `18min` |
| 空值 | 展示 `--` |
| 排名 | 来自官方排名/分位结果表，当前绝对值指标展示为 `小区排名：X/Y` |

## 11. 后续待固化事项

| 事项 | 说明 |
|---|---|
| 源数据集字段名 | 需在数据契约中明确每个指标读取的观远数据集字段 |
| 组织权限字段 | 需在数据契约中明确大区、小区、经销商权限字段 |
| 异常高值阈值 | 试驾里程、试驾时长异常阈值需由数仓或业务确认 |
| 负向标签字典 | IP 与试驾负向标签字典由数仓打标体系维护 |
| 样本不足阈值 | Codex 诊断样本不足阈值需和数据口径保持一致 |
