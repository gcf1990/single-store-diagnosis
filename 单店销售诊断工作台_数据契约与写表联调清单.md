# 单店销售诊断工作台 数据契约与写表联调清单

## 1. 文档目的

本文档用于固化单店销售诊断工作台三张 Codex 输出表的观远资源映射，并作为后续 Codex 定时任务、guancli 写表脚本和 Super APP 前端读取的联调清单。

本文档只覆盖 Codex 输出表单，不定义销售指标、过程指标、IP 打标明细、试驾打标明细的数仓输入口径。输入指标口径仍由 `单店销售诊断工作台_数据口径文档.md` 维护。

## 2. 当前资源状态

观远环境：

```text
https://rdata-pv.rauto.com
```

认证状态核验时间：

```text
2026-06-23
```

三张表单和表单数据集均已创建，并通过 `guancli ds get --brief`、`guancli form list`、`guancli form schema` 完成只读核验。

## 3. 表单与数据集映射

| 业务表 | 观远表单名称 | form 写入 fmId | 表单数据集 dsId | 数据集 URL | 当前状态 |
|---|---|---|---|---|---|
| 门店诊断结果表 | 门店诊断工作台-门店诊断结果表 | `a_11770b-f0d7-4349-a33d-00d76e485ef2` | `baf4b86a458ca4b2a8ce874d` | https://rdata-pv.rauto.com/data-center/data-sets/a2fc4aa0fb89746349c22a76/baf4b86a458ca4b2a8ce874d/details/overview?limit=50 | 已创建 |
| 官方排名分位结果表 | 门店诊断工作台-排名分位结果表 | `a_50f89c-da7c-46cc-aaa1-b09a8de9b5b5` | `xa257b3a018be4418b6100bc` | https://rdata-pv.rauto.com/data-center/data-sets/a2fc4aa0fb89746349c22a76/xa257b3a018be4418b6100bc/details/overview?limit=50 | 已创建 |
| Codex 批次状态表 | 门店诊断工作台-Codex批次状态表 | `a_1c81a3-b2b6-469d-adae-a4950184fa2f` | `vef05c5e538074b2885f3445` | https://rdata-pv.rauto.com/data-center/data-sets/a2fc4aa0fb89746349c22a76/vef05c5e538074b2885f3445/details/overview?limit=50 | 已创建 |

使用边界：

- `fmId` 用于 `guancli form add/query/update/delete/schema` 写入和表单 CRUD 联调。
- `dsId` 用于 `guancli ds get/preview`、Super APP 读取和前端数据集校验。
- 前端不得直接写表单；写入只由 Codex 定时任务或人工联调脚本执行。

## 4. 字段数量核验

| 业务表 | 文档业务字段数 | 表单 schema 字段数 | 数据集字段数 | 差异说明 |
|---|---:|---:|---:|---|
| 门店诊断结果表 | 37 | 41 | 44 | 表单 schema 多 4 个系统字段；数据集额外包含 `row_id`、创建/修改人 uid 等系统字段 |
| 官方排名分位结果表 | 26 | 30 | 33 | 表单 schema 多 4 个系统字段；数据集额外包含 `row_id`、创建/修改人 uid 等系统字段 |
| Codex 批次状态表 | 29 | 33 | 36 | 表单 schema 多 4 个系统字段；数据集额外包含 `row_id`、创建/修改人 uid 等系统字段 |

系统字段处理规则：

- 写入表单时不写系统字段。
- 读取数据集时可以保留系统字段用于排障，但 Super APP 业务逻辑默认不依赖系统字段。
- `row_id` 只用于更新、删除或排查，不作为业务主键。
- 表单 schema 中部分日期字段会显示为 `DATE_TIME`，数据集侧可能显示为 `DATE`；写入脚本统一使用观远可接受的日期或时间字符串，例如 `2026-06-23`、`2026-06-23 08:30:00`。

## 5. 写入主键与 upsert 规则

### 5.1 门店诊断结果表

| 项目 | 规则 |
|---|---|
| 逻辑主键 | `诊断结果ID` |
| 建议生成规则 | `dealer_code + stat_month + diagnosis_batch_id` |
| upsert 查找条件 | `诊断结果ID EQ <诊断结果ID>` |
| 新增规则 | 查无记录时执行 `form add` |
| 更新规则 | 查到记录时取 `row_id` 后执行 `form update` |
| 最新批次规则 | 同门店同月份仅一个 `是否最新成功批次 = true`；新成功批次写入后，需要将旧成功批次更新为 `false` |

### 5.2 官方排名分位结果表

| 项目 | 规则 |
|---|---|
| 逻辑主键 | `排名结果ID` |
| 建议生成规则 | `dealer_code + period_type + stat_month/stat_date + metric_code + rank_scope_type + rank_scope_code + diagnosis_batch_id` |
| upsert 查找条件 | `排名结果ID EQ <排名结果ID>` |
| 新增规则 | 查无记录时执行 `form add` |
| 更新规则 | 查到记录时取 `row_id` 后执行 `form update` |
| 行数规则 | 同一门店、同一周期、同一指标、同一排名范围、同一批次只保留一行 |

### 5.3 Codex 批次状态表

| 项目 | 规则 |
|---|---|
| 逻辑主键 | `批次状态ID` |
| 建议生成规则 | `diagnosis_batch_id + step_code + dealer_code`，批次总览行 `dealer_code` 可为空 |
| upsert 查找条件 | `批次状态ID EQ <批次状态ID>` |
| 新增规则 | 每个任务环节开始时可先写入 `运行中` |
| 更新规则 | 环节结束后更新为 `成功`、`失败` 或 `跳过` |
| 失败规则 | 失败时必须写入 `错误编码`、`错误信息`、`上一成功批次ID` |

## 6. guancli 联调命令清单

以下命令是联调动作模板，正式执行前需要替换示例 ID 和 JSON 内容。

### 6.1 查看表单 schema

```bash
guancli form schema a_11770b-f0d7-4349-a33d-00d76e485ef2
guancli form schema a_50f89c-da7c-46cc-aaa1-b09a8de9b5b5
guancli form schema a_1c81a3-b2b6-469d-adae-a4950184fa2f
```

### 6.2 查询表单记录

```bash
guancli form query a_11770b-f0d7-4349-a33d-00d76e485ef2 --filter "诊断结果ID EQ D202605_STORE001_B202606230800" -f json
guancli form query a_50f89c-da7c-46cc-aaa1-b09a8de9b5b5 --filter "排名结果ID EQ R202605_STORE001_orders_小区_D001_B202606230800" -f json
guancli form query a_1c81a3-b2b6-469d-adae-a4950184fa2f --filter "批次状态ID EQ S_B202606230800_写入表单_STORE001" -f json
```

### 6.3 插入单行样例

```bash
guancli form add <fmId> --data '{"字段名":"字段值"}'
```

建议首轮只写测试批次：

```text
diagnosis_batch_id = TEST_YYYYMMDDHHMM
```

### 6.4 更新单行样例

```bash
guancli form update <fmId> <row_id> --data '{"字段名":"新字段值"}'
```

### 6.5 读取表单数据集

```bash
guancli ds preview baf4b86a458ca4b2a8ce874d --limit 5
guancli ds preview xa257b3a018be4418b6100bc --limit 5
guancli ds preview vef05c5e538074b2885f3445 --limit 5
```

## 7. 写表联调验收清单

| 步骤 | 验收动作 | 通过标准 |
|---|---|---|
| 1. schema 核验 | 查询三张表单 schema | 字段名与 `单店销售诊断工作台_Codex沉淀清单与字段口径.md` 一致，系统字段不参与写入 |
| 2. 测试批次插入 | 每张表插入 1 行测试批次 | `form add` 返回成功，记录可查询 |
| 3. upsert 查询 | 用逻辑主键查询刚写入记录 | 能返回唯一记录，并拿到 `row_id` |
| 4. 单行更新 | 对同一逻辑主键执行更新 | 不新增重复记录，原记录字段被更新 |
| 5. 数据集读取 | 用 dsId 预览三张表单数据集 | 写入记录可从数据集读取 |
| 6. 最新批次标记 | 同门店同月写入新成功批次 | 新批次为 `true`，旧成功批次为 `false` |
| 7. 失败回退 | 写入失败批次状态 | 批次状态表记录失败环节、错误信息和上一成功批次 |
| 8. 前端读取 | Super APP 按 dsId 读取 | 能读取当前门店、当前月份、最新成功批次 |

## 8. 当前未闭环事项

| 事项 | 状态 | 影响 |
|---|---|---|
| 观远表单数据集刷新机制 | 待联调确认 | 写入后前端是否立即可读，需要实际验证 |
| guancli 批量写入性能 | 待测试 | 全量门店写入时需要验证耗时、失败重试和分页 |
| 源数据集 ID | 待补齐 | 定时任务还不能完整拉取销售指标、过程指标和打标明细 |
| 定时任务运行环境 | 待确认 | 需要明确本地 Codex、系统 cron、launchd 或后续生产调度 |
| 失败通知方式 | 待确认 | 需要明确失败后通知到谁、通过什么渠道通知 |

## 9. 下一步建议

1. 用 `TEST_` 批次完成三张表单的单行写入、查询、更新闭环。
2. 确认表单写入后，三个表单数据集多久可以被 `ds preview` 和 Super APP 读取。
3. 补齐源数据集契约，包括销售指标、过程指标、IP 打标明细、试驾打标明细的 dsId 和字段名。
4. 再进入 Codex 定时任务脚本设计，明确任务触发时间、输入范围、重试、日志和补跑方式。
