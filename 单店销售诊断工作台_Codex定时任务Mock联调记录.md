# 单店销售诊断工作台 Codex 定时任务 Mock 联调记录

## 1. 本次联调目标

验证在真实 IP 打标表、试驾打标表尚未上线前，是否可以用同字段结构的 mock 数据跑通以下链路：

```text
mock 输入数据
-> Codex 诊断脚本生成三张输出表
-> guancli 写入观远表单
-> guancli 反查表单记录
-> 等待或触发表单数据集刷新后供前端读取
```

## 2. 本次联调批次

| 项目 | 内容 |
|---|---|
| 诊断批次ID | `MOCK_202603_20260623231110` |
| 统计月份 | `2026-03` |
| 触发方式 | `Mock测试` |
| 脚本版本 | `mock_diagnosis_v0.1` |
| 诊断模型版本 | `mock_rule_v0.1` |
| 诊断 Prompt 版本 | `mock_prompt_v0.1` |
| 本地输出目录 | `outputs/mock_codex_run/MOCK_202603_20260623231110` |

## 3. 生成结果

| 输出表 | 本地生成行数 | 观远写入结果 |
|---|---:|---|
| 门店诊断结果表 | 3 | 已通过 `guancli form add` 插入，并通过本地精确匹配 upsert 更新 |
| 官方排名分位结果表 | 21 | 已通过 `guancli form add` 插入，并通过本地精确匹配 upsert 更新 |
| Codex 批次状态表 | 4 | 已通过 `guancli form add` 插入，并通过本地精确匹配 upsert 更新 |

本次 mock 数据覆盖 3 家门店：

| 经销商代码 | 统计月份 | 生成主问题 |
|---|---|---|
| `310101` | `2026-03` | 线索供给不足 |
| `310102` | `2026-03` | 到店转化不足 |
| `310103` | `2026-03` | 到店转化不足 |

## 4. 表单反查结果

已通过 `guancli form query` 反查到本批次记录。

门店诊断结果表反查到本批次 3 条记录：

| 诊断结果ID | 经销商代码 | 主问题名称 |
|---|---|---|
| `D_202603_310101_MOCK_202603_20260623231110` | `310101` | 线索供给不足 |
| `D_202603_310102_MOCK_202603_20260623231110` | `310102` | 到店转化不足 |
| `D_202603_310103_MOCK_202603_20260623231110` | `310103` | 到店转化不足 |

官方排名分位结果表反查到本批次排名记录，示例：

| 经销商代码 | 指标编码 | 官方排名 | 官方分位 |
|---|---|---:|---:|
| `310101` | `lead_to_order_rate` | 1 | 100 |
| `310102` | `lead_to_order_rate` | 2 | 66.67 |
| `310103` | `lead_to_order_rate` | 3 | 33.33 |

Codex 批次状态表反查到 4 个环节：

| 任务环节编码 | 环节状态 |
|---|---|
| 拉取数据 | 成功 |
| 生成诊断 | 成功 |
| 写入表单 | 成功 |
| 刷新表单数据集 | 成功 |

## 5. 纯 mock 批次历史观察

| 事项 | 当前结果 | 影响 |
|---|---|---|
| 表单数据集刷新 | 写入后立即 `ds preview` 仍只能读到旧数据，未读到 `MOCK_202603_20260623231110` | 已确认表单数据集由观远每日定时刷新，不作为 Codex 写表任务即时验收项 |
| guancli 触发刷新 | 当前 `guancli ds` 只有 `get/search/preview/execute-sql/tree`，未发现刷新命令 | 不影响 Codex 写表链路；前端读取等待观远定时刷新 |

## 6. upsert 验证结果

已将写表脚本升级为本地精确匹配 upsert：

```text
读取表单完整记录
-> 按业务主键在本地精确匹配
-> 命中则使用 rowId 执行 form update
-> 未命中则执行 form add
```

对同一批次 `MOCK_202603_20260623231110` 复跑写入，结果如下：

| 输出表 | 新增行数 | 更新行数 |
|---|---:|---:|
| 门店诊断结果表 | 0 | 3 |
| 官方排名分位结果表 | 0 | 21 |
| Codex 批次状态表 | 0 | 4 |

## 7. 纯 mock 批次结论

已跑通：

- mock 输入生成。
- 三张 Codex 输出表生成。
- 通过 `guancli form add` 写入三张观远表单。
- 通过本地精确匹配 upsert 对同一批次执行 `guancli form update`，未重复新增。
- 通过 `guancli form query` 反查写入记录。

边界说明：

- 表单数据集刷新由观远每日定时任务负责，不作为 Codex 写表任务即时验收项。
- 纯 mock 批次只用于验证写表链路；正式联调以 HYBRID 真实指标批次为准。

## 8. 真实指标 + mock 打标链路调整

用户已明确：测试链路不能全部使用 mock 数据。已经明确的数据源必须用 guancli 拉真实数据；只有 IP 打标、试驾打标等尚未上线或待定数据源使用 mock 数据。

因此联调链路调整为：

```text
真实销售漏斗指标
+ 真实 DCC 话务过程指标
+ 真实试驾过程指标
+ mock IP 打标明细
+ mock 试驾打标明细
-> Codex 诊断脚本生成三张输出表
-> guancli 写入观远表单
-> 表单数据集刷新后供前端读取
```

已新增脚本：

| 脚本 | 用途 |
|---|---|
| `scripts/run_hybrid_codex_diagnosis.py` | 生成真实指标 + mock 打标的 HYBRID 批次输出 |
| `scripts/guancli_form_load.py` | 继续负责三张输出表 upsert 写入 |

## 9. 真实源预检结果

预检时间：2026-06-23。

| 数据源 | dsId | 预检结果 | 说明 |
|---|---|---|---|
| 销售漏斗指标源 | `gae00de628b274fdf837719d` | 可取数 | `2026-03` 至 `2026-06` 有真实数据，`2026-03` 行数约 508806，门店数约 2251 |
| DCC 话务指标源 | `fa1bfbd7736f34d1d8633883` | 可通过 `ds preview` 取真实明细 | `execute-sql` 对 DATAFLOW 表名报错，脚本采用 `ds preview` 按月份和经销商筛选后本地聚合 |
| 试驾过程指标源 | `c6428f1c9ca204859b553421` | 可取数 | `2026-03` 至 `2026-06` 有真实数据，`2026-03` 行数约 30528，门店数约 855 |

已验证的真实样例门店：

| 经销商代码 | 经销商简称 | 销售漏斗 | DCC 话务明细 | 成功试驾过程指标 |
|---|---|---|---|---|
| `MQ2164` | 上海金益诚 | 可取数 | 可取数 | 可取数 |
| `SQ3703` | 河南瑞爵 | 可取数 | 可取数 | 可取数 |
| `MQ532C` | 济南名智 | 可取数 | 可取数 | 可取数 |

## 10. 历史待确认口径

以下为 HYBRID 批次正式生成和写表前的保护要求；本轮口径已在第 11 节确认：

| 待确认项 | 当前脚本要求的显式参数 | 需要确认的业务含义 |
|---|---|---|
| 统计月和测试门店 | `--stat-month`、`--dealer-codes` | 本轮真实经销商测试使用哪个自然月、哪些经销商 |
| 销售漏斗聚合范围 | `--confirm-sales-scope brand_all_series_all_channel` | 是否按指定品牌、全部车系、全部渠道聚合 |
| DCC 去重规则 | `--confirm-dcc-dedup lead_any_row` | 同一线索多行时，是否任一行满足条件即计入分子 |
| 试驾月份归属 | `--confirm-drive-month-field trial_recv_date` | 是否按 `试驾接待日期` 归属自然月 |
| mock 打标范围 | `--confirm-mock-tags selected_dealers_only` | IP/试驾打标 mock 是否只为本轮选定经销商生成 |

脚本保护规则：

- 上述确认参数缺失时，`scripts/run_hybrid_codex_diagnosis.py` 不生成结果。
- DCC 明细读取达到 `--dcc-limit` 上限时，默认中止，避免截断后生成错误指标。
- 表单写入仍需单独执行 `scripts/guancli_form_load.py --dry-run` 和 `--yes`，不会在生成脚本中自动写表。

## 11. HYBRID 真实指标联调批次

用户已确认本轮真实指标联调口径：

| 项目 | 确认口径 |
|---|---|
| 品牌 | `MG` |
| 测试经销商 | `MQ2051`，广州美轮庆爵 |
| 统计月份 | `2026-03` 至 `2026-06` |
| 销售漏斗范围 | 指定品牌、全部车系、全部渠道 |
| DCC 去重规则 | 同一线索多行时，任一行满足条件即计入分子 |
| 试驾月份归属 | 按 `试驾接待日期` 归属 |
| mock 打标范围 | 只为本轮选定经销商生成 |

生成批次：

| 项目 | 内容 |
|---|---|
| 诊断批次ID | `HYBRID_MG_202603_202606_20260623233621` |
| 本地输出目录 | `outputs/hybrid_codex_run/HYBRID_MG_202603_202606_20260623233621` |
| 输入模式 | 真实销售漏斗 + 真实 DCC 话务 + 真实试驾过程 + mock IP/试驾打标明细 |
| 生成时间 | `2026-06-23 23:36:21` |

生成结果：

| 输出表 | 生成行数 | 写表结果 |
|---|---:|---|
| 门店诊断结果表 | 4 | 新增 4 行 |
| 官方排名分位结果表 | 32 | 新增 32 行 |
| Codex批次状态表 | 4 | 新增 4 行 |

说明：

- 门店诊断结果表覆盖 `MQ2051` 在 `2026-03`、`2026-04`、`2026-05`、`2026-06` 四个月的一店一月诊断结果。
- 官方排名分位结果表覆盖每个月 8 个销售漏斗指标：下发线索、首触到店、首触试驾、首触订单、线索到店率、到店试驾率、试驾订单率、线索订单率。
- 排名范围为小区 `SQRW44` / `张宗雷`，按 MG 品牌、自然月、小区内一网门店、剔除自然月下发线索量为 0 的门店计算。
- 本轮 IP/试驾打标明细仍为 mock，仅用于补齐未上线打标数据集的流程测试。

表单可见性验收：

| 层级 | 验收结果 | 说明 |
|---|---|---|
| 表单层 | 通过 | `validate_codex_batch_visibility.py` 校验三张表单均可读到本批次 |
| 表单数据集层 | 不作为本次验收 | 表单数据集由观远每日定时刷新，Codex 任务只负责更新表单 |

验收脚本：

```bash
python3 scripts/validate_codex_batch_visibility.py --batch-id HYBRID_MG_202603_202606_20260623233621 --limit 5000 --json-output
```

验收结果摘要：

| 表 | 表单层匹配行数 |
|---|---:|
| 门店诊断结果表 | 4 |
| 官方排名分位结果表 | 32 |
| Codex批次状态表 | 4 |

当前结论：

- `guancli` 拉真实数据、生成 Codex 输出、写入观远表单、表单反查已经跑通。
- 表单数据集由观远每日定时刷新，不作为 Codex 写表任务即时验收项。

## 12. 当前态 upsert 写表策略确认

用户已确认前端不需要筛选批次，同一家经销商只需要保留最新一次运行结果。

同时识别到“先删除旧数据、再写新批次”存在风险：如果新批次取数或写入失败，表单可能短时间为空或只剩部分数据。因此正式 Codex 定时任务不采用先删后写。

最终写表策略：

```text
新批次先完成取数和本地输出生成
-> 按当前态主键查询表单现有记录
-> 命中则 form update
-> 未命中则 form add
-> 不在写入前删除旧数据
```

当前态主键：

| 表 | 当前态主键 |
|---|---|
| 门店诊断结果表 | `经销商代码 + 统计月份` |
| 官方排名分位结果表 | `经销商代码 + 统计月份 + 指标编码 + 排名范围类型 + 排名范围编码` |
| Codex批次状态表 | `经销商代码 + 任务环节编码` |

最终验证批次：

| 项目 | 内容 |
|---|---|
| 诊断批次ID | `HYBRID_MG_202603_202606_20260623235305` |
| 写表方式 | `scripts/guancli_form_load.py --current-state-upsert --yes` |
| 删除旧数据 | 0 行 |
| 新增数据 | 0 行 |
| 更新数据 | 门店诊断结果表 4 行、官方排名分位结果表 32 行、Codex批次状态表 4 行 |

表单反查结果：

| 表 | MQ2051 当前行数 | 当前批次 |
|---|---:|---|
| 门店诊断结果表 | 4 | `HYBRID_MG_202603_202606_20260623235305` |
| 官方排名分位结果表 | 32 | `HYBRID_MG_202603_202606_20260623235305` |
| Codex批次状态表 | 4 | `HYBRID_MG_202603_202606_20260623235305` |

结论：

- 正式定时任务先完成取数和本地生成，再执行当前态 upsert。
- 当前态 upsert 不会先删除旧表单数据，可避免新批次失败导致表单为空。
- `诊断批次ID` 保留用于追溯和排障，不作为前端展示筛选条件。

## 13. 一键定时任务入口验收

验收时间：2026-06-24 00:10。

本轮使用一键入口串联真实指标取数、mock 打标补齐、Codex 输出生成、当前态 upsert 写表和表单层反查：

```bash
python3 scripts/run_codex_diagnosis_job.py --start-month 2026-03 --end-month 2026-06 --dealer-codes MQ2051 --brand-name MG --yes
```

验收范围：

| 项目 | 内容 |
|---|---|
| 品牌 | MG |
| 经销商代码 | `MQ2051` |
| 统计月份 | `2026-03` 至 `2026-06` |
| 输入模式 | 真实销售漏斗 + 真实 DCC 话务 + 真实试驾过程 + mock IP/试驾打标明细 |
| 诊断批次ID | `HYBRID_MG_202603_202606_20260624001006` |
| 输出目录 | `outputs/hybrid_codex_run/HYBRID_MG_202603_202606_20260624001006` |

输出与写表结果：

| 表 | 输出行数 | 写表动作 | 删除旧数据 |
|---|---:|---|---:|
| 门店诊断结果表 | 4 | 更新 4 行，新增 0 行 | 0 |
| 官方排名分位结果表 | 32 | 更新 32 行，新增 0 行 | 0 |
| Codex批次状态表 | 4 | 更新 4 行，新增 0 行 | 0 |

表单层反查结果：

| 表 | 表单层匹配行数 | 验收结果 |
|---|---:|---|
| 门店诊断结果表 | 4 | 通过 |
| 官方排名分位结果表 | 32 | 通过 |
| Codex批次状态表 | 4 | 通过 |

说明：

- 本轮验证的是表单层可见性，表单数据集刷新仍由观远每日定时任务负责，不作为 Codex 写表即时验收项。
- 一键入口在 `--dry-run-write` 模式下只生成和模拟写表，不再校验未写入批次的表单可见性。
- 写表策略仍为当前态 upsert，不在写入前删除旧数据。

## 14. 主问题兜底规则修复

修复时间：2026-06-24。

问题：

- MQ2051 在 2026-06 的真实数据中，下发线索为 2761，小区排名第 1 / 16，官方分位 100。
- 旧脚本在没有命中其它主问题时，默认兜底输出 `线索供给不足`，导致诊断结果显示“下发线索不足，后续转化承压”。
- 该结论与数据证据不一致。

修复规则：

```text
如果没有命中任何分位阈值或负向明细阈值
-> 不得兜底输出“线索供给不足”
-> 输出“未发现显著异常”
```

修复后验证批次：

| 项目 | 内容 |
|---|---|
| 诊断批次ID | `HYBRID_MG_202606_RULEFIX_20260624` |
| 统计月份 | `2026-06` |
| 经销商代码 | `MQ2051` |
| 主问题编码 | `no_significant_issue` |
| 主问题名称 | `未发现显著异常` |
| 结果断点 | `未发现显著异常` |
| 过程原因 | `未命中分位阈值或负向阈值` |

关键证据：

| 指标 | 官方排名 | 排名总数 | 官方分位 |
|---|---:|---:|---:|
| 下发线索 | 1 | 16 | 100 |
| 首触到店 | 3 | 16 | 87.5 |
| 首触试驾 | 1 | 16 | 100 |
| 首触订单 | 1 | 16 | 100 |
| 线索到店率 | 9 | 16 | 50 |
| 到店试驾率 | 6 | 16 | 68.75 |
| 试驾订单率 | 4 | 16 | 81.25 |
| 线索订单率 | 3 | 16 | 87.5 |

写表结果：

| 表 | 写表动作 |
|---|---|
| 门店诊断结果表 | 当前态更新 1 行 |
| 官方排名分位结果表 | 当前态更新 8 行 |
| Codex批次状态表 | 当前态更新 4 行 |

表单反查：

- `MQ2051`、`2026-06` 的门店诊断结果已更新为 `未发现显著异常`。
- 6 月 8 个官方排名/分位结果已更新到批次 `HYBRID_MG_202606_RULEFIX_20260624`。

## 15. 定时任务扩展到 MG 东南大区

调整时间：2026-06-24。

调整内容：

- 一键任务新增 `--region-name` 参数，支持按大区简称模糊匹配自动识别门店范围。
- `--dealer-codes` 和 `--region-name` 二选一。
- DCC 话务数据改为按经销商分片读取，避免触发观远 preview 单次 60,000 行上限。
- `automation-2` 已从单店 `MQ2051` 调整为 MG 品牌东南大区。

定时任务命令：

```bash
END_DATE=$(date -v-1d +%Y-%m-%d)
END_MONTH=${END_DATE%-*}
python3 scripts/run_codex_diagnosis_job.py --start-month 2026-03 --end-month "$END_MONTH" --end-date "$END_DATE" --region-name 东南 --brand-name MG --dcc-limit 50000 --dcc-chunk-size 10 --validate-limit 500 --yes
```

当前正式定时任务统计范围为 `2026-03-01` 至任务执行日前一天；包含昨天，不包含今天数据。

东南大区单月 dry-run 验证：

| 项目 | 内容 |
|---|---|
| 验证月份 | `2026-06` |
| 诊断批次ID | `HYBRID_MG_202606_20260624170104` |
| 自动识别门店数 | 84 |
| 门店诊断结果表 | 84 行 |
| 官方排名分位结果表 | 672 行 |
| Codex批次状态表 | 4 行 |
| 写表方式 | dry-run 当前态 upsert |
| 删除旧数据 | 0 行 |

dry-run 写表计划摘要：

| 表 | 新增 | 更新 |
|---|---:|---:|
| 门店诊断结果表 | 83 | 1 |
| 官方排名分位结果表 | 664 | 8 |
| Codex批次状态表 | 4 | 0 |

说明：

- 本次只做东南大区 dry-run，未实际覆盖表单。
- 明天 08:30 自动任务会按东南大区正式写表。

## 16. 182 DCC 话务指标最新覆盖口径

更新时间：2026-06-25。

本节记录后续 Super APP 和 Codex 定时任务应采用的最新 182 口径；第 10 至 15 节保留为历史批次记录，不代表最新口径。

| 项目 | 最新口径 |
|---|---|
| 数据集 | `【双品牌】DCC话务指标_182`，dsId `fa1bfbd7736f34d1d8633883` |
| 日期字段 | `下发CRM时间`，页面开始日期补 `00:00:00`，结束日期补 `23:59:59` |
| 品牌 | `品牌名称 = MG` |
| 门店 | 当前页面所选门店，优先用 `经销商代码` |
| 渠道大类 | `线索渠道大类名称 IN 厂方新媒体, 媒介投放, 官网及电商, 经销商新媒体, 网销平台, 基地, 官方新媒体, MCN` |
| 开业状态 | `开业状态 = 1` |
| 数据类型 | `data_type_ch != 来电咨询` |
| 考核范围 | `线索免考核 = 待考核` |
| 跟进范围 | `需跟进 = 需跟进` |

指标字段：

| 页面指标 | 182 对齐字段 / 公式 |
|---|---|
| 线索接通率 | 对齐看板 `72小时线索外呼接通率`：`count(distinct if(线索下发72小时外呼接通次数 > 0, 线索编码, null)) / count(distinct 线索编码)` |
| 30分钟外呼率 | 对齐看板 `(10-18)30分钟外呼率`：`工作时段30分钟跟进（10-18） = 是` / `是否工作时段线索（10-18） = 工作时段` |
| 2天3呼率 | 对齐看板 `2天3呼达标率`：`是否完成72小时三呼 = 是` / 当前口径过滤后线索行数 |

校验样例：

| 门店 | 日期区间 | 线索接通率 | 30分钟外呼率 | 2天3呼率 |
|---|---|---:|---:|---:|
| 宁波宁爵 | `2026-06-01` 至 `2026-06-24` | 57.8% | 85.7% | 87.2% |
