# Agent 工作说明

## 项目定位

本仓库是单店销售诊断工作台 demo，用于基于观远 Super APP 形态验证单店销售诊断页面、PRD 和后续数据链路。

当前页面是静态 demo，入口为：

```text
index.html
```

后续 Agent 进入本仓库时，必须先阅读：

1. `README.md`
2. `单店销售诊断工作台_PRD.md`
3. `单店销售诊断工作台_前端设计文档.md`
4. `index.html`

## 当前文件

| 文件 | 用途 |
|---|---|
| `index.html` | 单店销售诊断工作台前端静态 demo。 |
| `单店销售诊断工作台_PRD.md` | 当前页面范围对应的 PRD。 |
| `单店销售诊断工作台_前端设计文档.md` | 后续观远 Super APP 搭建用前端设计文档。 |
| `平台开发计划0621.xlsx` | 平台开发计划与任务节奏记录。 |
| `README.md` | GitHub 首页说明。 |
| `.gitignore` | 排除本地临时文件。 |

不要提交 Excel 临时锁文件，例如：

```text
.~平台开发计划0621.xlsx
```

## 范围边界

本期范围严格以 `index.html` 当前页面展示为准。

允许处理：

- 单店页面模块说明和文档完善。
- 当前 demo 页面样式、交互和静态展示优化。
- PRD、数据口径文档、数据契约、Tech Design、联调清单等文档补齐。
- Codex 定时任务、guancli 写表链路的方案设计和验证脚本。

不要擅自扩展：

- 全国/区域总览页面。
- 现场问答。
- 实时重新诊断。
- 整改任务流写回。
- 生产级调度平台线上化。
- 页面外的新业务功能。

如需扩展范围，必须先和用户确认。

## 前端设计文档边界

`单店销售诊断工作台_前端设计文档.md` 是后续搭建观远 Super APP 前端的基准文档。维护该文档时必须同时守住两层口径：

| 层级 | 要求 |
|---|---|
| 当前 demo 还原 | 严格对应 `index.html` 的页面结构、视觉样式、展示内容和已实现交互。 |
| Super APP 落地 | 在不改变 demo 主线的前提下，补齐数据接入、筛选刷新、异常状态、批次追溯、行权限、联调验收和工程发布要求。 |

后续修改设计文档时，不要把当前 demo 未实现的交互写成“已实现”；可以明确标注为“Super APP 落地要求”或“后续数据接入/联调验收项”。

## 既有 Super APP 工程参考

可参考既有六量七率 Super APP demo 的工程能力：

```text
../../六量七率工作台-观远APP/六量七率工作台/
```

该目录只作为工程能力参考，不作为本页面视觉或业务模块来源。

可复用能力：

- React + TypeScript + Vite 单入口 SPA。
- `public/settings.json` 运行时配置。
- `src/bi-services/` 统一封装观远 card、selector、form、LLM 等原生接口。
- `src/services/` 统一封装业务数据聚合、筛选转换、异常降级和 AI 解释。
- card/selector/form 动态读取观远数据。
- 构建、lint、测试、设计 lint 和发布包流程。

禁止照搬：

- 旧六量七率工作台的全国/区域视图。
- 旧六量七率工作台的视觉模块和页面结构。
- 与当前单店 demo 无关的业务指标和下钻层级。

## 数据与计算边界

本项目已确认的核心原则：

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

具体分层：

| 类型 | 处理方式 |
|---|---|
| 原子事实数据 | 落入观远数据集或表单，并继承观远行权限。 |
| 门店内累计值 | Super APP 前端可基于当前权限范围计算。 |
| 转化率、负向占比、环比 | Super APP 前端可基于当前权限范围和同口径数据计算。 |
| 当前权限范围内轻量筛选/排序 | Super APP 前端可计算。 |
| 官方排名、官方分位、跨组织对标 | 数据层预计算，不由前端临时代表官方口径。 |
| 主问题、结果断点、过程原因、优先动作 | Codex T+1 诊断链路生成并沉淀。 |
| AI 解释文案 | 正式版建议沉淀；前端可保留基于锁定事实的解释增强能力。 |

## 诊断生成链路

本期 MVP 采用以下链路：

```text
观远相关数据集完成刷新
-> Codex 每日定时任务触发
-> guancli 拉取门店销售指标、过程指标、IP打标明细、试驾打标明细
-> Codex 本地诊断脚本生成门店诊断结果
-> guancli 写入观远表单
-> 观远表单数据集刷新
-> Super APP 前端读取展示
```

该链路是 MVP / POC 方案。生产级线上化调度不在本期范围内，但后续需要迁移到不依赖个人本地 Codex 的稳定调度方案。

## 文档顺序

后续项目推进建议按以下顺序补齐：

1. PRD：已创建 `单店销售诊断工作台_PRD.md`。
2. 前端设计文档：已创建 `单店销售诊断工作台_前端设计文档.md`。
3. 数据口径文档：定义每个指标的分子、分母、过滤条件、去重规则和异常处理。
4. 数据契约：定义观远数据集/表单字段、粒度、主键、刷新时间、权限字段和前端读取方式。
5. Tech Design：定义 Codex 定时任务、guancli、表单写入、Super APP 读取和异常处理。
6. 联调清单：覆盖前端、观远数据集、表单数据集、行权限和异常状态。
7. 测试验收：验证页面范围、数据来源、计算边界、诊断链路和异常状态。

## 开发与修改规则

- 修改文件前先说明要改什么。
- 保持中文文档和中文说明。
- 不要把具体指标口径散落在 PRD 中；指标分子分母放到数据口径文档。
- 不要把数据集字段契约散落在 PRD 中；字段、类型、主键、刷新时间放到数据契约。
- 不要把官方诊断事实写成前端临时 LLM 生成。
- 设计文档涉及 Super APP 工程能力时，可以参考旧六量七率 Super APP，但必须声明只复用工程能力，不复用业务模块。
- 修改 `index.html` 后，需要用浏览器打开页面检查主要模块是否仍可展示。
- 修改文档后，需要检查是否存在 `TODO`、`TBD`、前后矛盾或范围扩张。

## Git 规则

当前远端仓库：

```text
git@github.com:gcf1990/single-store-diagnosis.git
```

提交要求：

- 每次提交只包含本次任务相关文件。
- 不提交本地临时文件、系统文件和无关产物。
- 提交信息使用简短英文，例如 `Add project agents guide`。
