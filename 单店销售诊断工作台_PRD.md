# 单店销售诊断工作台 PRD

## 1. 项目定位

单店销售诊断工作台是基于观远 Super APP 承载的单页工作台，用于帮助门店经理和区域/小区经理快速识别单店销售过程短板，并下钻到邀约、试驾接待、顾问和客户证据明细。

本文档是 Agent 开发上下文型 PRD，主要服务后续 Codex / AI Agent 开发、联调、测试和方案复核。文档重点说明页面范围、业务主线、数据对象、计算边界、诊断生成链路、异常状态和验收标准。

本 PRD 不展开每个指标的分子、分母、过滤条件、去重规则和 SQL 口径；这些内容由后续独立的《销售诊断工作台_数据口径文档》维护。

## 2. 本期范围

本期范围严格以前端 demo 当前页面为准，页面路径为：

```text
销售诊断工作台-new/单店诊断工作台demo/index-no-ai.html
```

`index.html` 为含门店 AI 诊断总览的历史探索稿，不作为首期验收基线。

本期包含：

- 顶部对象区：大区、小区、经销商、日期。
- 销售漏斗：下发线索、线索到店率、到店、到店试驾率、试驾、试驾订单率、订单、线索订单率。
- 邀约：线索质量、勤奋度、邀约技巧三组指标。
- 试驾：试驾强度、体验时长、接待技巧三组指标。
- 趋势浮窗：邀约和试驾日趋势辅助观察，支持图例显隐和悬浮查看单日指标值。
- 深度分析：电话邀约、试驾接待两个 Tab。
- 标签分布、顾问分布、客户明细清单和客户证据抽屉。

本期不包含：

- 不展示门店 AI 诊断总览。
- 不扩展全国/区域总览页面。
- 不做现场问答模块。
- 不做实时重新诊断。
- 不做整改任务流写回。
- 不做生产级调度平台线上化。
- 不在 PRD 内展开指标分子分母和 SQL 口径。

## 3. 用户角色与核心场景

### 3.1 用户角色

| 角色 | 目标 |
|---|---|
| 门店经理 | 看清本店销售过程短板，定位顾问和客户案例，组织门店复盘和话术改进。 |
| 区域/小区经理 | 下钻重点门店，判断门店问题是否需要辅导，监督门店经理复盘动作。 |
| 后续 Codex / AI Agent | 理解页面模块、数据依赖、AI 结果链路、计算边界和验收标准。 |

### 3.2 核心业务场景

```text
每日 T+1 数据刷新后，门店经理或区域经理进入单店页面，
先看销售漏斗判断本店当前销售结果，
然后看邀约和试驾指标定位过程短板，
最后下钻到标签、顾问和客户明细，
用证据摘录和建议话术组织门店复盘。
```

### 3.3 页面主线

```text
对象定位
-> 销售漏斗查看销售结果
-> 邀约/试驾定位过程问题
-> 深度分析查看标签分布和顾问分布
-> 客户明细抽屉查看证据和建议话术
-> 门店经理组织复盘动作
```

## 4. 页面模块与功能需求

| 模块 | 功能目标 | 展示内容 | 数据来源 | 交互要求 |
|---|---|---|---|---|
| 顶部对象区 | 明确当前诊断对象 | 大区、小区、经销商、日期 | Super APP 传参或页面筛选参数 | 切换对象后刷新页面数据 |
| 销售漏斗 | 判断销售结果和转化路径 | 下发线索、到店、试驾、订单及 4 个转化率 | 门店日粒度销售指标表 | 不联动下方模块，仅作为结果展示区 |
| 邀约 | 定位客户为什么带不到店 | 线索质量、勤奋度、邀约技巧三组指标 | 门店过程指标数据、IP 打标明细 | 点击模块或指标联动到电话邀约深度分析 |
| 试驾 | 定位到店后的试驾承接问题 | 试驾强度、体验时长、接待技巧三组指标 | 门店过程指标数据、试驾打标明细 | 点击模块或指标联动到试驾接待深度分析 |
| 趋势浮窗 | 辅助判断问题是否持续发生 | 邀约/试驾日趋势，图例最新值，悬浮单日值 | 日粒度指标数据；邀约负向占比读取 IP 打标明细，试驾负向占比在试驾打标明细未确认前不进入真实趋势图 | 点击“更多”打开；图例可隐藏/恢复指标线；鼠标悬浮展示日期和指标值；作为辅助诊断，不作为主操作入口 |
| 电话邀约 Tab | 下钻邀约问题 | 标签分布、顾问分布、客户明细、证据抽屉 | IP 打标明细 | Tab 切换后展示邀约问题证据 |
| 试驾接待 Tab | 下钻试驾接待问题 | 标签分布、顾问分布、客户明细、证据抽屉 | 试驾打标明细 | Tab 切换后展示接待问题证据 |
| 标签分布 | 看问题类型集中在哪 | 一级/二级标签、数量、占比 | IP 打标明细或试驾打标明细 | 支持按标签筛选明细 |
| 顾问分布 | 找到负向问题集中的顾问 | 顾问名称、负向问题数量、主要标签、标签正负向 | IP 打标明细或试驾打标明细 | 支持按顾问筛选明细 |
| 客户明细清单 | 查到可复盘案例 | 客户、顾问、事件时间、一级标签、二级标签、问题摘要、标签正负向 | IP 打标明细或试驾打标明细 | 点击明细打开抽屉 |
| 客户证据抽屉 | 给复盘提供证据和话术 | 客户、顾问、事件时间、标签、摘要、证据摘录、建议话术 | IP 打标明细或试驾打标明细 | 本期动作按钮仅展示，不定义真实任务写回 |

## 5. 数据对象与计算边界

### 5.1 数据对象

| 数据对象 | 粒度 | 用途 |
|---|---:|---|
| 门店日粒度销售指标表 | 门店 + 日期 | 支撑销售漏斗、门店内累计值、转化率和环比计算。 |
| 门店过程指标数据 | 门店 + 日期区间 | 支撑邀约、试驾指标展示。数据可由门店过程明细汇总得到。 |
| IP 打标明细 | IP 电话事件级 | 支撑电话邀约 Tab、负向邀约表现、标签分布、顾问分布、客户证据。 |
| 试驾打标明细 | 试驾事件级 | 支撑试驾接待 Tab、负向接待表现、标签分布、顾问分布、客户证据。 |
| 门店诊断结果表 | 门店 + 日期区间 + 诊断批次 | 后续 AI 诊断阶段支撑主问题、结果断点、过程原因、优先动作；首期页面不展示。 |
| 官方排名/分位结果 | 门店 + 日期区间 + 排名范围 | 支撑官方排名、官方分位、跨组织对标。 |

### 5.2 计算边界

| 类型 | 计算/生成位置 | 说明 |
|---|---|---|
| 原子事实数据 | 观远数据集/表单沉淀 | 受观远行权限控制。 |
| 门店内累计值 | Super APP 前端计算 | 例如当前门店当前日期区间的线索、到店、试驾、订单累计。 |
| 简单公式指标 | Super APP 前端计算 | 例如转化率、负向占比等当前权限范围内指标。 |
| 环比 | Super APP 前端计算 | 前端基于当前日期区间与上月同期同口径数据计算。 |
| 当前权限范围内轻量筛选/排序 | Super APP 前端计算 | 例如已加载明细内的标签筛选、顾问排序、正负向筛选。 |
| 官方排名、官方分位、跨组织对标 | 数据层预计算 | 不能由前端基于当前可见数据临时代表官方口径。 |
| 主问题、结果断点、过程原因、优先动作 | 后续 AI 诊断阶段由 Codex T+1 诊断任务生成并沉淀 | 首期页面不展示，前端不自由判断。 |
| AI 解释文案 | 后续预留 | 首期页面不展示。 |

核心原则：

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

## 6. AI 打标明细与页面映射

### 6.1 IP 打标明细

IP 打标明细支撑电话邀约 Tab，字段能力需覆盖：

- 诊断批次ID、日期区间。
- 大区、小区、经销商代码、经销商名称。
- 客户标识、顾问标识。
- 通话事件标识、通话时间。
- 通话事实字段，如是否接通、通话时长。
- 一级问题标签、二级问题标签。
- 标签正负向。
- 问题摘要、证据摘录、建议话术。
- 模型版本、Prompt 版本、打标状态。

### 6.2 试驾打标明细

试驾打标明细支撑试驾接待 Tab，字段能力需覆盖：

- 诊断批次ID、日期区间。
- 大区、小区、经销商代码、经销商名称。
- 客户标识、顾问标识。
- 试驾事件标识、试驾时间。
- 试驾事实字段，如试驾里程、试驾时长。
- 一级问题标签、二级问题标签。
- 标签正负向。
- 问题摘要、证据摘录、建议话术。
- 模型版本、Prompt 版本、打标状态。

### 6.3 PRD 与数据口径文档边界

本 PRD 只定义页面需要哪些数据对象、字段能力和前后端计算边界。以下内容不在本 PRD 展开：

- 每个指标的分子分母。
- 过滤条件。
- 多标签去重规则。
- 样本排除规则。
- SQL 或 ETL 实现。
- 标签判定规则和 Prompt 细节。

## 7. 门店诊断结果生成链路

本节为后续 AI 诊断阶段预留。首期 `index-no-ai.html` 不展示门店 AI 诊断总览，但仍可保留后续通过 Codex 每日任务沉淀官方诊断结果的产品方向；该结果不由 Super APP 前端实时生成。

```text
观远相关数据集完成刷新
-> Codex 每日定时任务触发
-> guancli 拉取门店销售指标、过程指标、IP打标明细、试驾打标明细
-> Codex 本地诊断脚本生成门店诊断结果
-> guancli 写入观远表单
-> 观远表单数据集刷新
-> Super APP 前端读取展示
```

### 7.1 调度依赖

| 环节 | 需要明确的内容 | 本期要求 |
|---|---|---|
| 观远源数据集刷新 | 每个数据集每日完成时间、失败通知、是否 T+1 完整 | 必须明确 |
| Codex 定时任务 | 每日触发时间、输入数据范围、重试策略、运行日志 | 必须明确 |
| 诊断脚本 | 诊断批次ID、门店/日期区间去重、覆盖策略、失败门店处理 | 必须明确 |
| 观远表单写入 | 表单字段、主键、插入/更新策略、写入失败处理 | 必须明确 |
| 表单数据集刷新 | 写入后多久可被 Super APP 读取、是否需手动刷新 | 必须明确 |
| Super APP 展示 | 读取最新成功批次，失败时展示上一次成功结果或空态 | 必须明确 |
| 线上化调度 | 替代本地 Codex 的生产调度平台 | 记录为后续，不在本期 |

### 7.2 MVP 链路验证

进入正式开发前，必须验证 Codex 定时任务能稳定完成以下动作：

1. 按指定时间自动触发。
2. 调用 guancli 拉取观远数据集。
3. 生成固定格式诊断结果。
4. 通过 guancli 写入观远表单。
5. 写入后 Super APP 能读取到表单数据集结果。
6. 失败时能留下日志，并支持人工补跑。

如果验证不通过，本期链路需要降级为手动运行脚本或观远侧工作流，不得默认依赖不可控的本地自动化。

### 7.3 线上化边界

本期接受 Codex 定时任务 + guancli + 观远表单作为 MVP / POC 链路。生产级线上化调度不在本期范围内，但必须记录后续迁移要求，包括：

- 不依赖个人本地 Codex。
- 不依赖个人电脑在线状态。
- 支持稳定认证、日志、告警、重试、补跑。
- 支持批次追溯和失败门店定位。

## 8. AI 解释文案链路

本节为后续 AI 诊断阶段预留。首期页面不展示 AI 解释文案；后续如启用，AI 解释文案默认由 T+1 诊断任务生成并沉淀，前端只读展示。

LLM 输入应是诊断任务整理后的结构化事实包，而不是原始全量数据。事实包至少包含：

- 门店对象：大区、小区、经销商、日期区间。
- 销售漏斗摘要。
- 过程指标摘要。
- 主要邀约/接待问题标签摘要。
- 典型证据摘要。
- 锁定事实：主问题、标签正负向、结果断点、优先动作等。

LLM 输出建议包含：

- 总体诊断结论。
- 结果断点解释。
- 过程原因解释。
- 优先动作解释。
- 负向问题提示。
- 话术建议。

如果后续为了 demo 或体验增强启用前端实时 LLM，必须遵守：

- 只能基于已加载的锁定事实包生成解释。
- 不得改写主问题、结果断点、标签正负向和指标数值。
- 结果只作为解释增强，不是官方诊断事实。
- 失败时不影响页面核心诊断展示。
- 需要缓存，避免同一门店反复调用模型。

## 9. 页面状态与异常处理

### 9.1 页面状态

| 状态 | 触发条件 | 页面表现 |
|---|---|---|
| 正常 | 当前门店、当前日期区间有销售漏斗、过程指标和明细数据 | 展示完整页面 |
| 数据加载中 | Super APP 正在读取数据集/表单结果 | 展示加载状态 |
| 无指标结果 | 当前门店或日期区间无销售/过程指标结果 | 指标区展示空态 |
| 诊断任务失败 | 后续 AI 诊断阶段最新批次生成失败或写入失败 | 首期页面不受影响；后续读取上一成功结果或空态 |
| 源数据未刷新 | 当前日期区间源数据缺失或未完成 T+1 | 展示数据准备中，不展示误导性诊断 |
| 样本不足 | 当前门店数据不足以生成可靠诊断 | 展示样本不足提示，保留基础指标 |
| 明细为空 | 无 IP/试驾打标明细 | 深度分析 Tab 展示空态，不影响销售漏斗和过程指标 |
| 权限无数据 | 用户无当前门店/区域数据权限 | 展示无权限或无可见数据提示 |

### 9.2 异常处理

| 异常 | 处理方式 |
|---|---|
| Codex 定时任务未执行 | 不生成新批次；页面读取上一成功批次或展示无诊断 |
| guancli 拉数失败 | 诊断批次标记失败，记录错误原因 |
| 源数据集缺失 | 诊断任务跳过相关门店或整体失败，记录缺失数据集 |
| 表单写入失败 | 不覆盖上一成功结果，保留失败日志 |
| 表单数据集未刷新 | 页面仍可能读到旧结果，需要展示生成时间 |
| 部分门店失败 | 成功门店正常展示，失败门店展示无最新诊断 |
| LLM 生成失败 | 保留官方诊断事实，解释文案展示降级文本 |
| 打标明细缺失 | 明细 Tab 空态，过程总结需提示证据不足 |

## 10. 验收标准

| 验收项 | 标准 |
|---|---|
| 页面范围 | 页面只包含当前 demo 已展示模块，不扩展新功能 |
| 数据来源 | 页面能明确区分销售指标、过程指标、IP 打标明细、试驾打标明细、诊断结果 |
| 前端计算边界 | 前端只计算当前权限范围内的累计、转化率、负向占比、环比和轻量筛选排序 |
| 后置计算边界 | 官方排名、官方分位、跨组织对标由数据层提供 |
| 诊断事实 | 后续 AI 诊断阶段主问题、结果断点、过程原因、优先动作由 Codex T+1 链路沉淀 |
| AI 结果 | 首期页面不展示官方 AI 诊断结果；后续来自沉淀结果，不由前端自由生成 |
| 明细下钻 | 电话邀约 Tab 来自 IP 打标明细，试驾接待 Tab 来自试驾打标明细 |
| 趋势交互 | 邀约/试驾趋势浮窗支持图例点击隐藏/恢复折线，至少保留 1 条可见折线；鼠标悬浮时展示最近日期和可见指标值 |
| 批次追溯 | 后续 AI 诊断阶段诊断结果能追踪诊断批次、生成时间、模型版本和 Prompt 版本 |
| 异常可解释 | 无数据、任务失败、样本不足、明细为空等状态有明确展示 |
| 行权限 | Super APP 读取观远数据集时继承观远行权限控制 |
| MVP 链路验证 | Codex 定时任务能完成拉数、生成、写表、前端读取闭环 |

## 11. 后续待建文档

| 文档 | 目的 |
|---|---|
| 销售诊断工作台_数据口径文档 | 定义每个指标的分子分母、过滤条件、去重规则、异常处理和 SQL/ETL 口径。 |
| 销售诊断工作台_数据契约 | 定义每张观远数据集/表单的字段、粒度、主键、刷新时间、权限字段和前端读取方式。 |
| 销售诊断工作台_Tech_Design | 定义整体实现链路，包括 Codex 定时任务、guancli、表单写入、Super APP 读取和异常处理。 |
| Codex 定时任务验证方案 | 验证本地 Codex 自动拉数、诊断生成、写表和前端读取闭环是否稳定。 |
| Super APP 联调清单 | 定义前端、观远数据集、表单数据集、行权限和异常状态的联调项。 |
