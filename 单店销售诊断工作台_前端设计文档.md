# 单店销售诊断工作台_前端设计文档

## 1. 文档目的与适用范围

### 1.1 文档目的

本文档用于指导后续在观远 Super APP 中，以当前静态 demo 为前端基线搭建单店销售诊断工作台。

本文档同时区分两类要求：

- 当前 demo 还原要求：严格还原现有 `index.html` 的页面结构、展示内容、视觉样式和已实现交互。
- Super APP 落地要求：在不新增页面主线和不改变 demo 表达的前提下，补齐 PRD 要求的数据接入、刷新联动、异常状态、批次追溯、权限和联调验收边界。

设计依据严格来自以下文件：

| 类型 | 文件 |
|---|---|
| 项目规则 | `/Users/chengfengguo/Documents/trae_projects/日常工作/零售全链路检核/AGENTS.md` |
| 子项目规则 | `/Users/chengfengguo/Documents/trae_projects/日常工作/零售全链路检核/销售诊断工作台-new/AGENTS.md` |
| 目标 demo | `/Users/chengfengguo/Documents/trae_projects/日常工作/零售全链路检核/销售诊断工作台-new/单店诊断工作台demo/index.html` |
| 同目录 PRD | `/Users/chengfengguo/Documents/trae_projects/日常工作/零售全链路检核/销售诊断工作台-new/单店诊断工作台demo/单店销售诊断工作台_PRD.md` |
| 既有 Super APP 参考 | `/Users/chengfengguo/Documents/trae_projects/日常工作/零售全链路检核/六量七率工作台-观远APP/六量七率工作台/` |

### 1.2 适用范围

本文档适用于：

- 观远 Super APP 单店销售诊断工作台前端搭建。
- 前端开发理解当前 demo 的模块、字段、交互和组件样式。
- 数据侧按页面需要准备对象、字段和占位契约。
- 测试侧分别验收当前 demo 还原度、Super APP 数据接入和异常状态完整性。

本文档不用于：

- 定义指标分子、分母、SQL、ETL 逻辑。
- 定义 AI 打标 Prompt 和模型判定规则。
- 扩展全国总览、区域总览、实时问答、实时调模型、整改任务流写回等 demo 中不存在的能力。

### 1.3 既有 Super APP 能力参考

`六量七率工作台-观远APP/六量七率工作台/` 仅作为 Super APP 工程能力和接入方式参考，不作为本页面视觉和业务模块的直接来源。本页面视觉、模块顺序和交互主线仍以当前单店 demo 为准。

可复用的 Super APP 能力要求如下：

| 能力 | 既有 demo 参考 | 本项目落地要求 |
|---|---|---|
| 工程形态 | React + TypeScript + Vite，单入口 SPA | 后续搭建应按单页应用组织，不拆多 HTML 入口。 |
| 运行时配置 | `public/settings.json` + `src/services/settings.ts` | 页面标题、应用名、可选模型配置等低频配置应放入运行时配置，避免每次调整都重新构建。 |
| BI 原生接口 | `src/bi-services/` | 观远 card、selector、form、LLM 等原生接口应集中封装，不在页面组件内散落请求。 |
| 业务服务层 | `src/services/` | 单店诊断的数据组装、筛选转换、异常降级、AI 解释封装应放在业务 service，不堆在页面组件内。 |
| 筛选与卡片取数 | `getSelectorOptions`、`getStandardCardData`、`IFieldFilter` | 大区、小区、经销商、月份切换后，应转成观远筛选条件读取对应数据。 |
| 表单/结果读取 | `src/bi-services/form.ts` | 若 AI 诊断结果、批次状态和打标明细落在观远表单/填报数据集，应通过统一 form/service 封装读取。 |
| 状态管理 | `loading`、`ready`、`error` 状态 | 页面需显式处理初始化、读取中、读取失败、无数据、上一成功批次等状态。 |
| 验证命令 | `npm run build`、`npm run lint`、`npm test`、`npm run design:lint` | 进入 Super APP 工程实现后，交付前必须完成构建、类型/格式/单测和设计 lint。 |
| 发布准备 | `index.html` 元数据、构建产物 zip、发布流程 | 发布前需确认应用名称、标题、favicon、版本号和绑定 AppId 等信息。 |

## 2. 页面定位、目标用户与业务主线

### 2.1 页面定位

当前页面定位为“单店销售过程诊断工作台”，部署形态为观远 Super APP 单页工作台。

页面表达方式是“轻汇报 + 强下钻”：

- 轻汇报：顶部先给当前门店 AI 诊断结论、结果断点、过程原因和优先动作。
- 强下钻：从销售漏斗和过程指标继续下钻到标签、顾问、客户明细和证据抽屉。

### 2.2 目标用户

| 用户 | 使用目标 |
|---|---|
| 门店经理 | 快速判断本店最大销售过程短板，找到可复盘客户和顾问。 |
| 区域/小区经理 | 下钻重点门店，识别是否需要门店辅导和过程整改。 |
| 前端开发者 | 按当前 demo 还原 Super APP 页面布局、组件、状态和交互。 |
| 数据/测试人员 | 按页面对象准备数据契约，并验证页面展示与交互。 |

### 2.3 页面主线

```text
对象定位
-> AI 诊断总览
-> 销售漏斗确认结果断点
-> 邀约/试驾过程指标定位过程短板
-> 第三层明细诊断查看标签和顾问分布
-> 点击客户明细打开证据抽屉
-> 用问题摘要、证据摘录、建议话术组织门店复盘
```

## 3. 页面整体信息架构

当前 demo 为单页纵向结构，包含 6 个主要区域：

| 层级 | 页面区域 | demo 节点/对象 | 业务作用 |
|---|---|---|---|
| 1 | 顶部对象区 | `header`、大区/小区/经销商/月度选择器、`重新诊断`按钮 | 明确当前诊断对象和月份。 |
| 2 | AI 诊断总览 | AI 诊断卡片、结果断点、过程原因、优先动作 | 用业务语言说明门店当前主要问题。 |
| 3 | 销售漏斗 | `funnelGrid`、`funnelMetrics` | 展示线索、到店、试驾、订单及关键转化率。 |
| 4 | 过程指标 | `ipProcessPanel`、`driveProcessPanel`、`processMetrics`、`processMetricGroups` | 展示邀约过程和试驾过程指标，并作为明细下钻入口。 |
| 5 | 第三层明细诊断 | `detailDiagnosis`、邀约技巧/接待技巧 Tab、标签分布、顾问分布、明细清单 | 围绕当前过程指标下钻到标签、顾问和客户记录。 |
| 6 | 辅助浮层 | `trendPopover`、`drawer` | 趋势浮窗展示日趋势；客户抽屉展示证据和话术。 |

页面默认状态：

- 默认门店：`上海浦东荣威体验中心`。
- 默认月份：`2026年6月`。
- 默认明细 Tab：`邀约技巧`。
- 默认聚焦指标：`负向邀约占比`。

## 4. 模块级设计说明

### 4.1 顶部对象区

| 项目 | 说明 |
|---|---|
| 位置 | 页面顶部 sticky header。 |
| 业务目的 | 明确当前诊断对象，支持用户切换大区、小区、经销商和月份。 |
| 展示内容 | 页面标题、门店名称、大区选择器、小区选择器、经销商选择器、月份选择器、`重新诊断`按钮。 |
| demo 字段/选项 | 大区：华东大区、华南大区、华北大区；小区：上海一区、上海二区、苏南一区；经销商：上海浦东/闵行/苏州工业园荣威体验中心；月份：2026年6月、2026年5月、2026年4月。 |
| 数据来源占位 | Super APP 页面传参或筛选参数；门店、组织、月份枚举来自观远数据集或权限范围。 |
| 交互规则 | demo 中选择器和按钮没有绑定数据刷新逻辑；Super APP 落地时，切换对象后应刷新整页数据。 |

`重新诊断`按钮说明：

- 当前 demo 只展示按钮，无实际事件处理。
- Super APP 落地时不得解释为“前端实时调模型重新诊断”。
- 如保留按钮，建议定义为“刷新/读取最新已沉淀诊断结果”；如需要触发新批次诊断，必须另行进入非本期范围和后端/调度方案确认。

### 4.2 AI 诊断总览

| 项目 | 说明 |
|---|---|
| 位置 | 顶部对象区下方第一张主卡片。 |
| 业务目的 | 用一句话说明本店当前最大问题，并给出结果断点、过程原因和优先动作。 |
| 展示内容 | AI 诊断标题、4 个状态标签、3 个结论块。 |
| 当前 demo 文案 | `订单转化偏弱，主要断点在客户到店；邀约质量和试驾顾虑承接是本周优先改善项。` |
| 状态标签 | `订单 9 ↓18.1%`、`到店率 28.0% ↓1.6pct`、`负向邀约 24.5% ↑3.5pct`、`负向试驾接待 22.6% ↑2.9pct`。 |
| 结论块 | 结果断点：客户到店不足，线索订单率偏弱；过程原因：邀约质量不足，接待技巧承接不足；优先动作：12 条高风险明细回访，复盘邀约和试驾话术。 |
| 数据来源占位 | 门店诊断结果表，粒度为门店 + 月份 + 诊断批次。 |
| 交互规则 | 只读展示，不由前端自由判断官方诊断结论。 |

### 4.3 销售漏斗

| 项目 | 说明 |
|---|---|
| 位置 | AI 诊断总览下方，独立卡片。 |
| 业务目的 | 让用户看到从线索到订单的结果断点。 |
| demo 对象 | `funnelMetrics`。 |
| 展示形态 | 8 张指标卡按链路顺序横向网格展示。绝对量卡为实线卡，转化率卡为虚线浅底卡。 |
| 数据来源占位 | 门店日粒度销售指标表或门店月度聚合指标。 |
| 交互规则 | 当前 demo 无点击联动；作为结果断点展示区，不直接驱动下方明细。 |

销售漏斗字段：

| 字段 | 含义 | 示例 |
|---|---|---|
| `label` | 指标名称 | 下发线索、到店、试驾、订单、线索到店率 |
| `value` | 指标值 | 168、47、31、9、28.0 |
| `unit` | 单位 | 条、人、单、% |
| `compare` | 环比展示文案 | 月环比 -6.2%、月环比 -1.6pct |
| `trend` | 趋势方向 | `up` / `down` |
| `rank` | 组织内排名，仅绝对量卡展示 | 2/8、5/8、3/8、6/8 |

当前 demo 指标清单：

| 类型 | 指标 |
|---|---|
| 绝对量 | 下发线索、到店、试驾、订单 |
| 转化率 | 线索到店率、到店试驾率、试驾订单率、线索订单率 |

### 4.4 过程指标

| 项目 | 说明 |
|---|---|
| 位置 | 销售漏斗下方，左右两列。 |
| 业务目的 | 定位过程短板属于邀约过程还是试驾过程。 |
| demo 对象 | `processMetrics`、`processMetricGroups`。 |
| 展示区域 | 左侧邀约过程，右侧试驾过程。 |
| 数据来源占位 | 门店过程指标数据、IP 电话打标汇总、试驾打标汇总。 |
| 交互规则 | 点击过程面板、指标组或单个指标，切换第三层明细 Tab 并聚焦对应指标；可滚动到第三层。 |

邀约过程指标组：

| 指标组 | 默认指标 | 组内指标 |
|---|---|---|
| 线索质量 | IP电话接通率 | IP电话接通率、30s以下线索占比 |
| 勤奋度 | 30分钟外呼率 | 30分钟外呼率、2天3呼率 |
| 邀约技巧 | 负向邀约占比 | 负向邀约占比 |

试驾过程指标组：

| 指标组 | 默认指标 | 组内指标 |
|---|---|---|
| 试驾强度 | 试驾平均里程 | 试驾平均里程 |
| 体验时长 | 平均时长 | 平均时长 |
| 接待技巧 | 负向试驾接待占比 | 负向试驾接待占比 |

过程指标字段：

| 字段 | 含义 | 示例 |
|---|---|---|
| `label` | 指标名称 | IP电话接通率 |
| `value` | 当前值 | 62.4% |
| `mom` | 月环比变化 | -3.2pct |
| `trend` | 趋势方向 | `up` / `down` |

### 4.5 趋势浮窗

| 项目 | 说明 |
|---|---|
| 位置 | 点击过程面板右上角 `more_horiz` 按钮后，以固定浮窗展示。 |
| 业务目的 | 辅助观察第二层过程指标在月内是否持续恶化或改善。 |
| demo 对象 | `dailyTrendData`。 |
| 展示内容 | 折线 SVG、图例、最新值。 |
| 数据来源占位 | 门店日粒度过程指标。 |
| 交互规则 | 点击邀约/试驾更多按钮打开对应趋势；再次点击同一按钮关闭；点击外部或关闭按钮关闭；`Escape` 关闭。 |

趋势数据字段：

| 字段 | 含义 |
|---|---|
| `eyebrow` | 浮窗小标题，如邀约过程、试驾过程。 |
| `title` | 浮窗标题，当前为第二层指标日趋势。 |
| `days` | X 轴日期数组。 |
| `series.name` | 指标名称。 |
| `series.color` | 折线颜色。 |
| `series.values` | 指标日趋势值。 |
| `normalize` | 是否归一化显示；试驾过程为 true。 |

### 4.6 第三层明细诊断

| 项目 | 说明 |
|---|---|
| 位置 | 过程指标下方，通过连接线与第二层过程指标形成视觉关系。 |
| 业务目的 | 将过程短板下钻到标签分布、顾问分布和客户明细。 |
| demo 对象 | `diagnosis`、`defaultMetricByTab`、`metricFocusTags`。 |
| 默认状态 | `邀约技巧` Tab，标题为 `邀约技巧下钻：负向邀约占比`。 |
| 数据来源占位 | IP 打标明细、试驾打标明细。 |
| 交互规则 | Tab 切换时切换数据域；过程指标点击时切换 Tab 和聚焦指标；明细行点击打开客户证据抽屉。 |

第三层包含 3 个子模块：

| 子模块 | 展示内容 | 交互规则 |
|---|---|---|
| 标签分布 | 一级标签、数量、占比、二级标签数量条 | 当前 demo 按聚焦指标高亮相关一级标签、弱化非相关标签；Super APP 落地应支持点击标签筛选明细。 |
| 顾问分布 | 顾问名称、问题数量、风险等级、横向条形图 | 当前 demo 只展示排序结果，无筛选联动；Super APP 落地应支持点击顾问筛选明细。 |
| 明细清单 | 客户、顾问、事件时间、AI 标签、问题摘要、风险 | 点击行打开证据抽屉；选中行高亮。 |

### 4.7 客户证据抽屉

| 项目 | 说明 |
|---|---|
| 位置 | 页面右侧抽屉，覆盖在当前页面之上。 |
| 业务目的 | 给门店复盘提供客户级证据和建议话术。 |
| demo 对象 | `openDrawer(record, typeName)` 使用 `diagnosis[activeTab].records`。 |
| 展示内容 | 客户、顾问、事件时间、AI 标签、问题摘要、证据摘录、建议话术、下一步动作按钮。 |
| 数据来源占位 | IP 打标明细或试驾打标明细的事件级记录。 |
| 交互规则 | 点击明细行打开；点击关闭按钮、遮罩或 `Escape` 关闭。 |

下一步动作按钮说明：

- 当前 demo 展示 `建立回访任务`、`加入顾问复盘清单` 两个按钮。
- demo 未定义真实任务写回、表单写入或流程状态更新。
- Super APP 本期如未接入任务流，按钮应作为展示型占位，或在落地时隐藏/禁用并说明后续范围。

## 5. 关键筛选、Tab、按钮、下钻与联动规则

### 5.1 筛选规则

| 控件 | 当前 demo | Super APP 落地规则 |
|---|---|---|
| 大区 | 静态选项，无 JS 事件 | 切换后联动小区、经销商，并刷新全页数据。 |
| 小区 | 静态选项，无 JS 事件 | 切换后联动经销商，并刷新全页数据。 |
| 经销商 | 静态选项，无 JS 事件 | 切换后刷新当前单店诊断数据。 |
| 月份 | 静态选项，无 JS 事件 | 切换后刷新当前月份诊断数据。 |

### 5.2 Tab 规则

| Tab | 数据域 | 默认聚焦指标 | 用途 |
|---|---|---|---|
| 邀约技巧 | `diagnosis.ip` | 负向邀约占比 | 展示 IP 电话邀约标签、顾问和客户证据。 |
| 接待技巧 | `diagnosis.drive` | 负向试驾接待占比 | 展示试驾接待标签、顾问和客户证据。 |

Tab 交互：

- 点击 `邀约技巧`，调用 `switchMetric("ip", "负向邀约占比", false)`。
- 点击 `接待技巧`，调用 `switchMetric("drive", "负向试驾接待占比", false)`。
- Tab 切换后，第三层标题、标签分布、顾问分布、明细清单、抽屉数据域均同步变化。

### 5.3 过程指标下钻规则

过程指标点击规则来自 `switchMetric(tab, metricLabel, scroll)`：

| 操作 | 结果 |
|---|---|
| 点击邀约过程面板空白区域 | 切到邀约技巧，聚焦 `负向邀约占比`，滚动到第三层。 |
| 点击试驾过程面板空白区域 | 切到接待技巧，聚焦 `负向试驾接待占比`，滚动到第三层。 |
| 点击指标组 | 切到对应过程域，聚焦该组默认指标，滚动到第三层。 |
| 点击单个指标卡 | 切到对应过程域，聚焦该指标，滚动到第三层。 |

### 5.4 指标与标签高亮规则

`metricFocusTags` 定义了指标与重点标签的映射。当前 demo 中只影响标签分布的高亮/弱化，不过滤明细记录。

Super APP 落地时，标签分布和顾问分布需要补齐明细筛选能力：

- 点击一级标签后，明细清单只展示当前 Tab 下命中该一级标签的记录；再次点击已选标签可取消筛选。
- 点击顾问后，明细清单只展示当前 Tab 下该顾问的记录；再次点击已选顾问可取消筛选。
- 标签筛选和顾问筛选如同时存在，应按交集过滤。
- 筛选只影响第三层明细清单和客户抽屉可打开记录，不反算销售漏斗、过程指标和官方 AI 诊断结论。
- 当前 demo 未实现该筛选行为，Super APP 搭建时应作为数据接入/联调验收项。

| 聚焦指标 | 高亮标签 |
|---|---|
| IP电话接通率 | 到店时间锁定、到店理由构建 |
| 30s以下线索占比 | 到店时间锁定、报价到店承接 |
| 30分钟外呼率 | 到店时间锁定 |
| 2天3呼率 | 到店时间锁定、到店理由构建 |
| 负向邀约占比 | 到店时间锁定、到店理由构建、报价到店承接、竞品比较转化 |
| 试驾平均里程 | 顾虑承接 |
| 平均时长 | 顾虑承接、版本推荐 |
| 负向试驾接待占比 | 顾虑承接、竞品攻防、版本推荐 |

### 5.5 浮窗与抽屉规则

| 组件 | 打开 | 关闭 |
|---|---|---|
| 趋势浮窗 | 点击邀约/试驾过程右上角更多按钮 | 点击同一按钮、关闭按钮、页面外部或 `Escape`。 |
| 客户证据抽屉 | 点击明细清单任一行 | 点击关闭按钮、抽屉遮罩或 `Escape`。 |

## 6. 数据结构与字段契约

本节只整理 demo 已有对象、字段和展示项，不扩展额外数据对象。

### 6.1 `funnelMetrics`

用途：渲染销售漏斗。

```js
{
  label: "下发线索",
  value: "168",
  unit: "条",
  compare: "月环比 -6.2%",
  trend: "down",
  rank: "2/8"
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `label` | 是 | string | 指标名称。 |
| `value` | 是 | string | 展示值，前端按文本展示。 |
| `unit` | 是 | string | 单位。 |
| `compare` | 是 | string | 环比文案。 |
| `trend` | 是 | enum | `up` 或 `down`，决定颜色和趋势图标。 |
| `rank` | 否 | string | 绝对量指标排名；转化率卡可为空。 |

### 6.2 `processMetrics`

用途：渲染邀约过程和试驾过程指标卡。

```js
{
  ip: [
    { label: "IP电话接通率", value: "62.4%", mom: "-3.2pct", trend: "down" }
  ],
  drive: [
    { label: "试驾平均里程", value: "4.8km", mom: "-0.7km", trend: "down" }
  ]
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `label` | 是 | string | 指标名称。 |
| `value` | 是 | string | 展示值，含单位可直接拼入。 |
| `mom` | 是 | string | 月环比变化，不含“月环比”前缀。 |
| `trend` | 是 | enum | `up` 或 `down`。 |

### 6.3 `processMetricGroups`

用途：定义过程指标的分组、默认下钻指标和组内指标。

```js
{
  title: "线索质量",
  defaultMetric: "IP电话接通率",
  metrics: ["IP电话接通率", "30s以下线索占比"]
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `title` | 是 | string | 指标组名称。 |
| `defaultMetric` | 是 | string | 点击指标组时默认聚焦的指标。 |
| `metrics` | 是 | string[] | 组内指标名称，必须能在 `processMetrics` 中找到。 |

### 6.4 `dailyTrendData`

用途：渲染趋势浮窗折线图。

```js
{
  eyebrow: "邀约过程",
  title: "第二层指标日趋势",
  yMin: 0,
  yMax: 100,
  ySuffix: "%",
  days: ["06-01", "06-03"],
  series: [
    { name: "IP电话接通率", color: "#003da6", values: [66, 64] }
  ]
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `eyebrow` | 是 | string | 浮窗所属过程域。 |
| `title` | 是 | string | 浮窗标题。 |
| `yMin` | 否 | number | Y 轴最小值。 |
| `yMax` | 否 | number | Y 轴最大值。 |
| `ySuffix` | 否 | string | Y 轴标签单位后缀。 |
| `normalize` | 否 | boolean | 是否按序列自身范围归一化展示。 |
| `days` | 是 | string[] | 日期标签。 |
| `series` | 是 | array | 折线序列。 |
| `series.name` | 是 | string | 指标名称。 |
| `series.color` | 是 | string | 折线颜色。 |
| `series.values` | 是 | number[] | 与 `days` 等长的数值数组。 |

### 6.5 `diagnosis`

用途：渲染第三层明细诊断、顾问分布、明细清单和证据抽屉。

```js
{
  ip: {
    name: "IP 电话",
    tags: [],
    advisors: [],
    records: []
  },
  drive: {
    name: "试驾接待",
    tags: [],
    advisors: [],
    records: []
  }
}
```

#### 6.5.1 `tags`

```js
{
  label: "到店时间锁定",
  count: 18,
  percent: 36,
  children: [
    { label: "到店时间主动询问", count: 7 }
  ]
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `label` | 是 | string | 一级标签。 |
| `count` | 是 | number | 命中记录数。 |
| `percent` | 是 | number | 标签占比，当前 demo 直接展示为 `%`。 |
| `children` | 是 | array | 二级标签列表。 |
| `children.label` | 是 | string | 二级标签。 |
| `children.count` | 是 | number | 二级标签命中数。 |

当前 demo 一级标签：

| 数据域 | 一级标签 |
|---|---|
| 邀约技巧 | 到店时间锁定、到店理由构建、报价到店承接、竞品比较转化 |
| 接待技巧 | 顾虑承接、竞品攻防、版本推荐 |

#### 6.5.2 `advisors`

```js
{
  name: "陈明",
  count: 13,
  risk: "高"
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `name` | 是 | string | 顾问名称。 |
| `count` | 是 | number | 问题记录数。 |
| `risk` | 是 | enum | `高`、`中`、`低`。 |

#### 6.5.3 `records`

```js
{
  customer: "客户 A1368",
  advisor: "陈明",
  time: "2026-06-17 10:42",
  tag: "到店时间锁定",
  risk: "高",
  summary: "顾问确认客户有购车计划，但未给出明确到店时间选项，客户仅回复再看看。",
  evidence: "客户说周末可能有空，顾问回复方便再联系，没有继续给出周六上午或下午二选一。",
  script: "建议补一句：我帮您先预留周六 10:30 或 14:00 两个试驾位，您看哪个时间更方便？"
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `customer` | 是 | string | 客户展示标识。 |
| `advisor` | 是 | string | 顾问名称。 |
| `time` | 是 | string | 事件时间。 |
| `tag` | 是 | string | AI 标签，展示为标签胶囊。 |
| `risk` | 是 | enum | `高`、`中`、`低`。 |
| `summary` | 是 | string | 问题摘要。 |
| `evidence` | 是 | string | 证据摘录。 |
| `script` | 是 | string | 建议话术。 |

### 6.6 Super APP 补充字段契约

本节为 Super APP 搭建补充字段，不要求当前静态 demo 已经渲染，但落地时需要由数据集或接口提供，用于满足 PRD 中的批次追溯、异常状态和权限边界。

#### 6.6.1 `diagnosisMeta`

用途：记录当前页面展示的诊断批次和数据状态。

```js
{
  batchId: "20260618_SH_PD_001",
  generatedAt: "2026-06-18 07:30:00",
  sourceRefreshAt: "2026-06-18 06:10:00",
  modelVersion: "model-v1",
  promptVersion: "prompt-v1",
  dataStatus: "normal",
  isLatestSuccess: true
}
```

字段契约：

| 字段 | 必填 | 类型 | 说明 |
|---|---|---|---|
| `batchId` | 是 | string | 诊断批次 ID，用于追溯本次 AI 诊断结果。 |
| `generatedAt` | 是 | string | 诊断结果生成时间。 |
| `sourceRefreshAt` | 否 | string | 源数据最近刷新时间，用于判断是否 T+1 完整。 |
| `modelVersion` | 是 | string | 生成 AI 诊断和打标明细使用的模型版本。 |
| `promptVersion` | 是 | string | 生成 AI 诊断和打标明细使用的 Prompt 版本。 |
| `dataStatus` | 是 | enum | `normal`、`loading`、`no_diagnosis`、`task_failed`、`source_not_ready`、`low_sample`、`detail_empty`、`no_permission`。 |
| `isLatestSuccess` | 是 | boolean | 当前展示结果是否为最新成功批次；为 false 时页面需提示展示的是上一成功结果。 |

#### 6.6.2 指标计算输入

当前 demo 中 `funnelMetrics` 和 `processMetrics` 使用已格式化文本。Super APP 落地时需在数据契约中确认采用哪种方式：

| 方式 | 前端要求 | 适用场景 |
|---|---|---|
| 后端提供展示值 | 前端直接渲染 `value`、`compare`、`rank` 等文本 | 快速还原 demo，减少前端公式差异。 |
| 前端基于原始值计算 | 需额外提供本期分子/分母、上期同口径值、排名文本或预计算排名 | 需要在 Super APP 端支持当前权限范围内累计、转化率、环比和轻量筛选排序。 |

无论采用哪种方式，官方排名、官方分位、跨组织对标、主问题、结果断点、过程原因和优先动作均不得由前端基于当前可见数据临时代表官方口径。

### 6.7 前端状态变量

| 变量 | 默认值 | 作用 |
|---|---|---|
| `activeTab` | `ip` | 当前第三层数据域。 |
| `activeMetricLabel` | `负向邀约占比` | 当前聚焦的过程指标。 |
| `selectedRowKey` | 空字符串 | 当前选中的明细行。 |

## 7. 视觉布局与组件规范

### 7.1 整体布局

| 项目 | 规范 |
|---|---|
| 页面最大宽度 | `1440px`。 |
| 页面背景 | `#f8f9ff`。 |
| 主体间距 | 页面左右 `16px`，中屏及以上 `24px`；模块间距 `20px`。 |
| 卡片 | 白底、浅边框、`rounded-lg`、轻阴影。 |
| Header | sticky 顶部固定，白底半透明，底部边框。 |
| 响应式 | 顶部筛选移动端换行；过程指标桌面双列，移动端单列；抽屉最大宽度 `440px`。 |

### 7.2 色彩

| 语义 | 颜色 |
|---|---|
| 主色 | `#003da6` |
| 主色浅底 | `#e6edff` |
| 页面背景 | `#f8f9ff` |
| 卡片背景 | `#ffffff` |
| 边框 | `#d9e2f2` |
| 主文本 | `#0b1c30` |
| 次文本 | `#5f6f85` |
| 警示 | `#b45f06` |
| 风险/下降 | `#b42318` |
| 成功/正向 | `#067647` |
| 试驾域强调 | `#006c4a` |

### 7.3 字体与图标

| 项目 | 规范 |
|---|---|
| 正文字体 | `Inter`。 |
| 标题字体 | `Hanken Grotesk`。 |
| 数字/排名 | `JetBrains Mono` 可用于局部数字。 |
| 图标 | Material Symbols Outlined。 |
| 主要图标 | `refresh`、`call`、`directions_car`、`more_horiz`、`close`、`trending_up`、`trending_down`、`task_alt`、`groups`。 |

### 7.4 组件规范

| 组件 | 规范 |
|---|---|
| 筛选器 | 高度 `36px`，白底，浅边框，聚焦时主色边框和浅主色外圈。 |
| 主按钮 | 主色背景、白字、图标 + 文案。 |
| 指标卡 | 最小高度 `116px`，标题、数值、单位、环比、趋势图标。 |
| 过程指标卡 | 可点击，选中时按邀约/试驾域显示主色或绿色边框与 ring。 |
| Tab | 胶囊式双 Tab，选中状态使用主色文字、主色边框和浅主色底。 |
| 标签分布 | 一级标签卡 + 进度条 + 二级标签小条。 |
| 顾问分布 | 横向条形图，风险高红色、中橙色、低绿色。 |
| 明细表 | 横向滚动，最小宽度 `880px`，行 hover 浅主色，选中行 `#f0f5ff`。 |
| 浮窗 | 固定定位，宽度不超过 `560px`，带浅边框和阴影。 |
| 抽屉 | 右侧滑入，宽度 `440px`，遮罩背景 `bg-slate-900/25`。 |

## 8. 观远 Super APP 落地注意事项

### 8.1 数据与前端边界

| 类型 | 落地要求 |
|---|---|
| 官方诊断事实 | AI 诊断结论、结果断点、过程原因、优先动作应读取沉淀结果，不由前端临时生成。 |
| 展示公式 | 前端可按已加载数据展示数值、环比、标签高亮和当前范围内排序；具体由后端提供展示值还是前端计算，需在数据契约中确认。 |
| 跨组织排名/分位 | 如需作为官方口径，应由数据层预计算后提供；当前 demo 仅展示排名文本。 |
| AI 解释 | 当前页面只读展示，不在前端实时调模型。 |
| 批次追溯 | 页面需能获取诊断批次、生成时间、模型版本、Prompt 版本和最新成功批次状态。 |
| 行权限 | Super APP 应继承观远数据集行权限，避免门店看到无权限客户/顾问记录。 |

### 8.2 数据对象映射

| demo 对象 | Super APP 建议来源 |
|---|---|
| `funnelMetrics` | 门店销售漏斗指标结果。 |
| `processMetrics` | 门店邀约/试驾过程指标结果。 |
| `processMetricGroups` | 前端配置或指标配置表。 |
| `dailyTrendData` | 门店日粒度过程指标。 |
| `diagnosis.ip.tags/advisors/records` | IP 电话打标明细及其聚合。 |
| `diagnosis.drive.tags/advisors/records` | 试驾打标明细及其聚合。 |
| AI 诊断总览 | 门店诊断结果表。 |
| `diagnosisMeta` | 门店诊断批次/运行状态结果。 |

### 8.3 推荐工程分层

参考既有六量七率 Super APP 工程，后续实现建议按以下分层组织：

| 层级 | 建议职责 | 本项目示例 |
|---|---|---|
| `src/App.tsx` | 页面状态、布局编排、用户交互、组件组合 | 当前单店诊断页面主体、Tab、抽屉、浮窗、筛选面板。 |
| `src/services/singleStoreDiagnosis.ts` | 业务数据服务，组装页面需要的数据对象 | 加载 `funnelMetrics`、`processMetrics`、`dailyTrendData`、`diagnosis`、`diagnosisMeta`。 |
| `src/services/settings.ts` | 读取运行时配置 | 应用名、页面标题、是否启用解释增强、可选 LLM 配置 ID。 |
| `src/bi-services/card.ts` | 观远卡片数据读取 | 门店销售漏斗、过程指标、日趋势等卡片或数据集结果。 |
| `src/bi-services/selector.ts` | 观远筛选器选项读取 | 大区、小区、经销商、月份候选值和默认值。 |
| `src/bi-services/form.ts` | 观远表单/填报数据读取 | AI 诊断结果表、AI 打标明细表、批次状态表。 |
| `src/core/request.ts`、`src/core/url.ts` | 请求和 URL 统一封装 | 避免在页面组件内直接拼 URL 或散落 `fetch`。 |

实现约束：

- 不在前端本地持久化业务数据；每次切换对象后按当前权限和筛选条件动态读取。
- 不直接修改 `src/bi-services/` 的通用协议，除非确实缺少观远原生接口封装。
- 业务字段映射、观远字段 ID、表单字段 ID、卡片 ID、selector ID 应在 service 层集中维护。
- 组件层只消费页面对象，不直接处理观远原始 `row.data`、`column.data` 或表单原始行结构。

### 8.4 刷新与异常状态

当前 demo 未实现加载、空态和失败态。Super APP 落地时至少需要补齐：

| 状态 | 页面要求 | 数据判断来源 |
|---|---|---|
| 初始化中 | 正在读取运行时配置、筛选器选项和默认对象。 | `settings.json`、selector 选项、默认参数。 |
| 加载中 | 页面模块显示加载态，避免旧数据误判。 | 当前筛选条件下的 card/form 请求状态。 |
| 无诊断结果 | AI 诊断区提示暂无诊断结果；有基础指标时可继续展示。 | 当前门店/月度无成功 `diagnosisMeta` 或诊断结果为空。 |
| 诊断任务失败 | 展示上一成功批次结果或空态，并提示最新任务失败和结果日期。 | 批次状态为失败，且存在/不存在上一成功批次。 |
| 源数据未刷新 | 展示数据准备中，不展示误导性 AI 诊断结论。 | `sourceRefreshAt` 未达到 T+1 完整要求。 |
| 样本不足 | 展示样本不足提示，保留基础销售漏斗和过程指标。 | `dataStatus = low_sample` 或诊断结果表给出样本不足标记。 |
| 明细为空 | 第三层 Tab 展示空态，不影响销售漏斗和过程指标。 | 当前 Tab 对应 IP/试驾打标明细为空。 |
| 权限无数据 | 展示无权限或当前范围无数据。 | 观远行权限导致对象/明细为空，或接口返回无可见数据。 |
| 诊断结果非最新 | 展示生成时间或批次时间，避免用户误解。 | `isLatestSuccess = false` 或表单数据集刷新延迟。 |

### 8.5 Super APP 数据接入验收

以下内容不是当前静态 demo 的已实现行为，但属于后续 Super APP 搭建必须验收的前端接入项：

| 验收项 | 标准 |
|---|---|
| 对象刷新 | 切换大区、小区、经销商、月份后，页面基于新对象读取对应销售漏斗、过程指标、诊断结果和明细数据。 |
| 最新成功批次 | 页面默认读取当前对象、当前月份的最新成功诊断批次。 |
| 失败回退 | 最新批次失败时，不覆盖上一成功结果；页面提示当前展示的是上一成功批次。 |
| 批次信息 | 页面能展示或至少持有诊断批次 ID、生成时间、模型版本、Prompt 版本。 |
| 表单数据集延迟 | 写入后如表单数据集尚未刷新，页面需提示结果可能不是最新。 |
| 明细筛选 | 标签和顾问筛选只影响第三层明细，不反算官方结论和上方指标。 |
| 行权限 | 用户只能看到权限范围内的门店、顾问和客户明细。 |
| 空态兜底 | 无诊断、样本不足、明细为空、无权限时均有明确页面反馈。 |

### 8.6 工程与发布要求

| 类别 | 要求 |
|---|---|
| SPA 单入口 | 应通过单个 `index.html` 和前端路由/状态完成页面，不拆多个 HTML 入口。 |
| 应用元数据 | 发布前更新应用名称、页面标题、favicon、版本号等模板信息。 |
| 运行时配置 | `settings.json` 中保留应用名、页面标题、可选 LLM 配置和功能开关；发布后可低成本调整。 |
| 接口封装 | 观远原生接口集中在 `src/bi-services/`，业务聚合集中在 `src/services/`。 |
| 构建产物 | 交付 Super APP 前需生成可上传发布包，并确认绑定 AppId 或创建/绑定策略。 |
| 质量检查 | 修改工程代码后执行 `npm run build`、`npm run lint`、`npm test`；涉及页面视觉后执行 `npm run design:lint`。 |
| 设计边界 | 不沿用旧六量七率工作台的视觉模块；仅复用 Super APP 工程、数据接入、发布和验证能力。 |

### 8.7 性能与适配

- 明细表字段较多，必须保留横向滚动。
- 趋势浮窗需要根据按钮位置计算坐标，避免超出视口。
- 抽屉在移动端宽度不超过视口。
- 标签分布和顾问分布数据量增大时，应限制默认展示数量或使用滚动容器；该行为需按后续数据量另行确认。

## 9. 非本期范围

以下能力不属于当前页面前端首版范围，不应写入本期 Super APP 前端实现：

- 全国/区域总览页面扩展。
- 现场问答模块。
- 前端实时重新诊断或实时调用模型。
- 整改任务流真实写回。
- 生产级调度平台线上化。
- 新增当前 demo 未出现的指标、页面、Tab、按钮、标签体系或客户字段。
- 将 `建立回访任务`、`加入顾问复盘清单` 两个展示按钮直接实现为真实流程写回。

## 10. 验收清单

### 10.1 当前 demo 还原验收

| 验收项 | 标准 |
|---|---|
| 页面范围 | 只包含当前 demo 已有模块：顶部对象区、AI 诊断、销售漏斗、过程指标、第三层明细、趋势浮窗、客户抽屉。 |
| 顶部筛选 | 大区、小区、经销商、月份控件存在；当前 demo 仅支持静态选项展示，不验收真实数据刷新。Super APP 落地如需刷新，应作为后续联调/数据接入验收项。 |
| AI 诊断 | 能展示总体结论、4 个状态标签、结果断点、过程原因、优先动作。 |
| 销售漏斗 | 8 个指标按当前 demo 顺序展示，趋势、环比、单位、排名样式正确。 |
| 过程指标 | 邀约过程 3 组 5 个指标、试驾过程 3 组 3 个指标展示完整。 |
| 指标下钻 | 点击过程面板、指标组、指标卡后，第三层 Tab、标题和标签高亮同步变化。 |
| 趋势浮窗 | 邀约/试驾趋势按钮可打开对应日趋势；可通过按钮、外部点击和 `Escape` 关闭。 |
| Tab 切换 | 邀约技巧展示 IP 电话数据；接待技巧展示试驾接待数据。 |
| 标签分布 | 一级标签、二级标签、数量、占比展示完整；聚焦标签高亮、非聚焦标签弱化。 |
| 顾问分布 | 顾问名称、问题数量、风险等级和条形图展示完整。 |
| 明细清单 | 客户、顾问、事件时间、AI 标签、问题摘要、风险字段展示完整。 |
| 客户抽屉 | 点击明细行后显示客户、顾问、时间、标签、摘要、证据摘录、建议话术。 |
| 关闭交互 | 抽屉和浮窗均支持关闭按钮、遮罩/外部点击和 `Escape` 关闭。 |
| 视觉还原 | 页面主色、卡片、Tab、指标卡、表格、抽屉和浮窗样式与 demo 保持一致。 |
| 边界合规 | 不出现 demo 外新增页面、实时问答、实时调模型或真实任务流写回。 |
| 数据边界 | 官方诊断结论读取沉淀结果；前端不自由生成主问题、结果断点、过程原因和优先动作。 |

### 10.2 Super APP 数据接入/联调验收

| 验收项 | 标准 |
|---|---|
| 筛选刷新 | 大区、小区、经销商、月份切换后，刷新当前对象对应的全页数据；筛选级联和权限范围一致。 |
| 数据来源 | 页面能明确区分销售指标、过程指标、IP 打标明细、试驾打标明细、门店诊断结果和批次状态。 |
| 前端计算边界 | 前端只计算当前权限范围内的累计、转化率、负向占比、环比和轻量筛选排序；官方排名/分位由数据层提供。 |
| 诊断事实 | 主问题、结果断点、过程原因、优先动作来自 Codex T+1 诊断结果沉淀，不由前端自由生成。 |
| 批次追溯 | 页面能追踪诊断批次 ID、生成时间、模型版本、Prompt 版本，并能识别是否最新成功批次。 |
| 异常状态 | 加载中、无诊断结果、诊断任务失败、源数据未刷新、样本不足、明细为空、权限无数据均有明确展示。 |
| 明细筛选 | 标签分布和顾问分布支持筛选第三层明细；筛选不影响上方销售漏斗、过程指标和 AI 诊断结论。 |
| 行权限 | Super APP 读取观远数据集时继承观远行权限控制，不能展示无权限门店、顾问或客户记录。 |
| MVP 链路闭环 | Codex/数据任务完成拉数、生成、写表后，Super APP 能读取最新成功结果；失败时能展示上一成功结果或空态。 |
| 工程分层 | 页面组件不直接散落观远请求；card、selector、form、settings 和业务聚合逻辑有清晰 service 分层。 |
| 运行时配置 | 应用名称、标题、可选模型配置和功能开关支持通过 `settings.json` 管理。 |
| 发布检查 | Super APP 工程交付前通过构建、lint、测试和设计 lint，并确认应用元数据与发布包。 |
