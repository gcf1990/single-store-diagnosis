# 单店销售诊断工作台 Codex 沉淀清单与字段口径

## 1. 文档目的

本文档用于明确单店销售诊断工作台中，哪些数据由 Codex 每日诊断任务沉淀到观远表单，哪些数据由数仓数据集提供，哪些展示值由 Super APP 前端在当前权限范围内计算。

核心原则：

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

由于观远存在行权限控制，涉及小区排名、大区分位、跨组织对标的字段，必须由数据层或 Codex 每日任务提前预计算并落表，前端不得基于当前可见门店临时计算官方排名或官方分位。

## 2. 数据供给边界

| 数据对象 | 供给方 | 是否 Codex 表单落表 | 前端使用方式 |
|---|---|---:|---|
| 门店诊断结果 | Codex 每日任务 | 是 | 读取官方诊断结论、结果断点、过程原因、优先动作 |
| 官方排名/分位结果 | 数据层预计算或 Codex 每日任务 | 是 | 读取小区排名、大区分位、跨组织对标结果 |
| Codex 批次状态 | Codex 每日任务 | 是 | 判断最新成功批次、失败回退、源数据状态 |
| 门店销售指标数据集 | 观远/数仓 | 否 | 前端读取后计算累计、转化率、环比 |
| 门店过程指标数据集 | 观远/数仓 | 否 | 前端读取后计算过程指标和日趋势 |
| IP 打标明细数据集 | 数仓 | 否 | 前端读取标签、证据、命中原因、顾问分布、明细抽屉 |
| 试驾打标明细数据集 | 数仓 | 否 | 前端读取标签、证据、命中原因、顾问分布、明细抽屉 |
| 页面结构与指标分组 | 前端静态配置 | 否 | 控制 Tab、指标组、默认聚焦和展示顺序 |

## 3. Codex 输出表单

Codex 每日任务只沉淀 3 类表单：

| 表单 | 粒度 | 用途 |
|---|---|---|
| 门店诊断结果表 | 经销商 + 统计月份 + 诊断批次 | 存官方诊断事实 |
| 官方排名/分位结果表 | 经销商 + 统计周期 + 指标 + 排名范围 + 诊断批次 | 存跨组织预计算结果 |
| Codex 批次状态表 | 诊断批次 + 任务环节 + 可选经销商 | 存任务运行、失败、补跑和读取状态 |

枚举值规则：

- 字段名保留英文，便于脚本、接口和前端稳定引用。
- 状态、类型、范围、方向等枚举取值统一存中文，便于观远直接统计和前端直接展示。
- 如后续需要英文编码，可另增编码字段，不替换当前中文枚举展示字段。

## 4. 门店诊断结果表

### 4.1 粒度与主键

| 项目 | 口径 |
|---|---|
| 表粒度 | 一店一月一批次一行 |
| 逻辑主键 | `dealer_code + stat_month + diagnosis_batch_id` |
| 前端读取 | 默认读取当前经销商、当前月份的最新成功批次 |
| 覆盖策略 | 新批次成功后标记为最新成功批次；失败批次不覆盖上一成功结果 |

### 4.2 字段清单

| 中文字段 | 建议字段名 | 类型 | 必填 | 中文含义 | 字段口径 |
|---|---|---|---:|---|---|
| 诊断结果ID | diagnosis_result_id | string | 是 | 一条门店诊断结果的唯一标识 | 建议由 `dealer_code + stat_month + diagnosis_batch_id` 生成 |
| 诊断批次ID | diagnosis_batch_id | string | 是 | Codex 每次运行的全链路批次 | 与批次状态表、排名/分位结果表关联 |
| 统计月份 | stat_month | string | 是 | 本次诊断归属月份 | 格式 `YYYY-MM` |
| 大区编码 | region_code | string | 是 | 门店所属大区编码 | 来自组织主数据 |
| 大区名称 | region_name | string | 是 | 门店所属大区名称 | 前端展示和筛选使用 |
| 小区编码 | district_code | string | 是 | 门店所属小区编码 | 来自组织主数据 |
| 小区名称 | district_name | string | 是 | 门店所属小区名称 | 前端展示和筛选使用 |
| 经销商代码 | dealer_code | string | 是 | 门店唯一编码 | 行权限关键字段 |
| 经销商名称 | dealer_name | string | 是 | 门店展示名称 | 前端展示使用 |
| 诊断状态 | diagnosis_status | enum | 是 | 本店本月诊断是否生成成功 | `成功`、`失败`、`样本不足` |
| 数据状态 | data_status | enum | 是 | 前端展示状态 | `正常`、`无诊断结果`、`任务失败`、`源数据未就绪`、`样本不足`、`明细为空`、`无权限` |
| 总体诊断结论 | overall_conclusion | text | 是 | AI 诊断总览标题文案 | 官方诊断事实，前端只读展示 |
| 主问题编码 | main_issue_code | string | 是 | 官方主问题编码 | 必须来自固定主问题字典 |
| 主问题名称 | main_issue_name | string | 是 | 官方主问题名称 | 例如线索供给不足、到店转化不足、订单转化不足 |
| 结果断点 | result_breakpoint | text | 是 | 销售链路主要断点 | 说明结果问题，例如客户到店不足、线索订单率偏弱 |
| 过程原因 | process_reason | text | 是 | 造成结果断点的主要过程短板 | 例如邀约质量不足、接待技巧不足 |
| 优先动作 | priority_action | text | 是 | 建议门店优先执行的动作 | 使用负向明细复盘等可执行表达，不使用高风险概念 |
| 聚焦数据域 | focus_domain | enum | 是 | 前端默认聚焦的模块 | `销售漏斗`、`邀约过程`、`试驾过程` |
| 聚焦指标编码 | focus_metric_code | string | 是 | 前端默认聚焦指标编码 | 必须能匹配前端指标配置 |
| 聚焦指标名称 | focus_metric_name | string | 是 | 前端默认聚焦指标名称 | 例如负向邀约占比、负向试驾接待占比 |
| 邀约负向明细数 | ip_negative_detail_count | integer | 是 | 邀约模块负向事件数 | 当前门店当前月份 IP 明细中 `标签正负向 = 负向` 的事件级去重数 |
| 试驾接待负向明细数 | drive_negative_detail_count | integer | 是 | 试驾接待模块负向事件数 | 当前门店当前月份试驾明细中 `标签正负向 = 负向` 的事件级去重数 |
| 总负向明细数 | negative_detail_count | integer | 是 | 邀约和试驾接待负向事件总数 | `ip_negative_detail_count + drive_negative_detail_count` |
| 邀约样本量 | ip_sample_count | integer | 是 | 邀约模块可用于诊断的样本数 | 当前门店当前月份 IP 明细中成功打标的事件级去重数 |
| 试驾接待样本量 | drive_sample_count | integer | 是 | 试驾接待模块可用于诊断的样本数 | 当前门店当前月份试驾明细中成功打标的事件级去重数 |
| 总样本量 | sample_count | integer | 是 | 两个过程模块样本总数 | `ip_sample_count + drive_sample_count` |
| 邀约最小样本阈值 | ip_min_sample_threshold | integer | 是 | 邀约模块参与诊断的最小样本要求 | 首版默认 20 条成功打标事件 |
| 试驾接待最小样本阈值 | drive_min_sample_threshold | integer | 是 | 试驾接待模块参与诊断的最小样本要求 | 首版默认 10 条成功打标事件 |
| 样本不足原因 | low_sample_reason | text | 否 | 样本不足时的原因说明 | 当 `diagnosis_status = 样本不足` 或单模块样本不足时填写 |
| 高亮指标编码 | highlight_metric_codes | string | 否 | AI 诊断总览中需要高亮的指标 | 多个指标编码用英文逗号分隔；只做高亮，不带数值 |
| 证据摘要 | evidence_summary | text | 否 | 支撑诊断结论的证据摘要 | 汇总 TOP 负向标签、典型证据和负向明细规模 |
| AI 解释文案 | ai_explanation | text | 否 | 对官方诊断事实的解释增强 | 不得改写主问题、断点、原因和优先动作 |
| 诊断模型版本 | diagnosis_model_version | string | 是 | 生成门店诊断结果使用的模型版本 | 用于追溯 |
| 诊断 Prompt 版本 | diagnosis_prompt_version | string | 是 | 生成门店诊断结果使用的 Prompt 版本 | 用于追溯 |
| 生成时间 | generated_at | datetime | 是 | Codex 生成本行诊断结果的时间 | 精确到秒 |
| 是否最新成功批次 | is_latest_success | boolean | 是 | 当前门店月份是否读取本批次作为最新成功结果 | 同门店同月份仅一个最新成功批次 |
| 上一成功批次ID | fallback_batch_id | string | 否 | 当前批次失败时可回退的上一成功批次 | 只允许同门店同月份回退 |

### 4.3 字段口径说明

- `总体诊断结论`、`结果断点`、`过程原因`、`优先动作` 属于官方诊断事实，前端不得临时生成或改写。
- `高亮指标编码` 只告诉前端哪些指标需要在 AI 诊断总览中高亮；指标数值仍由前端从观远指标数据集中读取并计算。
- `ip_negative_detail_count` 和 `drive_negative_detail_count` 来自数仓已加工 IP/试驾打标明细，Codex 只做门店月度聚合，不重复落事件级明细。
- 负向判断统一使用明细表字段 `标签正负向 = 负向`，不再使用风险等级或高风险定义。
- `样本不足` 不是页面空态文案，而是诊断链路状态；前端应保留基础指标展示，并提示诊断样本不足。

### 4.4 核心诊断字段生成逻辑

门店诊断结果表的核心字段按“结果断点 -> 过程原因 -> 证据支撑 -> 优先动作”的顺序生成。Codex 可以读取基础指标和数仓打标明细参与判断，但只沉淀官方诊断事实，不沉淀基础指标值。

| 字段 | 生成逻辑 | 主要输入 | 输出要求 |
|---|---|---|---|
| `diagnosis_status` | 根据源数据、样本量、诊断任务是否成功确定 | 批次状态、销售指标、过程指标、IP/试驾打标明细 | 只允许 `成功`、`失败`、`样本不足` |
| `data_status` | 面向前端展示的数据状态 | 批次状态、源数据刷新状态、诊断结果、明细数据 | 只允许 `正常`、`无诊断结果`、`任务失败`、`源数据未就绪`、`样本不足`、`明细为空`、`无权限` |
| `main_issue_code` / `main_issue_name` | 从固定主问题字典中选择一个主问题 | 销售漏斗断点、过程指标异常、负向标签分布、官方排名/分位 | 必须使用稳定字典，不自由生成新分类 |
| `result_breakpoint` | 识别销售链路最大的结果断点 | 下发线索、到店、试驾、订单、转化率、环比、排名/分位 | 用业务语言说明结果问题，例如客户到店不足、线索订单率偏弱 |
| `process_reason` | 解释造成结果断点的主要过程原因 | IP 过程指标、试驾过程指标、负向标签、证据摘要 | 最多保留 1-2 个主因，避免堆叠过多原因 |
| `priority_action` | 基于主问题和负向证据生成优先动作 | 模块负向明细数、TOP 负向标签、证据摘要 | 必须可落到门店复盘动作，不写泛泛建议 |
| `focus_domain` | 决定前端默认聚焦模块 | 主问题、结果断点、过程原因 | 只允许 `销售漏斗`、`邀约过程`、`试驾过程` |
| `focus_metric_code` / `focus_metric_name` | 决定前端默认聚焦指标 | 异常程度最高或最能解释主问题的指标 | 必须能匹配前端指标配置 |
| `ip_negative_detail_count` | 统计邀约负向明细数 | 数仓 IP 打标明细 | 当前门店当前月份、事件级去重、`标签正负向 = 负向` |
| `drive_negative_detail_count` | 统计试驾接待负向明细数 | 数仓试驾打标明细 | 当前门店当前月份、事件级去重、`标签正负向 = 负向` |
| `negative_detail_count` | 统计总负向明细数 | 邀约和试驾接待负向明细数 | `ip_negative_detail_count + drive_negative_detail_count` |
| `ip_sample_count` / `drive_sample_count` | 统计模块样本量 | 数仓 IP/试驾打标明细 | 成功打标事件级去重数 |
| `highlight_metric_codes` | 标记 AI 诊断总览需要高亮的指标 | 主问题相关指标、异常指标、重点过程指标 | 多个指标用英文逗号分隔；只做高亮，不带数值 |
| `evidence_summary` | 汇总支撑诊断的证据摘要 | TOP 负向标签、典型证据、负向明细规模 | 只写摘要，不替代明细数据集 |
| `ai_explanation` | 生成更完整的解释文案 | 已锁定的主问题、断点、原因、动作和证据摘要 | 不得改写已锁定字段，只能解释增强 |

### 4.5 主问题字典

主问题必须从固定字典中选择。当前首版建议只覆盖页面已有业务链路，不扩展到页面外问题。

| 主问题编码 | 主问题名称 | 默认聚焦数据域 | 常见结果断点 | 常见过程原因 |
|---|---|---|---|---|
| `lead_volume_weak` | 线索供给不足 | 销售漏斗 | 下发线索不足，订单承压 | 线索规模偏低，线索供给不足 |
| `arrival_conversion_weak` | 到店转化不足 | 邀约过程 | 客户到店不足，线索订单率偏弱 | 邀约质量不足，到店理由构建不足 |
| `order_conversion_weak` | 订单转化不足 | 试驾过程 | 试驾后订单转化不足 | 顾虑承接不足，竞品攻防不足 |
| `invitation_quality_weak` | 邀约质量不足 | 邀约过程 | 到店率偏弱或持续下滑 | 负向邀约占比高，短通话占比高 |
| `reception_skill_weak` | 接待技巧不足 | 试驾过程 | 试驾订单率偏弱或持续下滑 | 负向试驾接待占比高，顾虑承接不足 |

主问题选择规则：

1. 先判断销售漏斗结果断点，再判断过程原因，避免只因某个过程指标异常就直接定主问题。
2. 如果结果断点与过程原因不一致，以结果断点作为主问题优先级，过程原因写入 `process_reason`。
3. 如果多个主问题同时命中，优先选择对订单影响最大、且有负向明细或官方排名/分位支撑的问题。
4. 如果基础数据足够但 IP/试驾明细为空，可以生成漏斗类诊断，但 `data_status` 应标记为 `明细为空`，证据摘要说明证据不足。

主问题触发条件：

| 主问题 | 主触发条件 | 辅助条件 | 备注 |
|---|---|---|---|
| 线索供给不足 | 下发线索表现分位 `< 30` | 下发线索环比下降，订单承压 | 结果类主问题 |
| 到店转化不足 | 线索到店率表现分位 `< 30` 或到店表现分位 `< 30` | 邀约过程表现分位 `< 30` 或 `ip_negative_detail_count > 10` | 结果类主问题，过程原因可写邀约质量不足 |
| 订单转化不足 | 线索订单率表现分位 `< 30`、试驾订单率表现分位 `< 30` 或订单表现分位 `< 30` | 试驾接待表现分位 `< 30` 或 `drive_negative_detail_count > 10` | 结果类主问题，过程原因可写接待技巧不足 |
| 邀约质量不足 | 结果断点不明显，且邀约过程表现分位 `< 20`，且 `ip_sample_count >= 20`，且 `ip_negative_detail_count > 10` | TOP 负向邀约标签集中 | 过程类兜底主问题 |
| 接待技巧不足 | 结果断点不明显，且试驾接待表现分位 `< 20`，且 `drive_sample_count >= 10`，且 `drive_negative_detail_count > 10` | TOP 负向试驾接待标签集中 | 过程类兜底主问题 |

阈值规则：

- 结果类主问题：表现分位 `< 30` 触发。
- 过程类作为过程原因：表现分位 `< 30` 或对应模块负向明细数 `> 10`。
- 过程类作为主问题：结果断点不明显，且表现分位 `< 20`，且对应模块样本充足，且对应模块负向明细数 `> 10`。
- 样本不足的模块不评价该模块负向问题，不使用该模块负向明细数触发主问题或过程原因。

### 4.6 结果断点判断逻辑

结果断点用于回答“这家店结果差在哪里”。Codex 读取销售漏斗基础数据、环比和官方排名/分位后生成文本，但不把基础数值写入诊断结果表。

| 候选断点 | 参考指标 | 判断优先级 | 输出示例 |
|---|---|---:|---|
| 线索供给不足 | 下发线索、下发线索排名/分位、下发线索环比 | 1 | 下发线索不足，后续转化承压 |
| 到店转化不足 | 线索到店率、到店数、到店排名/分位、到店环比 | 2 | 客户到店不足，线索订单率偏弱 |
| 订单转化不足 | 试驾订单率、线索订单率、订单数、订单排名/分位 | 3 | 试驾后订单转化不足，订单结果偏弱 |

默认判断顺序：

1. 先找最终结果最差的链路，例如订单、线索订单率、试驾订单率。
2. 再向上追溯最可能造成该结果的前序断点，例如到店不足或订单转化不足。
3. 如果绝对量和转化率同时异常，`result_breakpoint` 同时表达“规模不足 + 转化偏弱”。
4. 如果排名/分位显示跨组织落后，即使当前门店环比改善，也可以保留为结果断点，但解释中要说明“仍处于低分位”。

### 4.7 过程原因判断逻辑

过程原因用于回答“为什么出现这个结果断点”。过程原因必须由过程指标和数仓打标明细共同支撑。

| 过程原因 | 参考指标/明细 | 常见证据 |
|---|---|---|
| 邀约质量不足 | IP 电话接通率、30s 以下线索占比、负向邀约占比 | 到店时间锁定不足、到店理由构建不足、报价到店承接弱 |
| 接待技巧承接不足 | 负向试驾接待占比 | 顾虑承接不足、竞品攻防不足、版本推荐不清 |

默认判断规则：

1. 优先选择能解释 `result_breakpoint` 的过程原因。
2. 同一模块多个原因同时异常时，优先选择负向标签占比高且负向明细数多的原因。
3. 过程指标异常但缺少明细证据时，可以写入过程原因，但 `evidence_summary` 需说明明细证据不足。
4. 过程原因最多保留 2 个，避免 AI 诊断总览变成问题罗列。

### 4.8 优先动作生成逻辑

优先动作必须和主问题、过程原因、负向证据直接对应，避免输出泛泛建议。

| 主问题/原因 | 优先动作模板 | 必要证据 |
|---|---|---|
| 邀约质量不足 | 回访负向邀约明细，复盘到店理由和时间锁定话术 | `ip_negative_detail_count > 10`、TOP 邀约负向标签 |
| 接待技巧承接不足 | 回看负向试驾接待记录，复盘顾虑承接和竞品攻防话术 | `drive_negative_detail_count > 10`、TOP 接待负向标签 |
| 订单转化不足 | 聚焦试驾后未转订客户，复盘顾虑、报价和版本推荐承接 | 试驾后订单转化、负向试驾接待证据 |

默认输出要求：

- `priority_action` 只写 1 条主动作，便于门店执行。
- 动作中可以包含数量，例如“12 条负向明细复盘”，数量来自对应模块的负向明细数字段。
- 如果样本不足，不生成强动作，改为“补齐样本后重新生成诊断”或“先查看基础指标”。
- 不写任务流写回状态；建立回访任务、加入复盘清单仍是当前 demo 的展示按钮，不进入本期沉淀字段。

### 4.9 样本与状态生成逻辑

样本与状态字段决定前端展示正常诊断、样本不足、无结果还是失败回退。

| 场景 | `diagnosis_status` | `data_status` | 写入规则 |
|---|---|---|---|
| 源数据未刷新 | `失败` | `源数据未就绪` | 不写新的成功诊断；批次状态记录失败环节 |
| Codex 任务失败 | `失败` | `任务失败` | 不覆盖上一成功批次；批次状态写失败原因 |
| 样本不足但任务成功 | `样本不足` | `样本不足` | 写入诊断结果表，保留基础字段和样本不足原因 |
| 无 IP/试驾明细但有基础指标 | `成功` | `明细为空` | 可生成漏斗类诊断，证据摘要说明明细不足 |
| 无任何可用诊断结果 | `失败` | `无诊断结果` | 前端展示无诊断结果或回退上一成功批次 |
| 用户无门店权限 | `失败` | `无权限` | 前端不展示无权限门店数据 |
| 全链路成功 | `成功` | `正常` | 写入最新成功诊断，并更新 `is_latest_success` |

默认样本口径：

- `ip_sample_count` 取本店本月 IP 明细中成功打标的事件级去重数。
- `drive_sample_count` 取本店本月试驾明细中成功打标的事件级去重数。
- `sample_count = ip_sample_count + drive_sample_count`。
- `ip_min_sample_threshold` 首版默认 20。
- `drive_min_sample_threshold` 首版默认 10。
- 样本不足的模块不评价该模块负向问题，只提示样本不足。
- 两个模块都样本不足时，`diagnosis_status = 样本不足`。

## 5. 官方排名/分位结果表

### 5.1 粒度与主键

| 项目 | 口径 |
|---|---|
| 表粒度 | 一店一周期一指标一排名范围一批次一行 |
| 逻辑主键 | `dealer_code + period_type + stat_period + metric_code + rank_scope_type + rank_scope_code + diagnosis_batch_id` |
| 前端读取 | 根据当前门店、月份、指标编码读取官方排名或官方分位 |
| 重要边界 | 本表只存跨组织预计算结果，不重复沉淀基础指标值 |

### 5.2 字段清单

| 中文字段 | 建议字段名 | 类型 | 必填 | 中文含义 | 字段口径 |
|---|---|---|---:|---|---|
| 排名结果ID | rank_result_id | string | 是 | 一条官方排名/分位结果的唯一标识 | 排名结果唯一标识，由逻辑主键生成 |
| 诊断批次ID | diagnosis_batch_id | string | 是 | 生成本排名结果的 Codex 批次 | 与诊断结果主表关联 |
| 统计周期类型 | period_type | enum | 是 | 排名结果对应的时间粒度 | `日`、`月` |
| 统计日期 | stat_date | date | 否 | 日粒度排名对应日期 | 日趋势或日排名使用，月度排名为空 |
| 统计月份 | stat_month | string | 是 | 月粒度排名对应月份 | 月度排名归属月份，格式 `YYYY-MM` |
| 大区编码 | region_code | string | 是 | 门店所属大区编码 | 来自组织主数据 |
| 大区名称 | region_name | string | 是 | 门店所属大区名称 | 前端展示和筛选使用 |
| 小区编码 | district_code | string | 是 | 门店所属小区编码 | 来自组织主数据 |
| 小区名称 | district_name | string | 是 | 门店所属小区名称 | 前端展示和筛选使用 |
| 经销商代码 | dealer_code | string | 是 | 门店唯一编码 | 行权限关键字段 |
| 经销商名称 | dealer_name | string | 是 | 门店展示名称 | 前端展示使用 |
| 指标域 | metric_domain | enum | 是 | 指标所属业务模块 | `销售漏斗`、`邀约过程`、`试驾过程` |
| 指标组 | metric_group | string | 否 | 指标在模块内的细分分组 | 例如线索质量、邀约质量、接待技巧 |
| 指标编码 | metric_code | string | 是 | 稳定指标编码 | 需与前端指标配置一致 |
| 指标名称 | metric_name | string | 是 | 指标展示名称 | 例如订单、线索到店率、负向邀约占比 |
| 排名范围类型 | rank_scope_type | enum | 是 | 官方对标范围类别 | `小区`、`大区`、`全国`、`自定义对标组` |
| 排名范围编码 | rank_scope_code | string | 是 | 官方对标范围编码 | 小区、大区、全国或自定义对标组编码 |
| 排名范围名称 | rank_scope_name | string | 是 | 官方对标范围展示名称 | 小区、大区、全国或自定义对标组名称 |
| 官方排名 | official_rank | integer | 否 | 当前门店在官方范围内的名次 | 当前门店在指定范围内的官方排名 |
| 排名总数 | rank_total | integer | 是 | 当前排名范围内参与排名的门店总数 | 参与排名的有效门店数 |
| 官方分位 | official_percentile | decimal | 否 | 当前门店在官方范围内的表现分位 | 统一存表现分位，建议取 0-100，越高代表表现越好 |
| 排名方向 | rank_direction | enum | 是 | 指标排名时好坏方向 | `越高越好`、`越低越好` |
| 并列处理规则 | tie_break_rule | string | 是 | 同值门店的排名处理方式 | 默认同值同排名，下一名跳号；如采用其他规则需注明 |
| 异常门店处理 | excluded_store_rule | string | 是 | 异常门店是否纳入排名的规则 | 样本不足、停业、无权限、无数据门店是否剔除 |
| 排名生成时间 | rank_generated_at | datetime | 是 | 官方排名/分位完成预计算的时间 | 排名/分位预计算完成时间 |
| 数据完整性状态 | data_quality_status | enum | 是 | 本条排名结果的数据质量状态 | `正常`、`缺失`、`部分缺失`、`样本不足` |

### 5.3 字段口径说明

- 本表解决行权限下的跨组织比较问题。前端只能读取本表结果，不得基于当前可见门店计算官方排名。
- `官方排名` 用于“第几名”表达；`官方分位` 用于“大区分位、小区分位”等表达。
- `官方分位` 统一存表现分位，不存原始数值分位；分位越高代表表现越好。
- 基础指标值、分子、分母、环比不进入本表，由前端从观远销售指标、过程指标、打标明细数据集读取后计算。
- 如果某指标只做分位不做排名，`official_rank` 可为空，但 `official_percentile` 必须有值。
- 如果某指标只做排名不做分位，`official_percentile` 可为空，但 `official_rank` 和 `rank_total` 必须有值。

表现分位解释：

| 表现分位 | 含义 | 诊断用途 |
|---:|---|---|
| `>= 70` | 优势 | 一般不触发主问题 |
| `30 - 70` | 正常波动 | 结合环比和绝对量判断 |
| `< 30` | 明显落后 | 可触发结果断点或过程原因 |
| `< 20` | 严重落后 | 可作为过程类主问题的必要条件之一 |

### 5.4 首版沉淀指标范围

首版只沉淀当前页面会展示或会影响诊断判断的排名/分位，不扩展到页面外指标。

| 指标域 | 指标编码 | 指标名称 | 是否沉淀排名 | 是否沉淀分位 | 排名方向 |
|---|---|---|---:|---:|---|
| 销售漏斗 | `assigned_leads` | 下发线索 | 是 | 是 | 越高越好 |
| 销售漏斗 | `arrivals` | 到店 | 是 | 是 | 越高越好 |
| 销售漏斗 | `test_drives` | 试驾 | 是 | 是 | 越高越好 |
| 销售漏斗 | `orders` | 订单 | 是 | 是 | 越高越好 |
| 销售漏斗 | `lead_to_arrival_rate` | 线索到店率 | 否 | 是 | 越高越好 |
| 销售漏斗 | `arrival_to_drive_rate` | 到店试驾率 | 否 | 是 | 越高越好 |
| 销售漏斗 | `drive_to_order_rate` | 试驾订单率 | 否 | 是 | 越高越好 |
| 销售漏斗 | `lead_to_order_rate` | 线索订单率 | 否 | 是 | 越高越好 |
| 邀约过程 | `ip_connect_rate` | IP 电话接通率 | 否 | 是 | 越高越好 |
| 邀约过程 | `short_call_under_30s_rate` | 30s 以下线索占比 | 否 | 是 | 越低越好 |
| 邀约过程 | `outbound_within_30min_rate` | 30 分钟外呼率 | 否 | 是 | 越高越好 |
| 邀约过程 | `two_day_three_call_rate` | 2 天 3 呼率 | 否 | 是 | 越高越好 |
| 邀约过程 | `negative_invitation_rate` | 负向邀约占比 | 否 | 是 | 越低越好 |
| 试驾过程 | `avg_test_drive_mileage_km` | 试驾平均里程 | 否 | 是 | 越高越好 |
| 试驾过程 | `avg_test_drive_duration_min` | 平均时长 | 否 | 是 | 越高越好 |
| 试驾过程 | `negative_reception_rate` | 负向试驾接待占比 | 否 | 是 | 越低越好 |

说明：

- 绝对量指标保留 `official_rank`，用于复刻当前漏斗卡中的“排名：2/8”这类展示。
- 转化率、占比、均值类指标首版主要沉淀分位，用于诊断判断和后续展示“大区低分位、小区低分位”。
- 如果后续页面明确要展示某个过程指标的名次，再为该指标补 `official_rank`。

### 5.5 排名范围与行数生成规则

每个需要排名/分位的指标，按排名范围拆多行，不把多个范围塞进一个字段。

| 排名范围类型 | 排名范围编码 | 排名范围名称 | 生成规则 |
|---|---|---|---|
| 小区 | 小区编码 | 小区名称 | 当前门店所在小区内全部有效门店参与比较 |
| 大区 | 大区编码 | 大区名称 | 当前门店所在大区内全部有效门店参与比较 |
| 全国 | `ALL` | 全国 | 全部有效门店参与比较 |
| 自定义对标组 | 对标组编码 | 对标组名称 | 由业务预先定义的同类门店或专项门店范围 |

默认生成规则：

1. 首版至少生成 `小区` 和 `大区` 两类范围；`全国`、`自定义对标组` 可在数据范围具备后生成。
2. 同一门店、同一月份、同一指标、同一范围只保留一条当前批次结果。
3. 参与排名的门店必须和该门店有同口径指标值；无数据门店不计入 `rank_total`。
4. `rank_total` 是实际参与排名的有效门店数，不是组织架构下全部门店数。
5. 如果有效门店数不足 3 家，仍可写入分位和排名，但 `data_quality_status` 记为 `样本不足`。

### 5.6 官方排名计算规则

排名用于“第几名 / 共几家”的展示，分位用于“处于什么位置”的判断。两者可同时存在，也可以只存其中一种。

| 字段 | 计算规则 |
|---|---|
| `official_rank` | 按 `rank_direction` 对指标值排序后得到名次 |
| `rank_total` | 当前排名范围内参与计算的有效门店数 |
| `official_percentile` | 当前门店在排名范围内的表现分位，取 0-100，越高代表表现越好 |
| `tie_break_rule` | 默认同值同排名，下一名跳号 |
| `excluded_store_rule` | 默认剔除停业、无数据、样本不足且不可比门店 |

默认方向：

- `越高越好`：数值越大排名越靠前，表现分位越高。
- `越低越好`：数值越小排名越靠前，表现分位越高。
- 对于负向占比类指标，必须使用 `越低越好`，避免把高负向占比误判为好。

### 5.7 前端读取与展示规则

| 页面场景 | 读取规则 | 展示规则 |
|---|---|---|
| 漏斗绝对量卡片排名 | 读取对应指标、小区范围的 `official_rank` 和 `rank_total` | 展示为 `排名：第 X / 共 Y` 或按当前样式压缩为 `X/Y` |
| AI 诊断判断低分位 | 读取对应指标的大区或小区 `official_percentile` | 只参与诊断或解释，不由前端重新计算 |
| 大区分位展示 | 读取 `rank_scope_type = 大区` 的 `official_percentile` | 展示中文分位说明 |
| 小区排名展示 | 读取 `rank_scope_type = 小区` 的 `official_rank`、`rank_total` | 展示官方排名 |
| 排名缺失 | `data_quality_status` 为 `缺失` 或无结果 | 前端不展示排名，保留基础指标 |

硬规则：

- 凡是展示“排名”“分位”“超过/落后某组织”的内容，都必须来自本表。
- 前端可以筛选和格式化排名结果，但不得基于当前可见门店重新计算官方排名。
- 诊断结果表可以引用排名/分位参与诊断，但不重复存储排名明细。

## 6. Codex 批次状态表

### 6.1 粒度与主键

| 项目 | 口径 |
|---|---|
| 表粒度 | 一批次一任务环节一行；如需门店级失败定位，可增加经销商代码 |
| 逻辑主键 | `diagnosis_batch_id + step_code + dealer_code` |
| 前端读取 | 判断当前门店月份是否有最新成功结果、是否需要展示失败或回退状态 |

### 6.2 字段清单

| 中文字段 | 建议字段名 | 类型 | 必填 | 中文含义 | 字段口径 |
|---|---|---|---:|---|---|
| 批次状态ID | batch_status_id | string | 是 | 一条批次环节状态的唯一标识 | 批次状态唯一标识 |
| 诊断批次ID | diagnosis_batch_id | string | 是 | Codex 本次全链路运行批次 | 全链路唯一批次 ID |
| 运行日期 | run_date | date | 是 | Codex 任务实际执行日期 | Codex 任务实际运行日期 |
| 统计月份 | stat_month | string | 是 | 本次任务处理的数据月份 | 本次任务处理的统计月份，格式 `YYYY-MM` |
| 触发方式 | trigger_type | enum | 是 | 本批次任务的触发来源 | `定时触发`、`手动触发`、`补跑` |
| 任务范围 | task_scope | string | 是 | 本批次覆盖的组织或门店范围 | 全量、指定大区、指定小区或指定门店 |
| 经销商代码 | dealer_code | string | 否 | 门店级状态对应的门店编码 | 门店级状态填门店编码；批次总览行可为空 |
| 任务环节编码 | step_code | enum | 是 | 当前任务环节的稳定编码 | `拉取数据`、`生成诊断`、`写入表单`、`刷新表单数据集` |
| 任务环节名称 | step_name | string | 是 | 当前任务环节的中文名称 | 任务环节中文名称 |
| 环节状态 | step_status | enum | 是 | 当前任务环节执行状态 | `待执行`、`运行中`、`成功`、`失败`、`跳过` |
| 源数据刷新状态 | source_refresh_status | enum | 否 | 观远源数据集是否已完成刷新 | `已就绪`、`未就绪`、`部分就绪`、`未知` |
| 源数据刷新时间 | source_refreshed_at | datetime | 否 | 观远源数据集最近一次刷新完成时间 | 观远源数据集最近刷新完成时间 |
| 输入行数 | input_row_count | integer | 否 | 当前环节读取到的输入记录数 | 当前环节输入记录数 |
| 输出行数 | output_row_count | integer | 否 | 当前环节生成的输出记录数 | 当前环节输出记录数 |
| 成功门店数 | success_store_count | integer | 否 | 当前批次或环节成功处理的门店数 | 当前批次或环节成功门店数 |
| 失败门店数 | failed_store_count | integer | 否 | 当前批次或环节失败处理的门店数 | 当前批次或环节失败门店数 |
| 目标表单名称 | target_form_name | string | 否 | 写入环节对应的观远表单名称 | 写表环节对应观远表单名称 |
| 写入状态 | write_status | enum | 否 | 表单写入动作的结果状态 | `新增`、`更新`、`插入或更新`、`失败` |
| 表单数据集刷新状态 | form_dataset_refresh_status | enum | 否 | 观远表单数据集是否已刷新到可读状态 | `已就绪`、`未就绪`、`失败`、`未知` |
| 表单数据集刷新时间 | form_dataset_refreshed_at | datetime | 否 | 前端可读取表单数据集的刷新完成时间 | 表单数据集可被前端读取的刷新时间 |
| 上一成功批次ID | last_success_batch_id | string | 否 | 当前失败时前端可回退读取的上一成功批次 | 当前批次失败时可回退展示的上一成功批次 |
| 错误编码 | error_code | string | 否 | 失败场景下的标准错误码 | 标准错误码 |
| 错误信息 | error_message | text | 否 | 失败场景下的原始错误摘要 | 保留原始错误摘要，便于排障 |
| 重试次数 | retry_count | integer | 是 | 当前批次或环节已经重试的次数 | 当前批次或环节已重试次数 |
| 开始时间 | started_at | datetime | 是 | 当前环节开始执行时间 | 环节开始时间 |
| 结束时间 | ended_at | datetime | 否 | 当前环节结束执行时间 | 环节结束时间 |
| 脚本版本 | script_version | string | 是 | 本地诊断脚本版本号 | 本地诊断脚本版本 |
| 诊断模型版本 | diagnosis_model_version | string | 否 | 本批次生成诊断时使用的模型版本 | 本批次诊断模型版本 |
| 诊断 Prompt 版本 | diagnosis_prompt_version | string | 否 | 本批次生成诊断时使用的 Prompt 版本 | 本批次诊断 Prompt 版本 |

### 6.3 字段口径说明

- 失败批次不覆盖上一成功诊断结果。
- `last_success_batch_id` 用于前端展示“当前为上一成功批次结果”。
- `source_refresh_status` 用于区分源数据未刷新和 Codex 任务失败。
- 当前范围不包含前端点击“重新诊断”实时生成新批次；按钮在本期应理解为刷新读取最新已沉淀结果。

### 6.4 任务环节状态逻辑

批次状态表按任务环节记录执行结果。首版只记录与本期 MVP 闭环直接相关的 4 个环节。

| 任务环节编码 | 任务环节名称 | 成功条件 | 失败处理 |
|---|---|---|---|
| 拉取数据 | 拉取数据 | 销售指标、过程指标、IP 打标明细、试驾打标明细均可读取 | 标记 `失败`，写入错误信息，不生成新诊断 |
| 生成诊断 | 生成诊断 | 完成门店诊断结果和官方排名/分位结果生成 | 门店级失败写入失败门店数；不覆盖该门店上一成功结果 |
| 写入表单 | 写入表单 | 门店诊断结果表、官方排名/分位结果表、批次状态表写入成功 | 标记 `失败`，保留本地运行日志，前端继续读上一成功批次 |
| 刷新表单数据集 | 刷新表单数据集 | 观远表单数据集刷新完成，Super APP 可读取 | 标记 `失败` 或 `未就绪`，前端提示结果可能不是最新 |

环节状态默认流转：

```text
待执行 -> 运行中 -> 成功
待执行 -> 运行中 -> 失败
待执行 -> 跳过
```

跳过只用于上游失败后的下游环节。例如拉取数据失败后，生成诊断、写入表单、刷新表单数据集均可记录为 `跳过`。

### 6.5 批次成功与门店成功规则

批次状态需要同时支持批次级判断和门店级判断。

| 判断对象 | 成功规则 | 失败规则 |
|---|---|---|
| 批次级成功 | 全部必要环节完成，且至少有 1 家门店生成成功诊断 | 拉取数据失败、写表失败、表单数据集刷新失败 |
| 门店级成功 | 单店诊断结果写入成功，且 `diagnosis_status` 为 `成功` 或 `样本不足` | 单店源数据缺失、样本不可用、诊断生成失败、写入失败 |
| 前端可读成功 | 当前门店月份存在最新成功诊断结果，且表单数据集已刷新 | 无最新成功结果，或最新结果不可读 |

默认规则：

1. 批次级失败不代表所有门店失败，必须通过门店级状态判断前端展示。
2. 门店级 `样本不足` 视为任务成功但诊断质量不足，前端展示样本不足提示。
3. 只有 `写入表单` 和 `刷新表单数据集` 均成功后，前端才默认读取新批次。
4. 如果刷新表单数据集失败，已写入表单的数据不视为前端可读成功。

### 6.6 失败回退逻辑

失败回退用于保证工作台不因当天任务失败而展示空白或误导结果。

| 场景 | `last_success_batch_id` | 前端展示 |
|---|---|---|
| 当前批次成功 | 填当前批次或为空 | 展示当前最新结果 |
| 当前批次失败，存在上一成功批次 | 填上一成功批次 | 展示上一成功结果，并提示结果日期 |
| 当前批次失败，且无上一成功批次 | 为空 | 展示无诊断结果或任务失败 |
| 当前批次样本不足 | 可填上一成功批次 | 优先展示样本不足结果；必要时提示可参考上一成功批次 |
| 源数据未就绪 | 填上一成功批次 | 展示数据准备中；可展示上一成功结果但必须提示非最新 |

回退约束：

- 回退只能回退到同一门店、同一统计月份的上一成功批次。
- 不允许跨月份回退展示，避免把上月诊断误当成本月结论。
- 前端展示上一成功批次时，必须能显示生成时间或结果日期。

### 6.7 表单写入策略

| 表单 | 当前批次成功时 | 当前批次失败时 | 覆盖规则 |
|---|---|---|---|
| 门店诊断结果表 | 写入成功门店结果 | 不写失败门店结果；样本不足可写 | 同门店同月份仅一个 `is_latest_success = true` |
| 官方排名/分位结果表 | 写入本批次预计算结果 | 不写失败范围结果 | 同批次同门店同指标同范围只保留一行 |
| Codex 批次状态表 | 写入全部环节状态 | 必须写入失败环节、错误信息和回退批次 | 保留历史批次，不物理覆盖 |

写入状态取值：

- `新增`：首次写入某个逻辑主键。
- `更新`：更新同一逻辑主键的状态或补充字段。
- `插入或更新`：脚本采用 upsert 方式执行，观远侧不区分新增和更新。
- `失败`：写入失败，必须记录 `error_code` 和 `error_message`。

### 6.8 前端读取批次状态规则

Super APP 前端读取页面数据时，按以下顺序判断：

1. 读取当前门店、当前月份的最新批次状态。
2. 如果表单数据集刷新状态为 `已就绪`，读取 `is_latest_success = true` 的门店诊断结果。
3. 如果最新批次失败但存在 `last_success_batch_id`，读取上一成功批次并提示结果日期。
4. 如果源数据刷新状态为 `未就绪` 或 `部分就绪`，展示数据准备中。
5. 如果诊断状态为 `样本不足`，展示样本不足提示，同时保留基础指标展示。
6. 如果没有可用诊断结果，展示无诊断结果。

前端不得通过点击“重新诊断”生成新批次；本期按钮只能触发刷新读取最新已沉淀结果。

## 7. 非 Codex 表单输入数据

### 7.1 IP 打标明细数据集

IP 打标明细由数仓加工，Codex 和前端均只读取，不重复落表。字段能力至少需要覆盖：

| 字段能力 | 用途 |
|---|---|
| 经销商、月份、客户、顾问、线索、通话事件 | 支撑门店筛选、顾问分布和客户明细 |
| 是否接通、通话时长、首次外呼延迟、2 天外呼次数 | 支撑 IP 电话接通率、30s 以下线索占比、30 分钟外呼率、2 天 3 呼率 |
| 一级标签、二级标签、标签正负向 | 支撑负向邀约占比、标签分布和下钻筛选；负向判断统一使用 `标签正负向 = 负向` |
| 问题摘要、证据摘录、标签命中原因、建议话术 | 支撑客户证据抽屉 |
| 标签置信度、打标状态、模型版本、Prompt 版本 | 支撑质量复核和批次追溯 |

### 7.2 试驾打标明细数据集

试驾打标明细由数仓加工，Codex 和前端均只读取，不重复落表。字段能力至少需要覆盖：

| 字段能力 | 用途 |
|---|---|
| 经销商、月份、客户、顾问、试驾事件 | 支撑门店筛选、顾问分布和客户明细 |
| 试驾时间、试驾里程、试驾时长、试驾车系/车型 | 支撑试驾平均里程、平均时长和试驾明细 |
| 一级标签、二级标签、标签正负向 | 支撑负向试驾接待占比、标签分布和下钻筛选；负向判断统一使用 `标签正负向 = 负向` |
| 问题摘要、证据摘录、标签命中原因、建议话术 | 支撑客户证据抽屉 |
| 标签置信度、打标状态、模型版本、Prompt 版本 | 支撑质量复核和批次追溯 |

## 8. 前端计算边界

| 页面展示项 | 前端计算输入 | 前端是否可算 | 说明 |
|---|---|---:|---|
| 下发线索、到店、试驾、订单 | 门店销售指标数据集 | 是 | 当前门店、当前月份累计 |
| 线索到店率、到店试驾率、试驾订单率、线索订单率 | 销售指标分子分母 | 是 | 仅代表当前权限范围 |
| IP 电话接通率、30s 以下线索占比、30 分钟外呼率、2 天 3 呼率 | 门店过程指标或 IP 明细 | 是 | 指标分子分母需在数据口径文档锁定 |
| 试驾平均里程、平均时长 | 试驾过程指标或试驾明细 | 是 | 异常值剔除规则需在数据口径文档锁定 |
| 负向邀约占比、负向试驾接待占比 | 数仓打标明细中的负向标记 | 是 | 负向判定由数仓 AI 标签提供，前端只汇总 |
| 月环比、趋势方向 | 本期值、上期同口径值 | 是 | 上期周期口径需在数据口径文档锁定 |
| 标签分布、顾问分布 | 数仓打标明细 | 是 | 仅影响第三层明细，不反算官方诊断 |
| 明细筛选、客户抽屉 | 数仓打标明细 | 是 | 前端只筛选和展示，不改写标签和证据 |
| 小区排名、大区分位、跨组织对标 | 官方排名/分位结果表 | 否 | 必须读取预计算结果 |
| 主问题、断点、原因、优先动作 | 门店诊断结果表 | 否 | 必须读取 Codex 沉淀结果 |

## 9. 后续需进入数据口径文档的事项

以下内容不在本清单内最终锁死，需要进入独立数据口径文档逐项定义：

| 事项 | 需要定义的口径 |
|---|---|
| 销售漏斗指标 | 下发线索、到店、试驾、订单的来源、去重、过滤、归属周期 |
| 转化率 | 每个转化率的分子、分母、样本排除和除零处理 |
| 环比 | 自然月、MTD 同步天数或完整上月的选择 |
| IP 过程指标 | 接通、30s 以下、30 分钟外呼、2 天 3 呼的起算点和有效样本 |
| 试驾过程指标 | 试驾里程、试驾时长的来源、异常值和缺失处理 |
| 负向占比 | 负向标签口径、多标签去重和事件级/标签级汇总规则 |
| 官方排名/分位 | 排名范围、并列规则、剔除规则、越高越好/越低越好 |
| 样本不足 | 不同模块的最小样本量、提示文案和是否允许生成诊断 |
