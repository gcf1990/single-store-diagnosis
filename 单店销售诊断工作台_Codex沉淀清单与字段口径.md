# 单店销售诊断工作台 Codex 沉淀清单与字段口径

## 1. 文档目的

本文档用于明确单店销售诊断工作台中，哪些数据由 Codex 每日诊断任务沉淀到观远表单，哪些数据由数仓数据集提供，哪些展示值由 Super APP 前端在当前权限范围内计算。

核心原则：

```text
前端算展示公式，不算官方诊断；
前端算当前权限范围，不算跨组织官方排名。
```

由于观远存在行权限控制，涉及小区排名、大区分位、跨组织对标的字段，必须由数据层或 Codex 每日任务提前预计算并落表，前端不得基于当前可见门店临时计算官方排名或官方分位。

## 2. 数据供给边界

| 数据对象 | 供给方 | 是否 Codex 表单落表 | 前端使用方式 |
|---|---|---:|---|
| 门店诊断结果 | Codex 每日任务 | 是 | 读取官方诊断结论、结果断点、过程原因、优先动作 |
| 官方排名/分位结果 | 数据层预计算或 Codex 每日任务 | 是 | 读取小区排名、大区分位、跨组织对标结果 |
| Codex 批次状态 | Codex 每日任务 | 是 | 判断最新成功批次、失败回退、源数据状态 |
| 门店销售指标数据集 | 观远/数仓 | 否 | 前端读取后计算累计、转化率、环比 |
| 门店过程指标数据集 | 观远/数仓 | 否 | 前端读取后计算过程指标和日趋势 |
| IP 打标明细数据集 | 数仓 | 否 | 前端读取标签、证据、命中原因、顾问分布、明细抽屉 |
| 试驾打标明细数据集 | 数仓 | 否 | 前端读取标签、证据、命中原因、顾问分布、明细抽屉 |
| 页面结构与指标分组 | 前端静态配置 | 否 | 控制 Tab、指标组、默认聚焦和展示顺序 |

## 3. Codex 输出表单

Codex 每日任务只沉淀 3 类表单：

| 表单 | 粒度 | 用途 |
|---|---|---|
| 门店诊断结果表 | 经销商 + 统计月份 + 诊断批次 | 存官方诊断事实 |
| 官方排名/分位结果表 | 经销商 + 统计周期 + 指标 + 排名范围 + 诊断批次 | 存跨组织预计算结果 |
| Codex 批次状态表 | 诊断批次 + 任务环节 + 可选经销商 | 存任务运行、失败、补跑和读取状态 |

枚举值规则：

- 字段名保留英文，便于脚本、接口和前端稳定引用。
- 状态、类型、范围、方向等枚举取值统一存中文，便于观远直接统计和前端直接展示。
- 如后续需要英文编码，可另增编码字段，不替换当前中文枚举展示字段。

## 4. 门店诊断结果表

### 4.1 粒度与主键

| 项目 | 口径 |
|---|---|
| 表粒度 | 一店一月一批次一行 |
| 逻辑主键 | `dealer_code + stat_month + diagnosis_batch_id` |
| 前端读取 | 默认读取当前经销商、当前月份的最新成功批次 |
| 覆盖策略 | 新批次成功后标记为最新成功批次；失败批次不覆盖上一成功结果 |

### 4.2 字段清单

| 中文字段 | 建议字段名 | 类型 | 必填 | 字段口径 |
|---|---|---|---:|---|
| 诊断结果ID | diagnosis_result_id | string | 是 | 诊断结果唯一标识，建议由 `dealer_code + stat_month + diagnosis_batch_id` 生成 |
| 诊断批次ID | diagnosis_batch_id | string | 是 | Codex 每次运行生成的全链路批次 ID |
| 统计月份 | stat_month | string | 是 | 诊断归属月份，格式 `YYYY-MM` |
| 大区编码 | region_code | string | 是 | 门店所属大区编码，来自组织主数据 |
| 大区名称 | region_name | string | 是 | 门店所属大区名称，前端展示使用 |
| 小区编码 | district_code | string | 是 | 门店所属小区编码，来自组织主数据 |
| 小区名称 | district_name | string | 是 | 门店所属小区名称，前端展示使用 |
| 经销商代码 | dealer_code | string | 是 | 门店唯一编码，也是行权限关键字段 |
| 经销商名称 | dealer_name | string | 是 | 门店展示名称 |
| 诊断状态 | diagnosis_status | enum | 是 | `成功`、`失败`、`样本不足` |
| 数据状态 | data_status | enum | 是 | `正常`、`无诊断结果`、`任务失败`、`源数据未就绪`、`样本不足`、`明细为空`、`无权限` |
| 总体诊断结论 | overall_conclusion | text | 是 | AI 诊断总览标题文案，前端只读展示 |
| 主问题编码 | main_issue_code | string | 是 | 官方主问题编码，后续用于统计和治理 |
| 主问题名称 | main_issue_name | string | 是 | 官方主问题名称，例如邀约质量不足、接待技巧不足 |
| 结果断点 | result_breakpoint | text | 是 | 说明销售链路主要断点，例如客户到店不足、线索订单率偏弱 |
| 过程原因 | process_reason | text | 是 | 说明过程短板，例如邀约质量不足、接待技巧承接不足 |
| 优先动作 | priority_action | text | 是 | 本批次建议优先动作，例如回访高风险明细、复盘话术 |
| 门店风险等级 | store_risk_level | enum | 是 | `高`、`中`、`低`，由 Codex 诊断逻辑生成 |
| 聚焦数据域 | focus_domain | enum | 是 | `销售漏斗`、`邀约过程`、`试驾过程`，用于前端默认聚焦模块 |
| 聚焦指标编码 | focus_metric_code | string | 是 | 本次诊断重点指标编码，例如 `negative_invitation_rate` |
| 聚焦指标名称 | focus_metric_name | string | 是 | 本次诊断重点指标名称，例如负向邀约占比 |
| 高风险明细数 | high_risk_detail_count | integer | 是 | Codex 基于数仓 IP/试驾打标明细统计的高风险记录数 |
| 高亮指标编码 | highlight_metric_codes | string | 否 | AI 诊断总览中需要高亮展示的指标编码列表，用英文逗号分隔 |
| 证据摘要 | evidence_summary | text | 否 | 对高风险样本、核心标签和典型证据的摘要 |
| AI 解释文案 | ai_explanation | text | 否 | 更完整的解释文案；不得改写主问题、断点、原因和优先动作 |
| 样本量 | sample_count | integer | 是 | 本次诊断纳入的有效样本数，可按 IP 与试驾合并口径输出 |
| 最小样本阈值 | min_sample_threshold | integer | 是 | 生成可靠诊断所需最小样本量 |
| 样本不足原因 | low_sample_reason | text | 否 | 当 `diagnosis_status = 样本不足` 时填写 |
| 诊断模型版本 | diagnosis_model_version | string | 是 | 生成门店诊断结果使用的模型版本 |
| 诊断 Prompt 版本 | diagnosis_prompt_version | string | 是 | 生成门店诊断结果使用的 Prompt 版本 |
| 生成时间 | generated_at | datetime | 是 | Codex 生成本行诊断结果的时间 |
| 是否最新成功批次 | is_latest_success | boolean | 是 | 当前门店月份是否读取本批次作为最新成功结果 |
| 上一成功批次ID | fallback_batch_id | string | 否 | 当前批次失败时，前端可回退展示的上一成功批次 |

### 4.3 字段口径说明

- `总体诊断结论`、`结果断点`、`过程原因`、`优先动作` 属于官方诊断事实，前端不得临时生成或改写。
- `高亮指标编码` 只告诉前端哪些指标需要在 AI 诊断总览中高亮；指标数值仍由前端从观远指标数据集中读取并计算。
- `高风险明细数` 来自数仓已加工 IP/试驾打标明细，Codex 只做门店月度聚合，不重复落事件级明细。
- `样本不足` 不是页面空态文案，而是诊断链路状态；前端应保留基础指标展示，并提示诊断样本不足。

### 4.4 核心诊断字段生成逻辑

门店诊断结果表的核心字段按“结果断点 -> 过程原因 -> 证据支撑 -> 优先动作”的顺序生成。Codex 可以读取基础指标和数仓打标明细参与判断，但只沉淀官方诊断事实，不沉淀基础指标值。

| 字段 | 生成逻辑 | 主要输入 | 输出要求 |
|---|---|---|---|
| `diagnosis_status` | 根据源数据、样本量、诊断任务是否成功确定 | 批次状态、销售指标、过程指标、IP/试驾打标明细 | 只允许 `成功`、`失败`、`样本不足` |
| `data_status` | 面向前端展示的数据状态 | 批次状态、源数据刷新状态、诊断结果、明细数据 | 只允许 `正常`、`无诊断结果`、`任务失败`、`源数据未就绪`、`样本不足`、`明细为空`、`无权限` |
| `main_issue_code` / `main_issue_name` | 从固定主问题字典中选择一个主问题 | 销售漏斗断点、过程指标异常、负向标签分布、官方排名/分位 | 必须使用稳定字典，不自由生成新分类 |
| `result_breakpoint` | 识别销售链路最大的结果断点 | 下发线索、到店、试驾、订单、转化率、环比、排名/分位 | 用业务语言说明结果问题，例如客户到店不足、线索订单率偏弱 |
| `process_reason` | 解释造成结果断点的主要过程原因 | IP 过程指标、试驾过程指标、负向标签、顾问风险、证据摘要 | 最多保留 1-2 个主因，避免堆叠过多原因 |
| `priority_action` | 基于主问题和高风险证据生成优先动作 | 高风险明细数、TOP 标签、TOP 顾问、证据摘要 | 必须可落到门店复盘动作，不写泛泛建议 |
| `focus_domain` | 决定前端默认聚焦模块 | 主问题、结果断点、过程原因 | 只允许 `销售漏斗`、`邀约过程`、`试驾过程` |
| `focus_metric_code` / `focus_metric_name` | 决定前端默认聚焦指标 | 异常程度最高或最能解释主问题的指标 | 必须能匹配前端指标配置 |
| `store_risk_level` | 判断门店整体风险等级 | 结果断点严重度、官方排名/分位、高风险明细数、环比恶化 | 只允许 `高`、`中`、`低` |
| `high_risk_detail_count` | 统计本店本月高风险 IP/试驾明细数 | 数仓 IP/试驾打标明细 | 按当前诊断月份和经销商汇总，不重复落明细 |
| `highlight_metric_codes` | 标记 AI 诊断总览需要高亮的指标 | 主问题相关指标、异常指标、重点过程指标 | 多个指标用英文逗号分隔；只做高亮，不带数值 |
| `evidence_summary` | 汇总支撑诊断的证据摘要 | TOP 标签、典型证据、顾问风险、高风险明细 | 只写摘要，不替代明细数据集 |
| `ai_explanation` | 生成更完整的解释文案 | 已锁定的主问题、断点、原因、动作和证据摘要 | 不得改写已锁定字段，只能解释增强 |

### 4.5 主问题字典

主问题必须从固定字典中选择。当前首版建议只覆盖页面已有业务链路，不扩展到页面外问题。

| 主问题编码 | 主问题名称 | 默认聚焦数据域 | 常见结果断点 | 常见过程原因 |
|---|---|---|---|---|
| `lead_volume_weak` | 线索供给不足 | 销售漏斗 | 下发线索不足，订单承压 | 线索规模偏低，线索供给不足 |
| `arrival_conversion_weak` | 到店转化不足 | 邀约过程 | 客户到店不足，线索订单率偏弱 | 邀约质量不足，到店理由构建不足 |
| `test_drive_conversion_weak` | 试驾转化不足 | 试驾过程 | 到店后试驾承接不足 | 试驾强度不足，体验引导不足 |
| `order_conversion_weak` | 订单转化不足 | 试驾过程 | 试驾后订单转化不足 | 顾虑承接不足，竞品攻防不足 |
| `invitation_quality_weak` | 邀约质量不足 | 邀约过程 | 到店率偏弱或持续下滑 | 负向邀约占比高，短通话占比高 |
| `reception_skill_weak` | 接待技巧不足 | 试驾过程 | 试驾订单率偏弱或持续下滑 | 负向试驾接待占比高，顾虑承接不足 |
| `execution_diligence_weak` | 跟进勤奋度不足 | 邀约过程 | 到店转化偏弱 | 30 分钟外呼率低，2 天 3 呼率低 |

主问题选择规则：

1. 先判断销售漏斗结果断点，再判断过程原因，避免只因某个过程指标异常就直接定主问题。
2. 如果结果断点与过程原因不一致，以结果断点作为主问题优先级，过程原因写入 `process_reason`。
3. 如果多个主问题同时命中，优先选择对订单影响最大、且有高风险明细或官方排名/分位支撑的问题。
4. 如果基础数据足够但 IP/试驾明细为空，可以生成漏斗类诊断，但 `data_status` 应标记为 `明细为空`，证据摘要说明证据不足。

### 4.6 结果断点判断逻辑

结果断点用于回答“这家店结果差在哪里”。Codex 读取销售漏斗基础数据、环比和官方排名/分位后生成文本，但不把基础数值写入诊断结果表。

| 候选断点 | 参考指标 | 判断优先级 | 输出示例 |
|---|---|---:|---|
| 线索供给不足 | 下发线索、下发线索排名/分位、下发线索环比 | 1 | 下发线索不足，后续转化承压 |
| 到店转化不足 | 线索到店率、到店数、到店排名/分位、到店环比 | 2 | 客户到店不足，线索订单率偏弱 |
| 试驾承接不足 | 到店试驾率、试驾数、试驾排名/分位、试驾环比 | 3 | 到店后试驾承接不足 |
| 订单转化不足 | 试驾订单率、线索订单率、订单数、订单排名/分位 | 4 | 试驾后订单转化不足，订单结果偏弱 |

默认判断顺序：

1. 先找最终结果最差的链路，例如订单、线索订单率、试驾订单率。
2. 再向上追溯最可能造成该结果的前序断点，例如到店不足或试驾不足。
3. 如果绝对量和转化率同时异常，`result_breakpoint` 同时表达“规模不足 + 转化偏弱”。
4. 如果排名/分位显示跨组织落后，即使当前门店环比改善，也可以保留为结果断点，但解释中要说明“仍处于低分位”。

### 4.7 过程原因判断逻辑

过程原因用于回答“为什么出现这个结果断点”。过程原因必须由过程指标和数仓打标明细共同支撑。

| 过程原因 | 参考指标/明细 | 常见证据 |
|---|---|---|
| 邀约质量不足 | IP 电话接通率、30s 以下线索占比、负向邀约占比 | 到店时间锁定不足、到店理由构建不足、报价到店承接弱 |
| 跟进勤奋度不足 | 30 分钟外呼率、2 天 3 呼率 | 首次外呼不及时、触达频次不足 |
| 试驾强度不足 | 试驾平均里程、平均时长 | 试驾路线短、体验时长不足 |
| 接待技巧承接不足 | 负向试驾接待占比 | 顾虑承接不足、竞品攻防不足、版本推荐不清 |

默认判断规则：

1. 优先选择能解释 `result_breakpoint` 的过程原因。
2. 同一模块多个原因同时异常时，优先选择负向标签占比高且高风险明细多的原因。
3. 过程指标异常但缺少明细证据时，可以写入过程原因，但 `evidence_summary` 需说明明细证据不足。
4. 过程原因最多保留 2 个，避免 AI 诊断总览变成问题罗列。

### 4.8 优先动作生成逻辑

优先动作必须和主问题、过程原因、高风险证据直接对应，避免输出泛泛建议。

| 主问题/原因 | 优先动作模板 | 必要证据 |
|---|---|---|
| 邀约质量不足 | 回访高风险邀约明细，复盘到店理由和时间锁定话术 | 高风险 IP 明细数、TOP 邀约标签 |
| 跟进勤奋度不足 | 排查未及时外呼线索，补齐 30 分钟外呼和 2 天 3 呼跟进 | 首次外呼延迟、2 天外呼次数 |
| 试驾强度不足 | 复盘试驾路线和体验时长，抽查低里程/短时长试驾记录 | 试驾里程、试驾时长异常样本 |
| 接待技巧承接不足 | 回看高风险试驾接待记录，复盘顾虑承接和竞品攻防话术 | 高风险试驾明细数、TOP 接待标签 |
| 订单转化不足 | 聚焦试驾后未转订客户，复盘顾虑、报价和版本推荐承接 | 试驾后订单转化、负向试驾接待证据 |

默认输出要求：

- `priority_action` 只写 1 条主动作，便于门店执行。
- 动作中可以包含数量，例如“12 条高风险明细回访”，数量来自 `high_risk_detail_count`。
- 如果样本不足，不生成强动作，改为“补齐样本后重新生成诊断”或“先查看基础指标”。
- 不写任务流写回状态；建立回访任务、加入复盘清单仍是当前 demo 的展示按钮，不进入本期沉淀字段。

### 4.9 样本与状态生成逻辑

样本与状态字段决定前端展示正常诊断、样本不足、无结果还是失败回退。

| 场景 | `diagnosis_status` | `data_status` | 写入规则 |
|---|---|---|---|
| 源数据未刷新 | `失败` | `源数据未就绪` | 不写新的成功诊断；批次状态记录失败环节 |
| Codex 任务失败 | `失败` | `任务失败` | 不覆盖上一成功批次；批次状态写失败原因 |
| 样本不足但任务成功 | `样本不足` | `样本不足` | 写入诊断结果表，保留基础字段和样本不足原因 |
| 无 IP/试驾明细但有基础指标 | `成功` | `明细为空` | 可生成漏斗类诊断，证据摘要说明明细不足 |
| 无任何可用诊断结果 | `失败` | `无诊断结果` | 前端展示无诊断结果或回退上一成功批次 |
| 用户无门店权限 | `失败` | `无权限` | 前端不展示无权限门店数据 |
| 全链路成功 | `成功` | `正常` | 写入最新成功诊断，并更新 `is_latest_success` |

默认样本口径：

- `sample_count` 首版可取本店本月纳入诊断的 IP 打标明细数 + 试驾打标明细数。
- `min_sample_threshold` 首版写入脚本配置值，后续在数据口径文档中细化到 IP 和试驾模块。
- 如果仅 IP 或仅试驾样本不足，不必整体失败；`process_reason` 和 `evidence_summary` 需说明不足模块。

## 5. 官方排名/分位结果表

### 5.1 粒度与主键

| 项目 | 口径 |
|---|---|
| 表粒度 | 一店一周期一指标一排名范围一批次一行 |
| 逻辑主键 | `dealer_code + period_type + stat_period + metric_code + rank_scope_type + rank_scope_code + diagnosis_batch_id` |
| 前端读取 | 根据当前门店、月份、指标编码读取官方排名或官方分位 |
| 重要边界 | 本表只存跨组织预计算结果，不重复沉淀基础指标值 |

### 5.2 字段清单

| 中文字段 | 建议字段名 | 类型 | 必填 | 字段口径 |
|---|---|---|---:|---|
| 排名结果ID | rank_result_id | string | 是 | 排名结果唯一标识，由逻辑主键生成 |
| 诊断批次ID | diagnosis_batch_id | string | 是 | 与诊断结果主表关联 |
| 统计周期类型 | period_type | enum | 是 | `日`、`月` |
| 统计日期 | stat_date | date | 否 | 日趋势或日排名使用，月度排名为空 |
| 统计月份 | stat_month | string | 是 | 月度排名归属月份，格式 `YYYY-MM` |
| 大区编码 | region_code | string | 是 | 门店所属大区编码 |
| 大区名称 | region_name | string | 是 | 门店所属大区名称 |
| 小区编码 | district_code | string | 是 | 门店所属小区编码 |
| 小区名称 | district_name | string | 是 | 门店所属小区名称 |
| 经销商代码 | dealer_code | string | 是 | 门店唯一编码，也是行权限关键字段 |
| 经销商名称 | dealer_name | string | 是 | 门店展示名称 |
| 指标域 | metric_domain | enum | 是 | `销售漏斗`、`邀约过程`、`试驾过程` |
| 指标组 | metric_group | string | 否 | 例如线索质量、勤奋度、邀约技巧、试驾强度 |
| 指标编码 | metric_code | string | 是 | 稳定指标编码，需与前端指标配置一致 |
| 指标名称 | metric_name | string | 是 | 指标展示名，例如订单、线索到店率、负向邀约占比 |
| 排名范围类型 | rank_scope_type | enum | 是 | `小区`、`大区`、`全国`、`自定义对标组` |
| 排名范围编码 | rank_scope_code | string | 是 | 小区、大区、全国或自定义对标组编码 |
| 排名范围名称 | rank_scope_name | string | 是 | 小区、大区、全国或自定义对标组名称 |
| 官方排名 | official_rank | integer | 否 | 当前门店在指定范围内的官方排名 |
| 排名总数 | rank_total | integer | 是 | 参与排名的有效门店数 |
| 官方分位 | official_percentile | decimal | 否 | 当前门店在指定范围内的官方分位，建议取 0-100 |
| 排名方向 | rank_direction | enum | 是 | `越高越好`、`越低越好` |
| 并列处理规则 | tie_break_rule | string | 是 | 默认同值同排名，下一名跳号；如采用其他规则需注明 |
| 异常门店处理 | excluded_store_rule | string | 是 | 样本不足、停业、无权限、无数据门店是否剔除 |
| 排名生成时间 | rank_generated_at | datetime | 是 | 排名/分位预计算完成时间 |
| 数据完整性状态 | data_quality_status | enum | 是 | `正常`、`缺失`、`部分缺失`、`样本不足` |

### 5.3 字段口径说明

- 本表解决行权限下的跨组织比较问题。前端只能读取本表结果，不得基于当前可见门店计算官方排名。
- `官方排名` 用于“第几名”表达；`官方分位` 用于“大区分位、小区分位”等表达。
- 基础指标值、分子、分母、环比不进入本表，由前端从观远销售指标、过程指标、打标明细数据集读取后计算。
- 如果某指标只做分位不做排名，`official_rank` 可为空，但 `official_percentile` 必须有值。
- 如果某指标只做排名不做分位，`official_percentile` 可为空，但 `official_rank` 和 `rank_total` 必须有值。

### 5.4 首版沉淀指标范围

首版只沉淀当前页面会展示或会影响诊断判断的排名/分位，不扩展到页面外指标。

| 指标域 | 指标编码 | 指标名称 | 是否沉淀排名 | 是否沉淀分位 | 排名方向 |
|---|---|---|---:|---:|---|
| 销售漏斗 | `assigned_leads` | 下发线索 | 是 | 是 | 越高越好 |
| 销售漏斗 | `arrivals` | 到店 | 是 | 是 | 越高越好 |
| 销售漏斗 | `test_drives` | 试驾 | 是 | 是 | 越高越好 |
| 销售漏斗 | `orders` | 订单 | 是 | 是 | 越高越好 |
| 销售漏斗 | `lead_to_arrival_rate` | 线索到店率 | 否 | 是 | 越高越好 |
| 销售漏斗 | `arrival_to_drive_rate` | 到店试驾率 | 否 | 是 | 越高越好 |
| 销售漏斗 | `drive_to_order_rate` | 试驾订单率 | 否 | 是 | 越高越好 |
| 销售漏斗 | `lead_to_order_rate` | 线索订单率 | 否 | 是 | 越高越好 |
| 邀约过程 | `ip_connect_rate` | IP 电话接通率 | 否 | 是 | 越高越好 |
| 邀约过程 | `short_call_under_30s_rate` | 30s 以下线索占比 | 否 | 是 | 越低越好 |
| 邀约过程 | `outbound_within_30min_rate` | 30 分钟外呼率 | 否 | 是 | 越高越好 |
| 邀约过程 | `two_day_three_call_rate` | 2 天 3 呼率 | 否 | 是 | 越高越好 |
| 邀约过程 | `negative_invitation_rate` | 负向邀约占比 | 否 | 是 | 越低越好 |
| 试驾过程 | `avg_test_drive_mileage_km` | 试驾平均里程 | 否 | 是 | 越高越好 |
| 试驾过程 | `avg_test_drive_duration_min` | 平均时长 | 否 | 是 | 越高越好 |
| 试驾过程 | `negative_reception_rate` | 负向试驾接待占比 | 否 | 是 | 越低越好 |

说明：

- 绝对量指标保留 `official_rank`，用于复刻当前漏斗卡中的“排名：2/8”这类展示。
- 转化率、占比、均值类指标首版主要沉淀分位，用于诊断判断和后续展示“大区低分位、小区低分位”。
- 如果后续页面明确要展示某个过程指标的名次，再为该指标补 `official_rank`。

### 5.5 排名范围与行数生成规则

每个需要排名/分位的指标，按排名范围拆多行，不把多个范围塞进一个字段。

| 排名范围类型 | 排名范围编码 | 排名范围名称 | 生成规则 |
|---|---|---|---|
| 小区 | 小区编码 | 小区名称 | 当前门店所在小区内全部有效门店参与比较 |
| 大区 | 大区编码 | 大区名称 | 当前门店所在大区内全部有效门店参与比较 |
| 全国 | `ALL` | 全国 | 全部有效门店参与比较 |
| 自定义对标组 | 对标组编码 | 对标组名称 | 由业务预先定义的同类门店或专项门店范围 |

默认生成规则：

1. 首版至少生成 `小区` 和 `大区` 两类范围；`全国`、`自定义对标组` 可在数据范围具备后生成。
2. 同一门店、同一月份、同一指标、同一范围只保留一条当前批次结果。
3. 参与排名的门店必须和该门店有同口径指标值；无数据门店不计入 `rank_total`。
4. `rank_total` 是实际参与排名的有效门店数，不是组织架构下全部门店数。
5. 如果有效门店数不足 3 家，仍可写入分位和排名，但 `data_quality_status` 记为 `样本不足`。

### 5.6 官方排名计算规则

排名用于“第几名 / 共几家”的展示，分位用于“处于什么位置”的判断。两者可同时存在，也可以只存其中一种。

| 字段 | 计算规则 |
|---|---|
| `official_rank` | 按 `rank_direction` 对指标值排序后得到名次 |
| `rank_total` | 当前排名范围内参与计算的有效门店数 |
| `official_percentile` | 当前门店在排名范围内的位置分位，取 0-100 |
| `tie_break_rule` | 默认同值同排名，下一名跳号 |
| `excluded_store_rule` | 默认剔除停业、无数据、样本不足且不可比门店 |

默认方向：

- `越高越好`：数值越大排名越靠前，分位越高代表越好。
- `越低越好`：数值越小排名越靠前，分位越高代表越好。
- 对于负向占比类指标，必须使用 `越低越好`，避免把高负向占比误判为好。

### 5.7 前端读取与展示规则

| 页面场景 | 读取规则 | 展示规则 |
|---|---|---|
| 漏斗绝对量卡片排名 | 读取对应指标、小区范围的 `official_rank` 和 `rank_total` | 展示为 `排名：第 X / 共 Y` 或按当前样式压缩为 `X/Y` |
| AI 诊断判断低分位 | 读取对应指标的大区或小区 `official_percentile` | 只参与诊断或解释，不由前端重新计算 |
| 大区分位展示 | 读取 `rank_scope_type = 大区` 的 `official_percentile` | 展示中文分位说明 |
| 小区排名展示 | 读取 `rank_scope_type = 小区` 的 `official_rank`、`rank_total` | 展示官方排名 |
| 排名缺失 | `data_quality_status` 为 `缺失` 或无结果 | 前端不展示排名，保留基础指标 |

硬规则：

- 凡是展示“排名”“分位”“超过/落后某组织”的内容，都必须来自本表。
- 前端可以筛选和格式化排名结果，但不得基于当前可见门店重新计算官方排名。
- 诊断结果表可以引用排名/分位参与诊断，但不重复存储排名明细。

## 6. Codex 批次状态表

### 6.1 粒度与主键

| 项目 | 口径 |
|---|---|
| 表粒度 | 一批次一任务环节一行；如需门店级失败定位，可增加经销商代码 |
| 逻辑主键 | `diagnosis_batch_id + step_code + dealer_code` |
| 前端读取 | 判断当前门店月份是否有最新成功结果、是否需要展示失败或回退状态 |

### 6.2 字段清单

| 中文字段 | 建议字段名 | 类型 | 必填 | 字段口径 |
|---|---|---|---:|---|
| 批次状态ID | batch_status_id | string | 是 | 批次状态唯一标识 |
| 诊断批次ID | diagnosis_batch_id | string | 是 | 全链路唯一批次 ID |
| 运行日期 | run_date | date | 是 | Codex 任务实际运行日期 |
| 统计月份 | stat_month | string | 是 | 本次任务处理的统计月份，格式 `YYYY-MM` |
| 触发方式 | trigger_type | enum | 是 | `定时触发`、`手动触发`、`补跑` |
| 任务范围 | task_scope | string | 是 | 全量、指定大区、指定小区或指定门店 |
| 经销商代码 | dealer_code | string | 否 | 门店级状态填门店编码；批次总览行可为空 |
| 任务环节编码 | step_code | enum | 是 | `拉取数据`、`生成诊断`、`写入表单`、`刷新表单数据集` |
| 任务环节名称 | step_name | string | 是 | 任务环节中文名称 |
| 环节状态 | step_status | enum | 是 | `待执行`、`运行中`、`成功`、`失败`、`跳过` |
| 源数据刷新状态 | source_refresh_status | enum | 否 | `已就绪`、`未就绪`、`部分就绪`、`未知` |
| 源数据刷新时间 | source_refreshed_at | datetime | 否 | 观远源数据集最近刷新完成时间 |
| 输入行数 | input_row_count | integer | 否 | 当前环节输入记录数 |
| 输出行数 | output_row_count | integer | 否 | 当前环节输出记录数 |
| 成功门店数 | success_store_count | integer | 否 | 当前批次或环节成功门店数 |
| 失败门店数 | failed_store_count | integer | 否 | 当前批次或环节失败门店数 |
| 目标表单名称 | target_form_name | string | 否 | 写表环节对应观远表单名称 |
| 写入状态 | write_status | enum | 否 | `新增`、`更新`、`插入或更新`、`失败` |
| 表单数据集刷新状态 | form_dataset_refresh_status | enum | 否 | `已就绪`、`未就绪`、`失败`、`未知` |
| 表单数据集刷新时间 | form_dataset_refreshed_at | datetime | 否 | 表单数据集可被前端读取的刷新时间 |
| 上一成功批次ID | last_success_batch_id | string | 否 | 当前批次失败时可回退展示的上一成功批次 |
| 错误编码 | error_code | string | 否 | 标准错误码 |
| 错误信息 | error_message | text | 否 | 保留原始错误摘要，便于排障 |
| 重试次数 | retry_count | integer | 是 | 当前批次或环节已重试次数 |
| 开始时间 | started_at | datetime | 是 | 环节开始时间 |
| 结束时间 | ended_at | datetime | 否 | 环节结束时间 |
| 脚本版本 | script_version | string | 是 | 本地诊断脚本版本 |
| 诊断模型版本 | diagnosis_model_version | string | 否 | 本批次诊断模型版本 |
| 诊断 Prompt 版本 | diagnosis_prompt_version | string | 否 | 本批次诊断 Prompt 版本 |

### 6.3 字段口径说明

- 失败批次不覆盖上一成功诊断结果。
- `last_success_batch_id` 用于前端展示“当前为上一成功批次结果”。
- `source_refresh_status` 用于区分源数据未刷新和 Codex 任务失败。
- 当前范围不包含前端点击“重新诊断”实时生成新批次；按钮在本期应理解为刷新读取最新已沉淀结果。

## 7. 非 Codex 表单输入数据

### 7.1 IP 打标明细数据集

IP 打标明细由数仓加工，Codex 和前端均只读取，不重复落表。字段能力至少需要覆盖：

| 字段能力 | 用途 |
|---|---|
| 经销商、月份、客户、顾问、线索、通话事件 | 支撑门店筛选、顾问分布和客户明细 |
| 是否接通、通话时长、首次外呼延迟、2 天外呼次数 | 支撑 IP 电话接通率、30s 以下线索占比、30 分钟外呼率、2 天 3 呼率 |
| 一级标签、二级标签、是否负向邀约 | 支撑负向邀约占比、标签分布和下钻筛选 |
| 问题摘要、证据摘录、标签命中原因、建议话术 | 支撑客户证据抽屉 |
| 风险等级、标签置信度、打标状态、模型版本、Prompt 版本 | 支撑高风险明细、质量复核和批次追溯 |

### 7.2 试驾打标明细数据集

试驾打标明细由数仓加工，Codex 和前端均只读取，不重复落表。字段能力至少需要覆盖：

| 字段能力 | 用途 |
|---|---|
| 经销商、月份、客户、顾问、试驾事件 | 支撑门店筛选、顾问分布和客户明细 |
| 试驾时间、试驾里程、试驾时长、试驾车系/车型 | 支撑试驾平均里程、平均时长和试驾明细 |
| 一级标签、二级标签、是否负向试驾接待 | 支撑负向试驾接待占比、标签分布和下钻筛选 |
| 问题摘要、证据摘录、标签命中原因、建议话术 | 支撑客户证据抽屉 |
| 风险等级、标签置信度、打标状态、模型版本、Prompt 版本 | 支撑高风险明细、质量复核和批次追溯 |

## 8. 前端计算边界

| 页面展示项 | 前端计算输入 | 前端是否可算 | 说明 |
|---|---|---:|---|
| 下发线索、到店、试驾、订单 | 门店销售指标数据集 | 是 | 当前门店、当前月份累计 |
| 线索到店率、到店试驾率、试驾订单率、线索订单率 | 销售指标分子分母 | 是 | 仅代表当前权限范围 |
| IP 电话接通率、30s 以下线索占比、30 分钟外呼率、2 天 3 呼率 | 门店过程指标或 IP 明细 | 是 | 指标分子分母需在数据口径文档锁定 |
| 试驾平均里程、平均时长 | 试驾过程指标或试驾明细 | 是 | 异常值剔除规则需在数据口径文档锁定 |
| 负向邀约占比、负向试驾接待占比 | 数仓打标明细中的负向标记 | 是 | 负向判定由数仓 AI 标签提供，前端只汇总 |
| 月环比、趋势方向 | 本期值、上期同口径值 | 是 | 上期周期口径需在数据口径文档锁定 |
| 标签分布、顾问分布 | 数仓打标明细 | 是 | 仅影响第三层明细，不反算官方诊断 |
| 明细筛选、客户抽屉 | 数仓打标明细 | 是 | 前端只筛选和展示，不改写标签和证据 |
| 小区排名、大区分位、跨组织对标 | 官方排名/分位结果表 | 否 | 必须读取预计算结果 |
| 主问题、断点、原因、优先动作 | 门店诊断结果表 | 否 | 必须读取 Codex 沉淀结果 |

## 9. 后续需进入数据口径文档的事项

以下内容不在本清单内最终锁死，需要进入独立数据口径文档逐项定义：

| 事项 | 需要定义的口径 |
|---|---|
| 销售漏斗指标 | 下发线索、到店、试驾、订单的来源、去重、过滤、归属周期 |
| 转化率 | 每个转化率的分子、分母、样本排除和除零处理 |
| 环比 | 自然月、MTD 同步天数或完整上月的选择 |
| IP 过程指标 | 接通、30s 以下、30 分钟外呼、2 天 3 呼的起算点和有效样本 |
| 试驾过程指标 | 试驾里程、试驾时长的来源、异常值和缺失处理 |
| 负向占比 | 负向标签口径、多标签去重和事件级/标签级汇总规则 |
| 官方排名/分位 | 排名范围、并列规则、剔除规则、越高越好/越低越好 |
| 样本不足 | 不同模块的最小样本量、提示文案和是否允许生成诊断 |
