# 单店销售诊断工作台 Codex 定时诊断任务数据源清单

## 1. 文档目的

本文档用于固化 Codex 每日定时诊断任务需要读取的输入数据源、计算口径和输出去向。

本文档只关注：

- 指标来自哪个观远数据集。
- 使用哪些字段计算。
- 按什么过滤条件、时间范围和组织范围聚合。
- Codex 计算后写入哪张输出表。

本文档不记录指标在前端或 BI 中的展示位置。展示位置仅作为核验口径时的参考，不进入正式数据契约。

## 2. 数据源总览

### 2.1 输入数据源

| 序号 | 数据源类型 | 数据集名称 | 数据集 dsId | 当前状态 | 主要用途 |
|---:|---|---|---|---|---|
| 1 | 销售漏斗指标源 | `[华为云][微批][三品牌]新零售门店级每日全量指标宽表&ads_sale_mart_new_sale_store_lvl_day_exnorm_comb_wms_his` | `gae00de628b274fdf837719d` | 已确认 | 计算下发线索、首触到店、首触试驾、首触订单、小区内自然月排名和分位 |
| 2 | 邀约过程指标源 | `【双品牌】DCC话务指标_182` | `fa1bfbd7736f34d1d8633883` | 部分确认 | 计算 30 分钟外呼率、2 天 3 呼达标率、首跟接通率、30s 以下线索占比 |
| 3 | IP 打标明细源 | `IP电话顾问邀约问题诊断表` | 待确认 | 表结构已确认，数据集开发中 | 读取邀约标签、标签正负向、证据和标签命中原因 |
| 4 | 试驾过程指标源 | `[准实时]试驾明细宽表&ads_sale_mart_trial_dtl_wide_comb_wms` | `c6428f1c9ca204859b553421` | 已确认 | 计算试驾平均里程、试驾平均时长 |
| 5 | 试驾打标明细源 | `试驾顾问接待问题诊断表` | 待确认 | 表结构已确认，数据集开发中 | 读取试驾接待标签、标签正负向、证据和标签命中原因 |
| 6 | 源数据刷新状态源 | 待确认 | 待确认 | 待确认 | 判断本轮诊断是否具备运行条件，并写入批次状态 |

说明：

- IP 打标明细源和试驾打标明细源由数仓加工好 AI 标签、证据及标签命中原因，Codex 不再重复沉淀标签明细。
- Codex 只基于标签明细做门店级诊断汇总，例如负向明细数、主要负向标签、典型证据摘要。
- 涉及跨组织对比的小区排名、小区分位等字段，必须由 Codex 写入输出表，不由前端临时计算官方口径。

### 2.2 联调与定时任务运行边界

| 项目 | 口径 |
|---|---|
| 真实打标数据集 | `IP电话顾问邀约问题诊断表`、`试驾顾问接待问题诊断表` 的真实观远 dsId、上线时间、刷新时间，待数据集上线后补充 |
| mock 联调 | 真实打标数据集上线前，仅允许 IP 打标明细源、试驾打标明细源使用同字段结构的 mock 数据；已确认的销售漏斗、DCC 话务、试驾过程指标必须使用 guancli 拉真实数据 |
| mock 批次标记 | mock 测试批次建议使用 `diagnosis_batch_id` 前缀 `MOCK_`，并在批次状态表中写入 `触发方式 = Mock测试` |
| 默认统计范围 | Codex 默认处理 `2026-03` 及之后的所有可用自然月数据 |
| 邀约过程过滤 | `免考核`、`线索免考核`、`需跟进`、`开业状态` 暂不作为剔除条件 |
| 正式切换条件 | 打标数据集真实 dsId 提供后，将 IP/试驾打标 mock 输入替换为观远数据集读取，并保留同一套输出表和写表逻辑 |

### 2.3 输出数据源

| 输出表 | 表单名称 | form 写入 fmId | 表单数据集 dsId | 用途 |
|---|---|---|---|---|
| 门店诊断结果表 | 门店诊断工作台-门店诊断结果表 | `a_11770b-f0d7-4349-a33d-00d76e485ef2` | `baf4b86a458ca4b2a8ce874d` | 存放门店主诊断、结果断点、过程原因、优先动作和诊断解释 |
| 官方排名分位结果表 | 门店诊断工作台-排名分位结果表 | `a_50f89c-da7c-46cc-aaa1-b09a8de9b5b5` | `xa257b3a018be4418b6100bc` | 存放自然月、小区范围内的官方排名和分位 |
| Codex 批次状态表 | 门店诊断工作台-Codex批次状态表 | `a_1c81a3-b2b6-469d-adae-a4950184fa2f` | `vef05c5e538074b2885f3445` | 存放每批诊断任务的运行状态、输入范围、错误信息和回退信息 |

输出表字段、主键和 upsert 规则详见 `单店销售诊断工作台_数据契约与写表联调清单.md`。

## 3. 销售漏斗指标源

### 3.1 数据集信息

| 项目 | 内容 |
|---|---|
| 数据集名称 | `[华为云][微批][三品牌]新零售门店级每日全量指标宽表&ads_sale_mart_new_sale_store_lvl_day_exnorm_comb_wms_his` |
| 数据集 dsId | `gae00de628b274fdf837719d` |
| 数据粒度 | 门店日粒度 |
| Codex 聚合粒度 | 经销商代码 + 自然月 |
| 当前用途 | 销售漏斗指标、结果断点判断、小区内官方排名和分位 |

### 3.2 字段映射

| 业务含义 | 数据集字段 | Codex 用法 |
|---|---|---|
| 日期 | `日yyyy-mm-dd` / `日yyyymmdd` / `月份` / `日期` | 用于筛选统计周期，并聚合到自然月 |
| 大区 | `大区简称` | 写入门店诊断结果表和排名分位结果表 |
| 小区 | `小区简称` | 写入门店诊断结果表和排名分位结果表，并作为小区排名范围 |
| 经销商代码 | `经销商代码` | 门店主键字段，用于识别一网门店和关联输出表 |
| 经销商简称 | `经销商简称` | 前端展示字段 |
| 下发线索 | `当日下发线索数` | 按自然月求和 |
| 首触到店 | `当日首触客流数` | 按自然月求和 |
| 首触试驾 | `当日首触试驾数` | 按自然月求和 |
| 首触订单 | `当日订单数（首触）` | 按自然月求和 |

### 3.3 销售漏斗指标口径

| 指标 | 计算口径 | 输出去向 |
|---|---|---|
| 下发线索 | 统计周期内 `当日下发线索数` 求和 | 门店诊断结果表，可由前端展示 |
| 首触到店 | 统计周期内 `当日首触客流数` 求和 | 门店诊断结果表，可由前端展示 |
| 首触试驾 | 统计周期内 `当日首触试驾数` 求和 | 门店诊断结果表，可由前端展示 |
| 首触订单 | 统计周期内 `当日订单数（首触）` 求和 | 门店诊断结果表，可由前端展示 |
| 到店率 | `首触到店 / 下发线索` | 前端展示公式；如参与官方诊断，可由 Codex 同口径计算 |
| 试驾率 | `首触试驾 / 首触到店` | 前端展示公式；如参与官方诊断，可由 Codex 同口径计算 |
| 订单率 | `首触订单 / 下发线索` | 前端展示公式；如参与官方诊断，可由 Codex 同口径计算 |

异常处理：

- 分母为 0 时，转化率为空，不按 0% 展示。
- 基础数值为空时按 0 参与求和。
- 同一经销商代码、同一自然月只生成一组门店漏斗聚合结果。

### 3.4 官方排名和分位口径

| 项目 | 口径 |
|---|---|
| 统计周期 | 自然月 |
| 排名范围 | 小区内 |
| 门店范围 | 只统计一网门店 |
| 一网门店判断 | `经销商代码` 长度等于 6 |
| 非一网门店判断 | `经销商代码` 长度大于 6 |
| 剔除规则 | 自然月下发线索量为 0 的门店，不参与排名和分位 |
| 排名总数 | 符合门店范围和剔除规则后的有效一网门店数 |
| 排名写入 | 写入官方排名分位结果表 |

排名字段至少覆盖以下指标：

- 下发线索。
- 首触到店。
- 首触试驾。
- 首触订单。
- 到店率。
- 试驾率。
- 订单率。

如果同一指标存在并列值，默认采用密集排名或标准竞争排名需要在脚本设计阶段固定；当前建议使用标准竞争排名，并保留 `排名总数` 便于前端解释。

## 4. 邀约过程指标源

### 4.1 数据集信息

| 项目 | 内容 |
|---|---|
| 数据集名称 | `【双品牌】DCC话务指标_182` |
| 数据集 dsId | `fa1bfbd7736f34d1d8633883` |
| 数据粒度 | 线索 / 电话跟进明细粒度 |
| Codex 聚合粒度 | 经销商代码 + 自然月 |
| 当前用途 | 邀约过程指标、邀约环节诊断输入 |

### 4.2 字段映射

| 业务含义 | 数据集字段 | Codex 用法 |
|---|---|---|
| 线索编码 | `线索编码` | 作为线索去重和计数基础 |
| 下发 CRM 时间 | `下发CRM时间` | 用于派生自然日和自然月 |
| 大区 | `大区简称` | 组织维度字段 |
| 小区 | `小区简称` | 组织维度字段 |
| 经销商代码 | `经销商代码` | 门店主键字段 |
| 经销商简称 | `经销商简称` | 前端展示字段 |
| 工作时段线索 | `是否工作时段线索（9-18）` | 识别 9:00-18:00 工作时段线索 |
| 工作时段 30 分钟跟进 | `工作时段30分钟跟进` | 计算 30 分钟外呼率 |
| 是否完成 72 小时三呼 | `是否完成72小时三呼` | 计算 2 天 3 呼达标率 |
| 首次通话时长 | `首次通话时长` | 计算首跟接通率 |
| 72 小时总通话时长 | `72小时总通话时长` | 计算 30s 以下线索占比 |
| 线索渠道大类 | `线索渠道大类名称` | 后续可作为筛选或诊断维度 |
| 品牌 | `品牌名称` | 后续可作为筛选或诊断维度 |
| 线索状态 | `CRM线索状态名称` | 后续可作为筛选或剔除条件 |
| 开业状态 | `开业状态` | 后续可作为门店范围过滤条件 |
| 是否免考核 | `线索免考核` / `免考核` | 后续用于剔除免考核线索 |
| 是否需跟进 | `需跟进` | 后续用于剔除无需跟进线索 |
| AB 网类型 | `经销商AB网` | 后续用于门店范围过滤 |

### 4.3 邀约过程指标口径

| 指标 | 分子 | 分母 | 计算口径 | 输出去向 |
|---|---|---|---|---|
| 30 分钟外呼率 | `工作时段30分钟跟进 = 是` 的线索数 | `是否工作时段线索（9-18） = 工作时段` 的线索数 | 分子 / 分母 | 门店诊断结果表，可由前端展示 |
| 2 天 3 呼达标率 | `是否完成72小时三呼 = 是` 的线索数 | `线索编码` 计数 | 分子 / 分母 | 门店诊断结果表，可由前端展示 |
| 首跟接通率 | `首次通话时长 > 0` 且不为空的线索数 | `首次通话时长` 不为空的线索数 | 分子 / 分母 | 门店诊断结果表，可由前端展示 |
| 30s 以下线索占比 | 去重统计 `72小时总通话时长 < 30` 或 `72小时总通话时长` 为空、null 的 `线索编码` 数 | `count(distinct 线索编码)` | 分子 / 分母 | 门店诊断结果表，可由前端展示 |

异常处理：

- 分母为 0 时，指标为空，不按 0% 展示。
- `线索编码` 为空的记录不参与线索计数。
- 如同一线索存在多条电话记录，首跟类指标按数据集已加工字段计算，不在 Codex 中重新识别首通电话。
- 30s 以下线索占比按 `线索编码` 去重；`72小时总通话时长` 为空或 null 时计入 30s 以下线索数。
- `免考核`、`线索免考核`、`需跟进`、`开业状态` 暂不作为剔除条件。

## 5. IP 打标明细源

当前状态：表结构已确认，观远数据集 dsId 待确认。

数据集 dsId、上线时间、刷新时间待数据集上线后补充。

表结构来源：`/Users/chengfengguo/Downloads/IP电话、试驾 顾问问题标签提取.docx` 中嵌入的 `IP电话顾问邀约问题诊断表`。

已确认业务边界：

- 数仓明细表已经加工好 AI 标签、证据及标签命中原因。
- Codex 不沉淀明细标签本身，只读取并汇总。
- 负向邀约判断统一使用明细表中的 `标签正负向 = 负向`。
- 负向明细数阈值按模块分别判断，邀约模块为邀约负向明细数独立判断。
- 负向明细数大于 10 时，认为该模块负向样本量足以支撑诊断判断。

字段结构：

| 业务含义 | 表字段 | 用途 |
|---|---|---|
| IP 主键 | `IPID` | IP 周期或业务主键，沿用数仓逻辑 |
| IP 呼叫 ID | `IP呼叫ID` | 电话呼叫明细主键，用于事件级去重 |
| 呼叫开始时间 | `呼叫开始时间` | 归属诊断月份，支撑明细时间展示 |
| 通话时长 | `通话时长(单位:秒)` | 支撑通话时长展示和排查 |
| 周期首次意向闭环车系 | `周期首次意向闭环车系名称` | 客户意向车系展示和筛选 |
| 周期最近意向闭环车系 | `周期最近意向闭环车系名称` | 客户最近意向车系展示和筛选 |
| 最近线索渠道大类 | `最近线索渠道大类名称` | 渠道维度筛选或诊断解释 |
| 品牌 | `品牌名称` | 品牌筛选 |
| 车系 | `车系名称` | 车系筛选 |
| 大区 | `大区名称` / `大区编码` | 组织维度和权限字段 |
| 小区 | `小区名称` / `小区编码` | 组织维度和权限字段 |
| 经销商 | `经销商代码` / `经销商简称` | 关联门店和前端展示 |
| 周期创建顾问 | `周期创建顾问名称` / `周期创建顾问ID` | 线索周期创建顾问维度 |
| 客户 | `客户手机号码` / `客户姓名` | 客户明细和证据抽屉，前端展示需脱敏 |
| 周期 | `周期编码` | 周期维度关联 |
| 周期创建渠道大类 | `周期创建渠道大类名称` | 渠道维度筛选或诊断解释 |
| 顾问 | `顾问编码` / `顾问名称` | 顾问问题分布，逻辑取话务记录明细宽表中的 `cons_id`、`cons_name` |
| 一级标签 | `一级标签` | 邀约问题标签分布 |
| 二级标签 | `二级标签` | 邀约问题标签细分 |
| 标签正负向 | `标签正负向` | 判断负向邀约，负向口径固定为 `标签正负向 = 负向` |
| 证据 | `证据原文` | 证据抽屉和典型证据摘要 |
| 命中原因 | `命中原因` | 解释标签命中原因 |

来源逻辑：

- 沿用 `dm_sale_mart.ads_sale_mart_ipcall_cons_follow_ai_analy_d_inv_dig_comb_wms` 逻辑。
- 顾问编码、顾问名称取话务记录明细宽表 `ads_sale_mart_ipcall_recd_dtl_wide_comb_wms` 中的 `cons_id`、`cons_name`。

Codex 汇总口径：

| 指标或字段 | 口径 |
|---|---|
| 邀约样本量 | 当前门店当前月份，IP 明细中有通话原文的事件级去重数量 |
| 邀约负向明细数 | 当前门店当前月份，事件级去重且 `标签正负向 = 负向` 的数量 |
| 负向邀约占比 | 邀约负向明细数 / 邀约样本量 |
| TOP 负向邀约标签 | 在 `标签正负向 = 负向` 范围内，按一级标签、二级标签统计数量 |
| 顾问问题分布 | 在 `标签正负向 = 负向` 范围内，按顾问编码、顾问名称统计数量 |

## 6. 试驾过程指标源

当前状态：观远数据集已确认。

### 6.1 数据集信息

| 项目 | 内容 |
|---|---|
| 数据集名称 | `[准实时]试驾明细宽表&ads_sale_mart_trial_dtl_wide_comb_wms` |
| 数据集 dsId | `c6428f1c9ca204859b553421` |
| 数据粒度 | 试驾明细粒度 |
| Codex 聚合粒度 | 经销商代码 + 自然月 |
| 当前用途 | 试驾平均里程、试驾平均时长 |

已确认业务边界：

- 过程诊断先聚焦邀约和试驾接待两个环节。
- 试驾转化不足、跟进勤奋度不足暂不纳入本轮主问题。
- 试驾过程基础数值可以由前端基于当前门店权限范围计算展示。
- 如涉及跨组织排名、分位或官方诊断结果，需要由 Codex 预计算并写入输出表。

### 6.2 字段映射

| 业务含义 | 数据集字段 | Codex 用法 |
|---|---|---|
| 试驾接待编码 | `试驾接待编码` | 试驾明细主键，支撑试驾事件去重 |
| 试驾状态 | `试驾状态名称` | 试驾状态展示或排查 |
| 是否成功试驾 | `是否成功试驾` | 过滤条件，固定筛选 `是否成功试驾 = 是` |
| 试驾里程 | `试驾里程` | 计算试驾平均里程 |
| 试驾时长 | `试驾时长(分钟)` | 计算试驾平均时长 |
| 试驾接待日期 | `试驾接待日期` | 归属诊断月份 |
| 试驾接待时间 | `试驾接待时间` | 明细时间展示和排查 |
| 试驾接待顾问 | `试驾接待顾问编码` / `试驾接待顾问名称` | 顾问维度展示或排查 |
| 经销商 | `试驾接待经销商代码` / `试驾接待经销商简称` | 关联门店和前端展示 |
| 大区 | `试驾接待大区代码` / `试驾接待大区名称` / `试驾接待大区简称` | 组织维度和权限字段 |
| 小区 | `试驾接待小区代码` / `试驾接待小区名称` / `试驾接待小区简称` | 组织维度和权限字段 |
| 品牌 | `品牌代码` / `品牌名称` | 品牌筛选 |
| 车系 | `车系代码` / `车系名称` / `闭环车系名称` | 车系筛选 |

### 6.3 试驾过程指标口径

| 指标或字段 | 口径 |
|---|---|
| 试驾平均里程 | 当前门店当前月份，筛选 `是否成功试驾 = 是` 后，`试驾里程` 求和 / 按 `试驾接待编码` 去重的有效试驾事件数 |
| 试驾平均时长 | 当前门店当前月份，筛选 `是否成功试驾 = 是` 后，`试驾时长(分钟)` 求和 / 按 `试驾接待编码` 去重的有效试驾事件数 |
| 试驾过程样本量 | 当前门店当前月份，筛选 `是否成功试驾 = 是` 后，按 `试驾接待编码` 去重的有效试驾事件数 |

## 7. 试驾打标明细源

当前状态：表结构已确认，观远数据集 dsId 待确认。

试驾打标明细源使用开发中的 `试驾顾问接待问题诊断表`，与试驾平均里程、试驾平均时长所使用的试驾明细宽表不同。

数据集 dsId、上线时间、刷新时间待数据集上线后补充。

已确认业务边界：

- 数仓明细表已经加工好 AI 标签、证据及标签命中原因。
- Codex 不沉淀明细标签本身，只读取并汇总。
- 负向试驾接待判断统一使用明细表中的 `标签正负向 = 负向`。
- 负向明细数阈值按模块分别判断，试驾接待模块为试驾负向明细数独立判断。
- 负向明细数大于 10 时，认为该模块负向样本量足以支撑诊断判断。

Codex 汇总口径：

| 指标或字段 | 口径 |
|---|---|
| 试驾负向明细数 | 当前门店当前月份，按 `试驾清单ID` 去重且 `标签正负向 = 负向` 的数量 |
| 试驾打标样本量 | 当前门店当前月份，有试驾原文且按 `试驾清单ID` 去重的试驾事件数 |
| 负向试驾接待占比 | 试驾负向明细数 / 试驾打标样本量 |
| TOP 负向试驾标签 | 在 `标签正负向 = 负向` 范围内，按一级标签、二级标签统计数量 |
| 顾问问题分布 | 在 `标签正负向 = 负向` 范围内，按试驾接待顾问编码、试驾接待顾问名称统计数量 |
| 典型证据摘要 | 读取 `证据原文` 和 `命中原因`，由 Codex 汇总为门店级摘要 |

## 8. 源数据刷新状态源

当前状态：待确认数据源。

Codex 定时任务至少需要判断以下输入是否已刷新到可用状态：

| 输入源 | 刷新判断 | 未刷新处理 |
|---|---|---|
| 销售漏斗指标源 | 目标统计周期数据可查询 | 批次状态写入 `跳过` 或 `失败`，不产出新诊断结果 |
| 邀约过程指标源 | 目标统计周期数据可查询 | 批次状态写入 `跳过` 或 `失败`，不产出新诊断结果 |
| IP 打标明细源 | 正式阶段为目标统计周期标签明细可查询；mock 阶段为 mock 文件可读取且字段匹配 | 批次状态写入 `跳过` 或 `失败`，不产出新诊断结果 |
| 试驾过程指标源 | 目标统计周期数据可查询 | 批次状态写入 `跳过` 或 `失败`，不产出新诊断结果 |
| 试驾打标明细源 | 正式阶段为目标统计周期标签明细可查询；mock 阶段为 mock 文件可读取且字段匹配 | 批次状态写入 `跳过` 或 `失败`，不产出新诊断结果 |

如果某个输入源未刷新，Codex 不应生成半口径诊断结果；可以保留上一成功批次供前端读取，并在批次状态表记录本轮失败原因。

## 9. 当前待补齐事项

| 优先级 | 事项 | 需要确认内容 |
|---:|---|---|
| P0 | IP 打标明细源 | 真实数据集 dsId、上线时间、刷新时间、是否存在行权限字段；mock 阶段不阻塞流程测试 |
| P0 | 试驾打标明细源 | 真实数据集 dsId、上线时间、刷新时间；mock 阶段不阻塞流程测试 |
| P1 | 源数据刷新状态源 | 正式阶段可先用目标周期数据可查询作为就绪判断；如后续有刷新日志表再替换 |
| P1 | 排名并列规则 | 标准竞争排名或密集排名，建议脚本设计阶段固定一种 |
